# Advanced DataWeave Questions 00501-00750

Questions 501–750 of 1000 in the Advanced level.

## DW-09501 — Select name. Example 51, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.name
```

### Expected Output
```json
"Narsing"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Narsing".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09502 — Project only name. Example 51, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ name: payload.name }
```

### Expected Output
```json
{"name": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09503 — Wrap name under value. Example 51, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09504 — Uppercase name. Example 51, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.name) }
```

### Expected Output
```json
{"upper": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09505 — Return the length of name. Example 51, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.name) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09506 — Read name with a default. Example 51, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09507 — Check whether name is non-null. Example 51, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.name != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09508 — Add source metadata. Example 51, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"name": "Narsing", "index": 51, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing", "index": 51, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09509 — Return field metadata. Example 51, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "name", keyValue: payload.name }
```

### Expected Output
```json
{"keyName": "name", "keyValue": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "name", "keyValue": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09510 — Check whether name is a String. Example 51, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.name is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09511 — Select city. Example 52, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.city
```

### Expected Output
```json
"Hyderabad"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Hyderabad".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09512 — Project only city. Example 52, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ city: payload.city }
```

### Expected Output
```json
{"city": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09513 — Wrap city under value. Example 52, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09514 — Uppercase city. Example 52, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.city) }
```

### Expected Output
```json
{"upper": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09515 — Return the length of city. Example 52, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.city) }
```

### Expected Output
```json
{"length": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09516 — Read city with a default. Example 52, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09517 — Check whether city is non-null. Example 52, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.city != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09518 — Add source metadata. Example 52, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"city": "Hyderabad", "index": 52, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad", "index": 52, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09519 — Return field metadata. Example 52, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "city", keyValue: payload.city }
```

### Expected Output
```json
{"keyName": "city", "keyValue": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "city", "keyValue": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09520 — Check whether city is a String. Example 52, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.city is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09521 — Select country. Example 53, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.country
```

### Expected Output
```json
"India"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "India".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09522 — Project only country. Example 53, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ country: payload.country }
```

### Expected Output
```json
{"country": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09523 — Wrap country under value. Example 53, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09524 — Uppercase country. Example 53, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.country) }
```

### Expected Output
```json
{"upper": "INDIA"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "INDIA"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09525 — Return the length of country. Example 53, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.country) }
```

### Expected Output
```json
{"length": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09526 — Read country with a default. Example 53, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country default "UNKNOWN" }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09527 — Check whether country is non-null. Example 53, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.country != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09528 — Add source metadata. Example 53, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"country": "India", "index": 53, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India", "index": 53, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09529 — Return field metadata. Example 53, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "country", keyValue: payload.country }
```

### Expected Output
```json
{"keyName": "country", "keyValue": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "country", "keyValue": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09530 — Check whether country is a String. Example 53, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.country is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09531 — Select status. Example 54, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.status
```

### Expected Output
```json
"ACTIVE"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "ACTIVE".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09532 — Project only status. Example 54, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ status: payload.status }
```

### Expected Output
```json
{"status": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09533 — Wrap status under value. Example 54, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09534 — Uppercase status. Example 54, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.status) }
```

### Expected Output
```json
{"upper": "ACTIVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09535 — Return the length of status. Example 54, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.status) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09536 — Read status with a default. Example 54, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status default "UNKNOWN" }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09537 — Check whether status is non-null. Example 54, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.status != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09538 — Add source metadata. Example 54, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"status": "ACTIVE", "index": 54, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE", "index": 54, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09539 — Return field metadata. Example 54, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "status", keyValue: payload.status }
```

### Expected Output
```json
{"keyName": "status", "keyValue": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "status", "keyValue": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09540 — Check whether status is a String. Example 54, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.status is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09541 — Select category. Example 55, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.category
```

### Expected Output
```json
"PREMIUM"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "PREMIUM".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09542 — Project only category. Example 55, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ category: payload.category }
```

### Expected Output
```json
{"category": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09543 — Wrap category under value. Example 55, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09544 — Uppercase category. Example 55, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.category) }
```

### Expected Output
```json
{"upper": "PREMIUM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09545 — Return the length of category. Example 55, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.category) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09546 — Read category with a default. Example 55, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category default "UNKNOWN" }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09547 — Check whether category is non-null. Example 55, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.category != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09548 — Add source metadata. Example 55, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"category": "PREMIUM", "index": 55, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM", "index": 55, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09549 — Return field metadata. Example 55, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "category", keyValue: payload.category }
```

### Expected Output
```json
{"keyName": "category", "keyValue": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "category", "keyValue": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09550 — Check whether category is a String. Example 55, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.category is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09551 — Select department. Example 56, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.department
```

### Expected Output
```json
"IT"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "IT".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09552 — Project only department. Example 56, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ department: payload.department }
```

### Expected Output
```json
{"department": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09553 — Wrap department under value. Example 56, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09554 — Uppercase department. Example 56, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.department) }
```

### Expected Output
```json
{"upper": "IT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09555 — Return the length of department. Example 56, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.department) }
```

### Expected Output
```json
{"length": 2}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09556 — Read department with a default. Example 56, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department default "UNKNOWN" }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09557 — Check whether department is non-null. Example 56, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.department != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09558 — Add source metadata. Example 56, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"department": "IT", "index": 56, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT", "index": 56, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09559 — Return field metadata. Example 56, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "department", keyValue: payload.department }
```

### Expected Output
```json
{"keyName": "department", "keyValue": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "department", "keyValue": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09560 — Check whether department is a String. Example 56, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.department is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09561 — Select code. Example 57, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.code
```

### Expected Output
```json
"C100"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "C100".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09562 — Project only code. Example 57, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ code: payload.code }
```

### Expected Output
```json
{"code": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09563 — Wrap code under value. Example 57, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09564 — Uppercase code. Example 57, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.code) }
```

### Expected Output
```json
{"upper": "C100"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09565 — Return the length of code. Example 57, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.code) }
```

### Expected Output
```json
{"length": 4}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09566 — Read code with a default. Example 57, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code default "UNKNOWN" }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09567 — Check whether code is non-null. Example 57, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.code != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09568 — Add source metadata. Example 57, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"code": "C100", "index": 57, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100", "index": 57, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09569 — Return field metadata. Example 57, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "code", keyValue: payload.code }
```

### Expected Output
```json
{"keyName": "code", "keyValue": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "code", "keyValue": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09570 — Check whether code is a String. Example 57, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.code is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09571 — Select type. Example 58, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.type
```

### Expected Output
```json
"CUSTOMER"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "CUSTOMER".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09572 — Project only type. Example 58, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ type: payload.type }
```

### Expected Output
```json
{"type": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09573 — Wrap type under value. Example 58, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09574 — Uppercase type. Example 58, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.type) }
```

### Expected Output
```json
{"upper": "CUSTOMER"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09575 — Return the length of type. Example 58, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.type) }
```

### Expected Output
```json
{"length": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09576 — Read type with a default. Example 58, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type default "UNKNOWN" }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09577 — Check whether type is non-null. Example 58, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.type != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09578 — Add source metadata. Example 58, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"type": "CUSTOMER", "index": 58, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER", "index": 58, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09579 — Return field metadata. Example 58, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "type", keyValue: payload.type }
```

### Expected Output
```json
{"keyName": "type", "keyValue": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "type", "keyValue": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09580 — Check whether type is a String. Example 58, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.type is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09581 — Select email. Example 59, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.email
```

### Expected Output
```json
"a@example.com"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "a@example.com".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09582 — Project only email. Example 59, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ email: payload.email }
```

### Expected Output
```json
{"email": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09583 — Wrap email under value. Example 59, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09584 — Uppercase email. Example 59, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.email) }
```

### Expected Output
```json
{"upper": "A@EXAMPLE.COM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "A@EXAMPLE.COM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09585 — Return the length of email. Example 59, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.email) }
```

### Expected Output
```json
{"length": 13}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09586 — Read email with a default. Example 59, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email default "UNKNOWN" }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09587 — Check whether email is non-null. Example 59, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.email != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09588 — Add source metadata. Example 59, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"email": "a@example.com", "index": 59, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com", "index": 59, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09589 — Return field metadata. Example 59, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "email", keyValue: payload.email }
```

### Expected Output
```json
{"keyName": "email", "keyValue": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "email", "keyValue": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09590 — Check whether email is a String. Example 59, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.email is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09591 — Select description. Example 60, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.description
```

### Expected Output
```json
"Sample"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Sample".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09592 — Project only description. Example 60, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ description: payload.description }
```

### Expected Output
```json
{"description": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09593 — Wrap description under value. Example 60, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09594 — Uppercase description. Example 60, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.description) }
```

### Expected Output
```json
{"upper": "SAMPLE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "SAMPLE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09595 — Return the length of description. Example 60, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.description) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09596 — Read description with a default. Example 60, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09597 — Check whether description is non-null. Example 60, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.description != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09598 — Add source metadata. Example 60, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"description": "Sample", "index": 60, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample", "index": 60, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09599 — Return field metadata. Example 60, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "description", keyValue: payload.description }
```

### Expected Output
```json
{"keyName": "description", "keyValue": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "description", "keyValue": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09600 — Check whether description is a String. Example 60, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.description is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09601 — Select name. Example 61, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.name
```

### Expected Output
```json
"Narsing"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Narsing".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09602 — Project only name. Example 61, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ name: payload.name }
```

### Expected Output
```json
{"name": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09603 — Wrap name under value. Example 61, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09604 — Uppercase name. Example 61, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.name) }
```

### Expected Output
```json
{"upper": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09605 — Return the length of name. Example 61, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.name) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09606 — Read name with a default. Example 61, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09607 — Check whether name is non-null. Example 61, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.name != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09608 — Add source metadata. Example 61, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"name": "Narsing", "index": 61, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing", "index": 61, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09609 — Return field metadata. Example 61, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "name", keyValue: payload.name }
```

### Expected Output
```json
{"keyName": "name", "keyValue": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "name", "keyValue": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09610 — Check whether name is a String. Example 61, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.name is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09611 — Select city. Example 62, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.city
```

### Expected Output
```json
"Hyderabad"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Hyderabad".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09612 — Project only city. Example 62, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ city: payload.city }
```

### Expected Output
```json
{"city": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09613 — Wrap city under value. Example 62, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09614 — Uppercase city. Example 62, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.city) }
```

### Expected Output
```json
{"upper": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09615 — Return the length of city. Example 62, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.city) }
```

### Expected Output
```json
{"length": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09616 — Read city with a default. Example 62, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09617 — Check whether city is non-null. Example 62, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.city != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09618 — Add source metadata. Example 62, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"city": "Hyderabad", "index": 62, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad", "index": 62, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09619 — Return field metadata. Example 62, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "city", keyValue: payload.city }
```

### Expected Output
```json
{"keyName": "city", "keyValue": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "city", "keyValue": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09620 — Check whether city is a String. Example 62, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.city is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09621 — Select country. Example 63, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.country
```

### Expected Output
```json
"India"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "India".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09622 — Project only country. Example 63, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ country: payload.country }
```

### Expected Output
```json
{"country": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09623 — Wrap country under value. Example 63, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09624 — Uppercase country. Example 63, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.country) }
```

### Expected Output
```json
{"upper": "INDIA"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "INDIA"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09625 — Return the length of country. Example 63, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.country) }
```

### Expected Output
```json
{"length": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09626 — Read country with a default. Example 63, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country default "UNKNOWN" }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09627 — Check whether country is non-null. Example 63, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.country != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09628 — Add source metadata. Example 63, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"country": "India", "index": 63, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India", "index": 63, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09629 — Return field metadata. Example 63, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "country", keyValue: payload.country }
```

### Expected Output
```json
{"keyName": "country", "keyValue": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "country", "keyValue": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09630 — Check whether country is a String. Example 63, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.country is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09631 — Select status. Example 64, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.status
```

### Expected Output
```json
"ACTIVE"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "ACTIVE".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09632 — Project only status. Example 64, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ status: payload.status }
```

### Expected Output
```json
{"status": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09633 — Wrap status under value. Example 64, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09634 — Uppercase status. Example 64, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.status) }
```

### Expected Output
```json
{"upper": "ACTIVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09635 — Return the length of status. Example 64, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.status) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09636 — Read status with a default. Example 64, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status default "UNKNOWN" }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09637 — Check whether status is non-null. Example 64, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.status != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09638 — Add source metadata. Example 64, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"status": "ACTIVE", "index": 64, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE", "index": 64, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09639 — Return field metadata. Example 64, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "status", keyValue: payload.status }
```

### Expected Output
```json
{"keyName": "status", "keyValue": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "status", "keyValue": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09640 — Check whether status is a String. Example 64, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.status is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09641 — Select category. Example 65, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.category
```

### Expected Output
```json
"PREMIUM"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "PREMIUM".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09642 — Project only category. Example 65, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ category: payload.category }
```

### Expected Output
```json
{"category": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09643 — Wrap category under value. Example 65, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09644 — Uppercase category. Example 65, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.category) }
```

### Expected Output
```json
{"upper": "PREMIUM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09645 — Return the length of category. Example 65, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.category) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09646 — Read category with a default. Example 65, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category default "UNKNOWN" }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09647 — Check whether category is non-null. Example 65, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.category != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09648 — Add source metadata. Example 65, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"category": "PREMIUM", "index": 65, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM", "index": 65, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09649 — Return field metadata. Example 65, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "category", keyValue: payload.category }
```

### Expected Output
```json
{"keyName": "category", "keyValue": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "category", "keyValue": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09650 — Check whether category is a String. Example 65, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.category is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09651 — Select department. Example 66, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.department
```

### Expected Output
```json
"IT"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "IT".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09652 — Project only department. Example 66, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ department: payload.department }
```

### Expected Output
```json
{"department": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09653 — Wrap department under value. Example 66, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09654 — Uppercase department. Example 66, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.department) }
```

### Expected Output
```json
{"upper": "IT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09655 — Return the length of department. Example 66, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.department) }
```

### Expected Output
```json
{"length": 2}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09656 — Read department with a default. Example 66, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department default "UNKNOWN" }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09657 — Check whether department is non-null. Example 66, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.department != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09658 — Add source metadata. Example 66, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"department": "IT", "index": 66, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT", "index": 66, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09659 — Return field metadata. Example 66, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "department", keyValue: payload.department }
```

### Expected Output
```json
{"keyName": "department", "keyValue": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "department", "keyValue": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09660 — Check whether department is a String. Example 66, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.department is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09661 — Select code. Example 67, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.code
```

### Expected Output
```json
"C100"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "C100".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09662 — Project only code. Example 67, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ code: payload.code }
```

### Expected Output
```json
{"code": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09663 — Wrap code under value. Example 67, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09664 — Uppercase code. Example 67, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.code) }
```

### Expected Output
```json
{"upper": "C100"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09665 — Return the length of code. Example 67, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.code) }
```

### Expected Output
```json
{"length": 4}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09666 — Read code with a default. Example 67, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code default "UNKNOWN" }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09667 — Check whether code is non-null. Example 67, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.code != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09668 — Add source metadata. Example 67, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"code": "C100", "index": 67, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100", "index": 67, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09669 — Return field metadata. Example 67, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "code", keyValue: payload.code }
```

### Expected Output
```json
{"keyName": "code", "keyValue": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "code", "keyValue": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09670 — Check whether code is a String. Example 67, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.code is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09671 — Select type. Example 68, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.type
```

### Expected Output
```json
"CUSTOMER"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "CUSTOMER".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09672 — Project only type. Example 68, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ type: payload.type }
```

### Expected Output
```json
{"type": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09673 — Wrap type under value. Example 68, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09674 — Uppercase type. Example 68, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.type) }
```

### Expected Output
```json
{"upper": "CUSTOMER"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09675 — Return the length of type. Example 68, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.type) }
```

### Expected Output
```json
{"length": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09676 — Read type with a default. Example 68, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type default "UNKNOWN" }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09677 — Check whether type is non-null. Example 68, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.type != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09678 — Add source metadata. Example 68, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"type": "CUSTOMER", "index": 68, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER", "index": 68, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09679 — Return field metadata. Example 68, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "type", keyValue: payload.type }
```

### Expected Output
```json
{"keyName": "type", "keyValue": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "type", "keyValue": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09680 — Check whether type is a String. Example 68, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.type is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09681 — Select email. Example 69, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.email
```

### Expected Output
```json
"a@example.com"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "a@example.com".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09682 — Project only email. Example 69, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ email: payload.email }
```

### Expected Output
```json
{"email": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09683 — Wrap email under value. Example 69, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09684 — Uppercase email. Example 69, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.email) }
```

### Expected Output
```json
{"upper": "A@EXAMPLE.COM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "A@EXAMPLE.COM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09685 — Return the length of email. Example 69, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.email) }
```

### Expected Output
```json
{"length": 13}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09686 — Read email with a default. Example 69, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email default "UNKNOWN" }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09687 — Check whether email is non-null. Example 69, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.email != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09688 — Add source metadata. Example 69, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"email": "a@example.com", "index": 69, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com", "index": 69, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09689 — Return field metadata. Example 69, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "email", keyValue: payload.email }
```

### Expected Output
```json
{"keyName": "email", "keyValue": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "email", "keyValue": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09690 — Check whether email is a String. Example 69, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.email is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09691 — Select description. Example 70, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.description
```

### Expected Output
```json
"Sample"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Sample".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09692 — Project only description. Example 70, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ description: payload.description }
```

### Expected Output
```json
{"description": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09693 — Wrap description under value. Example 70, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09694 — Uppercase description. Example 70, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.description) }
```

### Expected Output
```json
{"upper": "SAMPLE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "SAMPLE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09695 — Return the length of description. Example 70, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.description) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09696 — Read description with a default. Example 70, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09697 — Check whether description is non-null. Example 70, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.description != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09698 — Add source metadata. Example 70, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"description": "Sample", "index": 70, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample", "index": 70, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09699 — Return field metadata. Example 70, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "description", keyValue: payload.description }
```

### Expected Output
```json
{"keyName": "description", "keyValue": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "description", "keyValue": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09700 — Check whether description is a String. Example 70, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.description is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09701 — Select name. Example 71, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.name
```

### Expected Output
```json
"Narsing"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Narsing".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09702 — Project only name. Example 71, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ name: payload.name }
```

### Expected Output
```json
{"name": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09703 — Wrap name under value. Example 71, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09704 — Uppercase name. Example 71, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.name) }
```

### Expected Output
```json
{"upper": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09705 — Return the length of name. Example 71, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.name) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09706 — Read name with a default. Example 71, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09707 — Check whether name is non-null. Example 71, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.name != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09708 — Add source metadata. Example 71, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"name": "Narsing", "index": 71, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing", "index": 71, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09709 — Return field metadata. Example 71, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "name", keyValue: payload.name }
```

### Expected Output
```json
{"keyName": "name", "keyValue": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "name", "keyValue": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09710 — Check whether name is a String. Example 71, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.name is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09711 — Select city. Example 72, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.city
```

### Expected Output
```json
"Hyderabad"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Hyderabad".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09712 — Project only city. Example 72, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ city: payload.city }
```

### Expected Output
```json
{"city": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09713 — Wrap city under value. Example 72, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09714 — Uppercase city. Example 72, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.city) }
```

### Expected Output
```json
{"upper": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09715 — Return the length of city. Example 72, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.city) }
```

### Expected Output
```json
{"length": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09716 — Read city with a default. Example 72, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09717 — Check whether city is non-null. Example 72, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.city != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09718 — Add source metadata. Example 72, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"city": "Hyderabad", "index": 72, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad", "index": 72, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09719 — Return field metadata. Example 72, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "city", keyValue: payload.city }
```

### Expected Output
```json
{"keyName": "city", "keyValue": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "city", "keyValue": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09720 — Check whether city is a String. Example 72, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.city is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09721 — Select country. Example 73, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.country
```

### Expected Output
```json
"India"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "India".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09722 — Project only country. Example 73, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ country: payload.country }
```

### Expected Output
```json
{"country": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09723 — Wrap country under value. Example 73, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09724 — Uppercase country. Example 73, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.country) }
```

### Expected Output
```json
{"upper": "INDIA"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "INDIA"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09725 — Return the length of country. Example 73, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.country) }
```

### Expected Output
```json
{"length": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09726 — Read country with a default. Example 73, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country default "UNKNOWN" }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09727 — Check whether country is non-null. Example 73, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.country != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09728 — Add source metadata. Example 73, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"country": "India", "index": 73, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India", "index": 73, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09729 — Return field metadata. Example 73, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "country", keyValue: payload.country }
```

### Expected Output
```json
{"keyName": "country", "keyValue": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "country", "keyValue": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09730 — Check whether country is a String. Example 73, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.country is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09731 — Select status. Example 74, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.status
```

### Expected Output
```json
"ACTIVE"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "ACTIVE".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09732 — Project only status. Example 74, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ status: payload.status }
```

### Expected Output
```json
{"status": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09733 — Wrap status under value. Example 74, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09734 — Uppercase status. Example 74, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.status) }
```

### Expected Output
```json
{"upper": "ACTIVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09735 — Return the length of status. Example 74, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.status) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09736 — Read status with a default. Example 74, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status default "UNKNOWN" }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09737 — Check whether status is non-null. Example 74, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.status != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09738 — Add source metadata. Example 74, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"status": "ACTIVE", "index": 74, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE", "index": 74, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09739 — Return field metadata. Example 74, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "status", keyValue: payload.status }
```

### Expected Output
```json
{"keyName": "status", "keyValue": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "status", "keyValue": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09740 — Check whether status is a String. Example 74, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.status is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09741 — Select category. Example 75, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.category
```

### Expected Output
```json
"PREMIUM"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "PREMIUM".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09742 — Project only category. Example 75, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ category: payload.category }
```

### Expected Output
```json
{"category": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09743 — Wrap category under value. Example 75, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09744 — Uppercase category. Example 75, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.category) }
```

### Expected Output
```json
{"upper": "PREMIUM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09745 — Return the length of category. Example 75, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.category) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09746 — Read category with a default. Example 75, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category default "UNKNOWN" }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09747 — Check whether category is non-null. Example 75, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.category != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09748 — Add source metadata. Example 75, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"category": "PREMIUM", "index": 75, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM", "index": 75, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09749 — Return field metadata. Example 75, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "category", keyValue: payload.category }
```

### Expected Output
```json
{"keyName": "category", "keyValue": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "category", "keyValue": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-09750 — Check whether category is a String. Example 75, family 7.

**Difficulty:** Advanced
**Topic:** Dates & DateTime

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.category is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
