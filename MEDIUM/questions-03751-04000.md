# Medium DataWeave Questions 03751-04000

Questions 3751–4000 of 5000 in the Medium level.

## DW-07751 — Select department. Example 76, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 76}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.department
```

### Expected Output
```json
"IT"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "IT".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07752 — Project only department. Example 76, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 76}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ department: payload.department }
```

### Expected Output
```json
{"department": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07753 — Wrap department under value. Example 76, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 76}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07754 — Uppercase department. Example 76, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 76}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.department) }
```

### Expected Output
```json
{"upper": "IT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07755 — Return the length of department. Example 76, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 76}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.department) }
```

### Expected Output
```json
{"length": 2}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07756 — Read department with a default. Example 76, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 76}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department default "UNKNOWN" }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07757 — Check whether department is non-null. Example 76, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 76}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.department != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07758 — Add source metadata. Example 76, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 76}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"department": "IT", "index": 76, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT", "index": 76, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07759 — Return field metadata. Example 76, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 76}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "department", keyValue: payload.department }
```

### Expected Output
```json
{"keyName": "department", "keyValue": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "department", "keyValue": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07760 — Check whether department is a String. Example 76, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 76}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.department is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07761 — Select code. Example 77, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 77}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.code
```

### Expected Output
```json
"C100"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "C100".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07762 — Project only code. Example 77, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 77}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ code: payload.code }
```

### Expected Output
```json
{"code": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07763 — Wrap code under value. Example 77, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 77}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07764 — Uppercase code. Example 77, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 77}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.code) }
```

### Expected Output
```json
{"upper": "C100"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07765 — Return the length of code. Example 77, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 77}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.code) }
```

### Expected Output
```json
{"length": 4}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07766 — Read code with a default. Example 77, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 77}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code default "UNKNOWN" }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07767 — Check whether code is non-null. Example 77, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 77}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.code != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07768 — Add source metadata. Example 77, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 77}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"code": "C100", "index": 77, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100", "index": 77, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07769 — Return field metadata. Example 77, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 77}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "code", keyValue: payload.code }
```

### Expected Output
```json
{"keyName": "code", "keyValue": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "code", "keyValue": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07770 — Check whether code is a String. Example 77, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 77}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.code is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07771 — Select type. Example 78, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 78}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.type
```

### Expected Output
```json
"CUSTOMER"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "CUSTOMER".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07772 — Project only type. Example 78, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 78}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ type: payload.type }
```

### Expected Output
```json
{"type": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07773 — Wrap type under value. Example 78, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 78}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07774 — Uppercase type. Example 78, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 78}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.type) }
```

### Expected Output
```json
{"upper": "CUSTOMER"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07775 — Return the length of type. Example 78, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 78}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.type) }
```

### Expected Output
```json
{"length": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07776 — Read type with a default. Example 78, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 78}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type default "UNKNOWN" }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07777 — Check whether type is non-null. Example 78, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 78}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.type != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07778 — Add source metadata. Example 78, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 78}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"type": "CUSTOMER", "index": 78, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER", "index": 78, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07779 — Return field metadata. Example 78, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 78}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "type", keyValue: payload.type }
```

### Expected Output
```json
{"keyName": "type", "keyValue": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "type", "keyValue": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07780 — Check whether type is a String. Example 78, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 78}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.type is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07781 — Select email. Example 79, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 79}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.email
```

### Expected Output
```json
"a@example.com"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "a@example.com".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07782 — Project only email. Example 79, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 79}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ email: payload.email }
```

### Expected Output
```json
{"email": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07783 — Wrap email under value. Example 79, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 79}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07784 — Uppercase email. Example 79, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 79}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.email) }
```

### Expected Output
```json
{"upper": "A@EXAMPLE.COM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "A@EXAMPLE.COM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07785 — Return the length of email. Example 79, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 79}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.email) }
```

### Expected Output
```json
{"length": 13}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07786 — Read email with a default. Example 79, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 79}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email default "UNKNOWN" }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07787 — Check whether email is non-null. Example 79, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 79}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.email != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07788 — Add source metadata. Example 79, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 79}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"email": "a@example.com", "index": 79, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com", "index": 79, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07789 — Return field metadata. Example 79, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 79}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "email", keyValue: payload.email }
```

### Expected Output
```json
{"keyName": "email", "keyValue": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "email", "keyValue": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07790 — Check whether email is a String. Example 79, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 79}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.email is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07791 — Select description. Example 80, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 80}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.description
```

### Expected Output
```json
"Sample"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Sample".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07792 — Project only description. Example 80, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 80}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ description: payload.description }
```

### Expected Output
```json
{"description": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07793 — Wrap description under value. Example 80, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 80}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07794 — Uppercase description. Example 80, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 80}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.description) }
```

### Expected Output
```json
{"upper": "SAMPLE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "SAMPLE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07795 — Return the length of description. Example 80, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 80}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.description) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07796 — Read description with a default. Example 80, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 80}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07797 — Check whether description is non-null. Example 80, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 80}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.description != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07798 — Add source metadata. Example 80, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 80}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"description": "Sample", "index": 80, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample", "index": 80, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07799 — Return field metadata. Example 80, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 80}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "description", keyValue: payload.description }
```

### Expected Output
```json
{"keyName": "description", "keyValue": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "description", "keyValue": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07800 — Check whether description is a String. Example 80, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 80}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.description is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07801 — Select name. Example 81, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 81}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.name
```

### Expected Output
```json
"Narsing"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Narsing".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07802 — Project only name. Example 81, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 81}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ name: payload.name }
```

### Expected Output
```json
{"name": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07803 — Wrap name under value. Example 81, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 81}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07804 — Uppercase name. Example 81, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 81}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.name) }
```

### Expected Output
```json
{"upper": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07805 — Return the length of name. Example 81, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 81}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.name) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07806 — Read name with a default. Example 81, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 81}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07807 — Check whether name is non-null. Example 81, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 81}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.name != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07808 — Add source metadata. Example 81, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 81}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"name": "Narsing", "index": 81, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing", "index": 81, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07809 — Return field metadata. Example 81, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 81}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "name", keyValue: payload.name }
```

### Expected Output
```json
{"keyName": "name", "keyValue": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "name", "keyValue": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07810 — Check whether name is a String. Example 81, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 81}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.name is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07811 — Select city. Example 82, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 82}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.city
```

### Expected Output
```json
"Hyderabad"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Hyderabad".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07812 — Project only city. Example 82, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 82}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ city: payload.city }
```

### Expected Output
```json
{"city": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07813 — Wrap city under value. Example 82, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 82}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07814 — Uppercase city. Example 82, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 82}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.city) }
```

### Expected Output
```json
{"upper": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07815 — Return the length of city. Example 82, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 82}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.city) }
```

### Expected Output
```json
{"length": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07816 — Read city with a default. Example 82, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 82}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07817 — Check whether city is non-null. Example 82, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 82}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.city != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07818 — Add source metadata. Example 82, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 82}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"city": "Hyderabad", "index": 82, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad", "index": 82, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07819 — Return field metadata. Example 82, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 82}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "city", keyValue: payload.city }
```

### Expected Output
```json
{"keyName": "city", "keyValue": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "city", "keyValue": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07820 — Check whether city is a String. Example 82, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 82}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.city is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07821 — Select country. Example 83, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 83}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.country
```

### Expected Output
```json
"India"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "India".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07822 — Project only country. Example 83, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 83}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ country: payload.country }
```

### Expected Output
```json
{"country": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07823 — Wrap country under value. Example 83, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 83}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07824 — Uppercase country. Example 83, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 83}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.country) }
```

### Expected Output
```json
{"upper": "INDIA"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "INDIA"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07825 — Return the length of country. Example 83, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 83}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.country) }
```

### Expected Output
```json
{"length": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07826 — Read country with a default. Example 83, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 83}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country default "UNKNOWN" }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07827 — Check whether country is non-null. Example 83, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 83}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.country != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07828 — Add source metadata. Example 83, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 83}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"country": "India", "index": 83, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India", "index": 83, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07829 — Return field metadata. Example 83, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 83}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "country", keyValue: payload.country }
```

### Expected Output
```json
{"keyName": "country", "keyValue": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "country", "keyValue": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07830 — Check whether country is a String. Example 83, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 83}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.country is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07831 — Select status. Example 84, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 84}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.status
```

### Expected Output
```json
"ACTIVE"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "ACTIVE".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07832 — Project only status. Example 84, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 84}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ status: payload.status }
```

### Expected Output
```json
{"status": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07833 — Wrap status under value. Example 84, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 84}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07834 — Uppercase status. Example 84, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 84}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.status) }
```

### Expected Output
```json
{"upper": "ACTIVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07835 — Return the length of status. Example 84, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 84}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.status) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07836 — Read status with a default. Example 84, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 84}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status default "UNKNOWN" }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07837 — Check whether status is non-null. Example 84, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 84}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.status != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07838 — Add source metadata. Example 84, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 84}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"status": "ACTIVE", "index": 84, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE", "index": 84, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07839 — Return field metadata. Example 84, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 84}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "status", keyValue: payload.status }
```

### Expected Output
```json
{"keyName": "status", "keyValue": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "status", "keyValue": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07840 — Check whether status is a String. Example 84, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 84}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.status is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07841 — Select category. Example 85, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 85}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.category
```

### Expected Output
```json
"PREMIUM"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "PREMIUM".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07842 — Project only category. Example 85, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 85}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ category: payload.category }
```

### Expected Output
```json
{"category": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07843 — Wrap category under value. Example 85, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 85}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07844 — Uppercase category. Example 85, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 85}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.category) }
```

### Expected Output
```json
{"upper": "PREMIUM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07845 — Return the length of category. Example 85, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 85}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.category) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07846 — Read category with a default. Example 85, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 85}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category default "UNKNOWN" }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07847 — Check whether category is non-null. Example 85, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 85}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.category != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07848 — Add source metadata. Example 85, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 85}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"category": "PREMIUM", "index": 85, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM", "index": 85, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07849 — Return field metadata. Example 85, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 85}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "category", keyValue: payload.category }
```

### Expected Output
```json
{"keyName": "category", "keyValue": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "category", "keyValue": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07850 — Check whether category is a String. Example 85, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 85}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.category is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07851 — Select department. Example 86, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 86}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.department
```

### Expected Output
```json
"IT"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "IT".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07852 — Project only department. Example 86, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 86}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ department: payload.department }
```

### Expected Output
```json
{"department": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07853 — Wrap department under value. Example 86, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 86}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07854 — Uppercase department. Example 86, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 86}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.department) }
```

### Expected Output
```json
{"upper": "IT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07855 — Return the length of department. Example 86, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 86}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.department) }
```

### Expected Output
```json
{"length": 2}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07856 — Read department with a default. Example 86, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 86}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department default "UNKNOWN" }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07857 — Check whether department is non-null. Example 86, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 86}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.department != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07858 — Add source metadata. Example 86, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 86}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"department": "IT", "index": 86, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT", "index": 86, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07859 — Return field metadata. Example 86, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 86}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "department", keyValue: payload.department }
```

### Expected Output
```json
{"keyName": "department", "keyValue": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "department", "keyValue": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07860 — Check whether department is a String. Example 86, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 86}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.department is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07861 — Select code. Example 87, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 87}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.code
```

### Expected Output
```json
"C100"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "C100".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07862 — Project only code. Example 87, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 87}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ code: payload.code }
```

### Expected Output
```json
{"code": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07863 — Wrap code under value. Example 87, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 87}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07864 — Uppercase code. Example 87, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 87}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.code) }
```

### Expected Output
```json
{"upper": "C100"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07865 — Return the length of code. Example 87, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 87}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.code) }
```

### Expected Output
```json
{"length": 4}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07866 — Read code with a default. Example 87, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 87}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code default "UNKNOWN" }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07867 — Check whether code is non-null. Example 87, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 87}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.code != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07868 — Add source metadata. Example 87, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 87}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"code": "C100", "index": 87, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100", "index": 87, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07869 — Return field metadata. Example 87, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 87}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "code", keyValue: payload.code }
```

### Expected Output
```json
{"keyName": "code", "keyValue": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "code", "keyValue": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07870 — Check whether code is a String. Example 87, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 87}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.code is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07871 — Select type. Example 88, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 88}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.type
```

### Expected Output
```json
"CUSTOMER"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "CUSTOMER".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07872 — Project only type. Example 88, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 88}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ type: payload.type }
```

### Expected Output
```json
{"type": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07873 — Wrap type under value. Example 88, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 88}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07874 — Uppercase type. Example 88, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 88}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.type) }
```

### Expected Output
```json
{"upper": "CUSTOMER"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07875 — Return the length of type. Example 88, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 88}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.type) }
```

### Expected Output
```json
{"length": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07876 — Read type with a default. Example 88, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 88}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type default "UNKNOWN" }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07877 — Check whether type is non-null. Example 88, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 88}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.type != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07878 — Add source metadata. Example 88, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 88}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"type": "CUSTOMER", "index": 88, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER", "index": 88, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07879 — Return field metadata. Example 88, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 88}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "type", keyValue: payload.type }
```

### Expected Output
```json
{"keyName": "type", "keyValue": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "type", "keyValue": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07880 — Check whether type is a String. Example 88, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 88}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.type is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07881 — Select email. Example 89, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 89}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.email
```

### Expected Output
```json
"a@example.com"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "a@example.com".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07882 — Project only email. Example 89, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 89}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ email: payload.email }
```

### Expected Output
```json
{"email": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07883 — Wrap email under value. Example 89, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 89}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07884 — Uppercase email. Example 89, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 89}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.email) }
```

### Expected Output
```json
{"upper": "A@EXAMPLE.COM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "A@EXAMPLE.COM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07885 — Return the length of email. Example 89, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 89}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.email) }
```

### Expected Output
```json
{"length": 13}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07886 — Read email with a default. Example 89, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 89}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email default "UNKNOWN" }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07887 — Check whether email is non-null. Example 89, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 89}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.email != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07888 — Add source metadata. Example 89, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 89}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"email": "a@example.com", "index": 89, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com", "index": 89, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07889 — Return field metadata. Example 89, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 89}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "email", keyValue: payload.email }
```

### Expected Output
```json
{"keyName": "email", "keyValue": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "email", "keyValue": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07890 — Check whether email is a String. Example 89, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 89}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.email is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07891 — Select description. Example 90, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 90}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.description
```

### Expected Output
```json
"Sample"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Sample".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07892 — Project only description. Example 90, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 90}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ description: payload.description }
```

### Expected Output
```json
{"description": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07893 — Wrap description under value. Example 90, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 90}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07894 — Uppercase description. Example 90, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 90}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.description) }
```

### Expected Output
```json
{"upper": "SAMPLE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "SAMPLE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07895 — Return the length of description. Example 90, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 90}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.description) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07896 — Read description with a default. Example 90, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 90}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07897 — Check whether description is non-null. Example 90, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 90}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.description != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07898 — Add source metadata. Example 90, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 90}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"description": "Sample", "index": 90, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample", "index": 90, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07899 — Return field metadata. Example 90, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 90}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "description", keyValue: payload.description }
```

### Expected Output
```json
{"keyName": "description", "keyValue": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "description", "keyValue": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07900 — Check whether description is a String. Example 90, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 90}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.description is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07901 — Select name. Example 91, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 91}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.name
```

### Expected Output
```json
"Narsing"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Narsing".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07902 — Project only name. Example 91, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 91}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ name: payload.name }
```

### Expected Output
```json
{"name": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07903 — Wrap name under value. Example 91, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 91}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07904 — Uppercase name. Example 91, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 91}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.name) }
```

### Expected Output
```json
{"upper": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07905 — Return the length of name. Example 91, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 91}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.name) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07906 — Read name with a default. Example 91, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 91}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07907 — Check whether name is non-null. Example 91, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 91}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.name != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07908 — Add source metadata. Example 91, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 91}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"name": "Narsing", "index": 91, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing", "index": 91, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07909 — Return field metadata. Example 91, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 91}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "name", keyValue: payload.name }
```

### Expected Output
```json
{"keyName": "name", "keyValue": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "name", "keyValue": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07910 — Check whether name is a String. Example 91, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"name": "Narsing", "index": 91}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.name is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07911 — Select city. Example 92, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 92}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.city
```

### Expected Output
```json
"Hyderabad"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Hyderabad".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07912 — Project only city. Example 92, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 92}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ city: payload.city }
```

### Expected Output
```json
{"city": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07913 — Wrap city under value. Example 92, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 92}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07914 — Uppercase city. Example 92, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 92}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.city) }
```

### Expected Output
```json
{"upper": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07915 — Return the length of city. Example 92, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 92}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.city) }
```

### Expected Output
```json
{"length": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07916 — Read city with a default. Example 92, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 92}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07917 — Check whether city is non-null. Example 92, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 92}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.city != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07918 — Add source metadata. Example 92, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 92}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"city": "Hyderabad", "index": 92, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad", "index": 92, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07919 — Return field metadata. Example 92, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 92}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "city", keyValue: payload.city }
```

### Expected Output
```json
{"keyName": "city", "keyValue": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "city", "keyValue": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07920 — Check whether city is a String. Example 92, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"city": "Hyderabad", "index": 92}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.city is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07921 — Select country. Example 93, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 93}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.country
```

### Expected Output
```json
"India"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "India".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07922 — Project only country. Example 93, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 93}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ country: payload.country }
```

### Expected Output
```json
{"country": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07923 — Wrap country under value. Example 93, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 93}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07924 — Uppercase country. Example 93, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 93}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.country) }
```

### Expected Output
```json
{"upper": "INDIA"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "INDIA"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07925 — Return the length of country. Example 93, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 93}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.country) }
```

### Expected Output
```json
{"length": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07926 — Read country with a default. Example 93, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 93}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country default "UNKNOWN" }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07927 — Check whether country is non-null. Example 93, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 93}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.country != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07928 — Add source metadata. Example 93, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 93}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"country": "India", "index": 93, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India", "index": 93, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07929 — Return field metadata. Example 93, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 93}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "country", keyValue: payload.country }
```

### Expected Output
```json
{"keyName": "country", "keyValue": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "country", "keyValue": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07930 — Check whether country is a String. Example 93, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"country": "India", "index": 93}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.country is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07931 — Select status. Example 94, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 94}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.status
```

### Expected Output
```json
"ACTIVE"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "ACTIVE".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07932 — Project only status. Example 94, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 94}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ status: payload.status }
```

### Expected Output
```json
{"status": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07933 — Wrap status under value. Example 94, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 94}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07934 — Uppercase status. Example 94, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 94}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.status) }
```

### Expected Output
```json
{"upper": "ACTIVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07935 — Return the length of status. Example 94, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 94}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.status) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07936 — Read status with a default. Example 94, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 94}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status default "UNKNOWN" }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07937 — Check whether status is non-null. Example 94, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 94}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.status != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07938 — Add source metadata. Example 94, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 94}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"status": "ACTIVE", "index": 94, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE", "index": 94, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07939 — Return field metadata. Example 94, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 94}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "status", keyValue: payload.status }
```

### Expected Output
```json
{"keyName": "status", "keyValue": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "status", "keyValue": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07940 — Check whether status is a String. Example 94, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"status": "ACTIVE", "index": 94}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.status is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07941 — Select category. Example 95, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 95}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.category
```

### Expected Output
```json
"PREMIUM"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "PREMIUM".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07942 — Project only category. Example 95, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 95}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ category: payload.category }
```

### Expected Output
```json
{"category": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07943 — Wrap category under value. Example 95, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 95}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07944 — Uppercase category. Example 95, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 95}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.category) }
```

### Expected Output
```json
{"upper": "PREMIUM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07945 — Return the length of category. Example 95, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 95}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.category) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07946 — Read category with a default. Example 95, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 95}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category default "UNKNOWN" }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07947 — Check whether category is non-null. Example 95, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 95}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.category != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07948 — Add source metadata. Example 95, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 95}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"category": "PREMIUM", "index": 95, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM", "index": 95, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07949 — Return field metadata. Example 95, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 95}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "category", keyValue: payload.category }
```

### Expected Output
```json
{"keyName": "category", "keyValue": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "category", "keyValue": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07950 — Check whether category is a String. Example 95, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"category": "PREMIUM", "index": 95}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.category is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07951 — Select department. Example 96, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 96}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.department
```

### Expected Output
```json
"IT"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "IT".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07952 — Project only department. Example 96, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 96}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ department: payload.department }
```

### Expected Output
```json
{"department": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07953 — Wrap department under value. Example 96, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 96}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07954 — Uppercase department. Example 96, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 96}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.department) }
```

### Expected Output
```json
{"upper": "IT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07955 — Return the length of department. Example 96, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 96}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.department) }
```

### Expected Output
```json
{"length": 2}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07956 — Read department with a default. Example 96, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 96}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department default "UNKNOWN" }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07957 — Check whether department is non-null. Example 96, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 96}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.department != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07958 — Add source metadata. Example 96, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 96}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"department": "IT", "index": 96, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT", "index": 96, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07959 — Return field metadata. Example 96, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 96}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "department", keyValue: payload.department }
```

### Expected Output
```json
{"keyName": "department", "keyValue": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "department", "keyValue": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07960 — Check whether department is a String. Example 96, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"department": "IT", "index": 96}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.department is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07961 — Select code. Example 97, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 97}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.code
```

### Expected Output
```json
"C100"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "C100".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07962 — Project only code. Example 97, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 97}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ code: payload.code }
```

### Expected Output
```json
{"code": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07963 — Wrap code under value. Example 97, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 97}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07964 — Uppercase code. Example 97, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 97}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.code) }
```

### Expected Output
```json
{"upper": "C100"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07965 — Return the length of code. Example 97, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 97}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.code) }
```

### Expected Output
```json
{"length": 4}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07966 — Read code with a default. Example 97, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 97}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code default "UNKNOWN" }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07967 — Check whether code is non-null. Example 97, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 97}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.code != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07968 — Add source metadata. Example 97, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 97}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"code": "C100", "index": 97, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100", "index": 97, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07969 — Return field metadata. Example 97, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 97}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "code", keyValue: payload.code }
```

### Expected Output
```json
{"keyName": "code", "keyValue": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "code", "keyValue": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07970 — Check whether code is a String. Example 97, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"code": "C100", "index": 97}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.code is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07971 — Select type. Example 98, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 98}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.type
```

### Expected Output
```json
"CUSTOMER"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "CUSTOMER".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07972 — Project only type. Example 98, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 98}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ type: payload.type }
```

### Expected Output
```json
{"type": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07973 — Wrap type under value. Example 98, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 98}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07974 — Uppercase type. Example 98, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 98}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.type) }
```

### Expected Output
```json
{"upper": "CUSTOMER"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07975 — Return the length of type. Example 98, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 98}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.type) }
```

### Expected Output
```json
{"length": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07976 — Read type with a default. Example 98, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 98}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type default "UNKNOWN" }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07977 — Check whether type is non-null. Example 98, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 98}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.type != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07978 — Add source metadata. Example 98, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 98}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"type": "CUSTOMER", "index": 98, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER", "index": 98, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07979 — Return field metadata. Example 98, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 98}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "type", keyValue: payload.type }
```

### Expected Output
```json
{"keyName": "type", "keyValue": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "type", "keyValue": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07980 — Check whether type is a String. Example 98, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"type": "CUSTOMER", "index": 98}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.type is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07981 — Select email. Example 99, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 99}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.email
```

### Expected Output
```json
"a@example.com"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "a@example.com".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07982 — Project only email. Example 99, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 99}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ email: payload.email }
```

### Expected Output
```json
{"email": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07983 — Wrap email under value. Example 99, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 99}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07984 — Uppercase email. Example 99, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 99}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.email) }
```

### Expected Output
```json
{"upper": "A@EXAMPLE.COM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "A@EXAMPLE.COM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07985 — Return the length of email. Example 99, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 99}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.email) }
```

### Expected Output
```json
{"length": 13}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07986 — Read email with a default. Example 99, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 99}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email default "UNKNOWN" }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07987 — Check whether email is non-null. Example 99, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 99}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.email != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07988 — Add source metadata. Example 99, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 99}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"email": "a@example.com", "index": 99, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com", "index": 99, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07989 — Return field metadata. Example 99, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 99}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "email", keyValue: payload.email }
```

### Expected Output
```json
{"keyName": "email", "keyValue": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "email", "keyValue": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07990 — Check whether email is a String. Example 99, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"email": "a@example.com", "index": 99}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.email is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07991 — Select description. Example 100, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 100}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.description
```

### Expected Output
```json
"Sample"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Sample".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07992 — Project only description. Example 100, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 100}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ description: payload.description }
```

### Expected Output
```json
{"description": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07993 — Wrap description under value. Example 100, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 100}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07994 — Uppercase description. Example 100, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 100}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.description) }
```

### Expected Output
```json
{"upper": "SAMPLE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "SAMPLE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07995 — Return the length of description. Example 100, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 100}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.description) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07996 — Read description with a default. Example 100, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 100}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07997 — Check whether description is non-null. Example 100, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 100}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.description != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07998 — Add source metadata. Example 100, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 100}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"description": "Sample", "index": 100, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample", "index": 100, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-07999 — Return field metadata. Example 100, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 100}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "description", keyValue: payload.description }
```

### Expected Output
```json
{"keyName": "description", "keyValue": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "description", "keyValue": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-08000 — Check whether description is a String. Example 100, family 5.

**Difficulty:** Medium
**Topic:** Nested Data

### Input
```json
{"description": "Sample", "index": 100}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.description is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
