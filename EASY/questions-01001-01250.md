# Easy DataWeave Questions 01001-01250

Questions 1001–1250 of 4000 in the Easy level.

## DW-01001 — Add two numbers. Example 1.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 1, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01002 — Subtract b from a. Example 1.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 1, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": -1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": -1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01003 — Multiply two numbers. Example 1.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 1, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01004 — Divide a by b. Example 1.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 1, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 0.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01005 — Calculate the remainder. Example 1.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 1, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01006 — Compare the numbers. Example 1.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 1, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01007 — Classify a as HIGH or LOW. Example 1.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 1, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01008 — Apply an 18 percent multiplier. Example 1.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 1, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 1.18}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1.18}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01009 — Round the division result. Example 1.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 1, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01010 — Check whether a is even. Example 1.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 1, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01011 — Add two numbers. Example 2.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 2, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01012 — Subtract b from a. Example 2.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 2, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": -1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": -1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01013 — Multiply two numbers. Example 2.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 2, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 6}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01014 — Divide a by b. Example 2.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 2, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 0.6666666666666666}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0.6666666666666666}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01015 — Calculate the remainder. Example 2.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 2, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01016 — Compare the numbers. Example 2.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 2, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01017 — Classify a as HIGH or LOW. Example 2.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 2, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01018 — Apply an 18 percent multiplier. Example 2.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 2, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 2.36}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2.36}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01019 — Round the division result. Example 2.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 2, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01020 — Check whether a is even. Example 2.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 2, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01021 — Add two numbers. Example 3.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 3, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 7}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01022 — Subtract b from a. Example 3.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 3, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": -1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": -1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01023 — Multiply two numbers. Example 3.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 3, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 12}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01024 — Divide a by b. Example 3.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 3, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 0.75}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0.75}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01025 — Calculate the remainder. Example 3.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 3, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01026 — Compare the numbers. Example 3.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 3, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01027 — Classify a as HIGH or LOW. Example 3.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 3, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01028 — Apply an 18 percent multiplier. Example 3.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 3, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 3.54}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3.54}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01029 — Round the division result. Example 3.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 3, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01030 — Check whether a is even. Example 3.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 3, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01031 — Add two numbers. Example 4.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 4, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 9}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01032 — Subtract b from a. Example 4.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 4, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": -1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": -1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01033 — Multiply two numbers. Example 4.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 4, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 20}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 20}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01034 — Divide a by b. Example 4.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 4, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 0.8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0.8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01035 — Calculate the remainder. Example 4.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 4, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01036 — Compare the numbers. Example 4.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 4, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01037 — Classify a as HIGH or LOW. Example 4.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 4, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01038 — Apply an 18 percent multiplier. Example 4.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 4, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 4.72}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4.72}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01039 — Round the division result. Example 4.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 4, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01040 — Check whether a is even. Example 4.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 4, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01041 — Add two numbers. Example 5.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 5, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 11}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01042 — Subtract b from a. Example 5.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 5, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": -1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": -1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01043 — Multiply two numbers. Example 5.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 5, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 30}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 30}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01044 — Divide a by b. Example 5.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 5, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 0.8333333333333334}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0.8333333333333334}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01045 — Calculate the remainder. Example 5.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 5, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01046 — Compare the numbers. Example 5.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 5, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01047 — Classify a as HIGH or LOW. Example 5.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 5, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01048 — Apply an 18 percent multiplier. Example 5.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 5, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 5.8999999999999995}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5.8999999999999995}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01049 — Round the division result. Example 5.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 5, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01050 — Check whether a is even. Example 5.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 5, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01051 — Add two numbers. Example 6.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 6, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 13}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01052 — Subtract b from a. Example 6.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 6, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": -1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": -1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01053 — Multiply two numbers. Example 6.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 6, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 42}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 42}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01054 — Divide a by b. Example 6.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 6, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 0.8571428571428571}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0.8571428571428571}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01055 — Calculate the remainder. Example 6.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 6, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 6}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01056 — Compare the numbers. Example 6.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 6, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01057 — Classify a as HIGH or LOW. Example 6.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 6, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01058 — Apply an 18 percent multiplier. Example 6.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 6, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 7.08}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7.08}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01059 — Round the division result. Example 6.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 6, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01060 — Check whether a is even. Example 6.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 6, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01061 — Add two numbers. Example 7.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 7, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 15}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 15}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01062 — Subtract b from a. Example 7.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 7, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": -1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": -1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01063 — Multiply two numbers. Example 7.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 7, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 56}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 56}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01064 — Divide a by b. Example 7.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 7, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 0.875}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0.875}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01065 — Calculate the remainder. Example 7.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 7, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 7}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01066 — Compare the numbers. Example 7.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 7, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01067 — Classify a as HIGH or LOW. Example 7.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 7, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01068 — Apply an 18 percent multiplier. Example 7.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 7, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 8.26}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8.26}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01069 — Round the division result. Example 7.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 7, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01070 — Check whether a is even. Example 7.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 7, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01071 — Add two numbers. Example 8.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 8, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 17}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01072 — Subtract b from a. Example 8.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 8, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": -1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": -1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01073 — Multiply two numbers. Example 8.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 8, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 72}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 72}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01074 — Divide a by b. Example 8.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 8, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 0.8888888888888888}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0.8888888888888888}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01075 — Calculate the remainder. Example 8.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 8, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01076 — Compare the numbers. Example 8.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 8, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01077 — Classify a as HIGH or LOW. Example 8.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 8, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01078 — Apply an 18 percent multiplier. Example 8.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 8, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 9.44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9.44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01079 — Round the division result. Example 8.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 8, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01080 — Check whether a is even. Example 8.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 8, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01081 — Add two numbers. Example 9.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 9, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 19}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 19}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01082 — Subtract b from a. Example 9.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 9, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": -1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": -1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01083 — Multiply two numbers. Example 9.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 9, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 90}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 90}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01084 — Divide a by b. Example 9.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 9, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 0.9}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0.9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01085 — Calculate the remainder. Example 9.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 9, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 9}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01086 — Compare the numbers. Example 9.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 9, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01087 — Classify a as HIGH or LOW. Example 9.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 9, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01088 — Apply an 18 percent multiplier. Example 9.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 9, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 10.62}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 10.62}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01089 — Round the division result. Example 9.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 9, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01090 — Check whether a is even. Example 9.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 9, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01091 — Add two numbers. Example 10.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 10, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 12}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01092 — Subtract b from a. Example 10.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 10, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01093 — Multiply two numbers. Example 10.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 10, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 20}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 20}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01094 — Divide a by b. Example 10.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 10, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 5.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01095 — Calculate the remainder. Example 10.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 10, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01096 — Compare the numbers. Example 10.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 10, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01097 — Classify a as HIGH or LOW. Example 10.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 10, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01098 — Apply an 18 percent multiplier. Example 10.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 10, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 11.799999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 11.799999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01099 — Round the division result. Example 10.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 10, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01100 — Check whether a is even. Example 10.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 10, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01101 — Add two numbers. Example 11.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 11, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 14}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 14}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01102 — Subtract b from a. Example 11.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 11, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01103 — Multiply two numbers. Example 11.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 11, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 33}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 33}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01104 — Divide a by b. Example 11.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 11, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 3.6666666666666665}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3.6666666666666665}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01105 — Calculate the remainder. Example 11.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 11, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01106 — Compare the numbers. Example 11.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 11, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01107 — Classify a as HIGH or LOW. Example 11.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 11, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01108 — Apply an 18 percent multiplier. Example 11.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 11, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 12.979999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12.979999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01109 — Round the division result. Example 11.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 11, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01110 — Check whether a is even. Example 11.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 11, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01111 — Add two numbers. Example 12.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 12, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 16}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 16}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01112 — Subtract b from a. Example 12.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 12, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01113 — Multiply two numbers. Example 12.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 12, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 48}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 48}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01114 — Divide a by b. Example 12.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 12, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 3.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01115 — Calculate the remainder. Example 12.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 12, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01116 — Compare the numbers. Example 12.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 12, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01117 — Classify a as HIGH or LOW. Example 12.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 12, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01118 — Apply an 18 percent multiplier. Example 12.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 12, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 14.16}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 14.16}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01119 — Round the division result. Example 12.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 12, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01120 — Check whether a is even. Example 12.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 12, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01121 — Add two numbers. Example 13.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 13, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 18}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 18}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01122 — Subtract b from a. Example 13.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 13, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01123 — Multiply two numbers. Example 13.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 13, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 65}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 65}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01124 — Divide a by b. Example 13.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 13, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 2.6}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2.6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01125 — Calculate the remainder. Example 13.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 13, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01126 — Compare the numbers. Example 13.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 13, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01127 — Classify a as HIGH or LOW. Example 13.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 13, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01128 — Apply an 18 percent multiplier. Example 13.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 13, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 15.34}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 15.34}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01129 — Round the division result. Example 13.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 13, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01130 — Check whether a is even. Example 13.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 13, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01131 — Add two numbers. Example 14.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 14, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 20}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 20}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01132 — Subtract b from a. Example 14.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 14, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01133 — Multiply two numbers. Example 14.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 14, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 84}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 84}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01134 — Divide a by b. Example 14.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 14, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 2.3333333333333335}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2.3333333333333335}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01135 — Calculate the remainder. Example 14.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 14, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01136 — Compare the numbers. Example 14.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 14, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01137 — Classify a as HIGH or LOW. Example 14.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 14, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01138 — Apply an 18 percent multiplier. Example 14.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 14, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 16.52}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 16.52}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01139 — Round the division result. Example 14.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 14, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01140 — Check whether a is even. Example 14.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 14, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01141 — Add two numbers. Example 15.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 15, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 22}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 22}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01142 — Subtract b from a. Example 15.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 15, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01143 — Multiply two numbers. Example 15.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 15, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 105}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 105}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01144 — Divide a by b. Example 15.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 15, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 2.142857142857143}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2.142857142857143}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01145 — Calculate the remainder. Example 15.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 15, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01146 — Compare the numbers. Example 15.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 15, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01147 — Classify a as HIGH or LOW. Example 15.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 15, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01148 — Apply an 18 percent multiplier. Example 15.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 15, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 17.7}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17.7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01149 — Round the division result. Example 15.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 15, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01150 — Check whether a is even. Example 15.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 15, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01151 — Add two numbers. Example 16.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 16, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 24}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 24}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01152 — Subtract b from a. Example 16.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 16, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01153 — Multiply two numbers. Example 16.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 16, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 128}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 128}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01154 — Divide a by b. Example 16.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 16, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 2.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01155 — Calculate the remainder. Example 16.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 16, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01156 — Compare the numbers. Example 16.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 16, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01157 — Classify a as HIGH or LOW. Example 16.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 16, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01158 — Apply an 18 percent multiplier. Example 16.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 16, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 18.88}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 18.88}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01159 — Round the division result. Example 16.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 16, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01160 — Check whether a is even. Example 16.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 16, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01161 — Add two numbers. Example 17.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 17, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 26}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 26}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01162 — Subtract b from a. Example 17.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 17, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01163 — Multiply two numbers. Example 17.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 17, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 153}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 153}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01164 — Divide a by b. Example 17.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 17, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 1.8888888888888888}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1.8888888888888888}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01165 — Calculate the remainder. Example 17.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 17, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01166 — Compare the numbers. Example 17.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 17, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01167 — Classify a as HIGH or LOW. Example 17.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 17, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01168 — Apply an 18 percent multiplier. Example 17.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 17, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 20.06}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 20.06}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01169 — Round the division result. Example 17.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 17, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01170 — Check whether a is even. Example 17.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 17, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01171 — Add two numbers. Example 18.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 18, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 28}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 28}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01172 — Subtract b from a. Example 18.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 18, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01173 — Multiply two numbers. Example 18.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 18, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 180}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 180}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01174 — Divide a by b. Example 18.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 18, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 1.8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1.8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01175 — Calculate the remainder. Example 18.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 18, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01176 — Compare the numbers. Example 18.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 18, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01177 — Classify a as HIGH or LOW. Example 18.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 18, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01178 — Apply an 18 percent multiplier. Example 18.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 18, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 21.24}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 21.24}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01179 — Round the division result. Example 18.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 18, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01180 — Check whether a is even. Example 18.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 18, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01181 — Add two numbers. Example 19.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 19, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 21}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 21}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01182 — Subtract b from a. Example 19.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 19, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 17}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01183 — Multiply two numbers. Example 19.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 19, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 38}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 38}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01184 — Divide a by b. Example 19.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 19, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 9.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01185 — Calculate the remainder. Example 19.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 19, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01186 — Compare the numbers. Example 19.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 19, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01187 — Classify a as HIGH or LOW. Example 19.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 19, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01188 — Apply an 18 percent multiplier. Example 19.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 19, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 22.419999999999998}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 22.419999999999998}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01189 — Round the division result. Example 19.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 19, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 10}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 10}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01190 — Check whether a is even. Example 19.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 19, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01191 — Add two numbers. Example 20.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 20, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 23}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 23}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01192 — Subtract b from a. Example 20.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 20, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 17}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01193 — Multiply two numbers. Example 20.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 20, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 60}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 60}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01194 — Divide a by b. Example 20.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 20, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 6.666666666666667}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6.666666666666667}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01195 — Calculate the remainder. Example 20.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 20, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01196 — Compare the numbers. Example 20.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 20, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01197 — Classify a as HIGH or LOW. Example 20.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 20, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01198 — Apply an 18 percent multiplier. Example 20.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 20, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 23.599999999999998}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 23.599999999999998}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01199 — Round the division result. Example 20.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 20, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 7}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01200 — Check whether a is even. Example 20.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 20, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01201 — Add two numbers. Example 21.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 21, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 25}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 25}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01202 — Subtract b from a. Example 21.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 21, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 17}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01203 — Multiply two numbers. Example 21.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 21, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 84}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 84}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01204 — Divide a by b. Example 21.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 21, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 5.25}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5.25}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01205 — Calculate the remainder. Example 21.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 21, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01206 — Compare the numbers. Example 21.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 21, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01207 — Classify a as HIGH or LOW. Example 21.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 21, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01208 — Apply an 18 percent multiplier. Example 21.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 21, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 24.779999999999998}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 24.779999999999998}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01209 — Round the division result. Example 21.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 21, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01210 — Check whether a is even. Example 21.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 21, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01211 — Add two numbers. Example 22.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 22, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 27}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 27}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01212 — Subtract b from a. Example 22.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 22, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 17}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01213 — Multiply two numbers. Example 22.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 22, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 110}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 110}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01214 — Divide a by b. Example 22.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 22, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 4.4}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4.4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01215 — Calculate the remainder. Example 22.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 22, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01216 — Compare the numbers. Example 22.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 22, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01217 — Classify a as HIGH or LOW. Example 22.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 22, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01218 — Apply an 18 percent multiplier. Example 22.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 22, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 25.959999999999997}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 25.959999999999997}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01219 — Round the division result. Example 22.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 22, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01220 — Check whether a is even. Example 22.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 22, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01221 — Add two numbers. Example 23.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 23, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 29}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 29}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01222 — Subtract b from a. Example 23.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 23, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 17}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01223 — Multiply two numbers. Example 23.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 23, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 138}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 138}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01224 — Divide a by b. Example 23.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 23, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 3.8333333333333335}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3.8333333333333335}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01225 — Calculate the remainder. Example 23.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 23, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01226 — Compare the numbers. Example 23.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 23, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01227 — Classify a as HIGH or LOW. Example 23.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 23, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01228 — Apply an 18 percent multiplier. Example 23.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 23, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 27.139999999999997}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 27.139999999999997}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01229 — Round the division result. Example 23.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 23, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01230 — Check whether a is even. Example 23.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 23, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01231 — Add two numbers. Example 24.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 24, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 31}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 31}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01232 — Subtract b from a. Example 24.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 24, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 17}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01233 — Multiply two numbers. Example 24.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 24, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 168}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 168}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01234 — Divide a by b. Example 24.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 24, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 3.4285714285714284}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3.4285714285714284}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01235 — Calculate the remainder. Example 24.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 24, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01236 — Compare the numbers. Example 24.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 24, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01237 — Classify a as HIGH or LOW. Example 24.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 24, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01238 — Apply an 18 percent multiplier. Example 24.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 24, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 28.32}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 28.32}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01239 — Round the division result. Example 24.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 24, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01240 — Check whether a is even. Example 24.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 24, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01241 — Add two numbers. Example 25.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 25, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 33}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 33}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01242 — Subtract b from a. Example 25.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 25, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 17}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01243 — Multiply two numbers. Example 25.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 25, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 200}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 200}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01244 — Divide a by b. Example 25.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 25, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 3.125}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3.125}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01245 — Calculate the remainder. Example 25.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 25, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01246 — Compare the numbers. Example 25.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 25, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01247 — Classify a as HIGH or LOW. Example 25.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 25, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01248 — Apply an 18 percent multiplier. Example 25.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 25, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 29.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 29.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01249 — Round the division result. Example 25.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 25, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01250 — Check whether a is even. Example 25.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 25, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
