# Easy DataWeave Questions 00501-00750

Questions 501–750 of 4000 in the Easy level.

## DW-00501 — Convert the string to uppercase. Example 51.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00502 — Convert the string to lowercase. Example 51.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00503 — Remove leading and trailing spaces. Example 51.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00504 — Return the string length. Example 51.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00505 — Append -DW. Example 51.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "narsing-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00506 — Prefix with DW- Example 51.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-narsing"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00507 — Check whether the string contains a. Example 51.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00508 — Check whether the string starts with D. Example 51.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00509 — Check whether the string ends with e. Example 51.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00510 — Replace a with @. Example 51.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "n@rsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "n@rsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00511 — Convert the string to uppercase. Example 52.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "DATAWEAVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DATAWEAVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00512 — Convert the string to lowercase. Example 52.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "dataweave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "dataweave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00513 — Remove leading and trailing spaces. Example 52.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "DataWeave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00514 — Return the string length. Example 52.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00515 — Append -DW. Example 52.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "DataWeave-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00516 — Prefix with DW- Example 52.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00517 — Check whether the string contains a. Example 52.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00518 — Check whether the string starts with D. Example 52.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00519 — Check whether the string ends with e. Example 52.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00520 — Replace a with @. Example 52.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "D@t@We@ve"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "D@t@We@ve"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00521 — Convert the string to uppercase. Example 53.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MULESOFT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MULESOFT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00522 — Convert the string to lowercase. Example 53.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mulesoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mulesoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00523 — Remove leading and trailing spaces. Example 53.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00524 — Return the string length. Example 53.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00525 — Append -DW. Example 53.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MuleSoft-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00526 — Prefix with DW- Example 53.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MuleSoft"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00527 — Check whether the string contains a. Example 53.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00528 — Check whether the string starts with D. Example 53.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00529 — Check whether the string ends with e. Example 53.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00530 — Replace a with @. Example 53.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00531 — Convert the string to uppercase. Example 54.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HELLO WORLD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HELLO WORLD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00532 — Convert the string to lowercase. Example 54.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00533 — Remove leading and trailing spaces. Example 54.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00534 — Return the string length. Example 54.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00535 — Append -DW. Example 54.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "hello world-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00536 — Prefix with DW- Example 54.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-hello world"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00537 — Check whether the string contains a. Example 54.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00538 — Check whether the string starts with D. Example 54.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00539 — Check whether the string ends with e. Example 54.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00540 — Replace a with @. Example 54.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00541 — Convert the string to uppercase. Example 55.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "  TRIM ME  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  TRIM ME  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00542 — Convert the string to lowercase. Example 55.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00543 — Remove leading and trailing spaces. Example 55.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "trim me"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "trim me"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00544 — Return the string length. Example 55.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00545 — Append -DW. Example 55.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "  trim me  -DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  -DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00546 — Prefix with DW- Example 55.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-  trim me  "}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00547 — Check whether the string contains a. Example 55.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00548 — Check whether the string starts with D. Example 55.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00549 — Check whether the string ends with e. Example 55.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00550 — Replace a with @. Example 55.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00551 — Convert the string to uppercase. Example 56.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MIXED"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MIXED"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00552 — Convert the string to lowercase. Example 56.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mixed"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mixed"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00553 — Remove leading and trailing spaces. Example 56.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00554 — Return the string length. Example 56.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00555 — Append -DW. Example 56.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MiXeD-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00556 — Prefix with DW- Example 56.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MiXeD"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00557 — Check whether the string contains a. Example 56.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00558 — Check whether the string starts with D. Example 56.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00559 — Check whether the string ends with e. Example 56.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00560 — Replace a with @. Example 56.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00561 — Convert the string to uppercase. Example 57.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "CUSTOMER-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00562 — Convert the string to lowercase. Example 57.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00563 — Remove leading and trailing spaces. Example 57.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00564 — Return the string length. Example 57.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00565 — Append -DW. Example 57.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "customer-123-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00566 — Prefix with DW- Example 57.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-customer-123"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00567 — Check whether the string contains a. Example 57.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00568 — Check whether the string starts with D. Example 57.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00569 — Check whether the string ends with e. Example 57.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00570 — Replace a with @. Example 57.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00571 — Convert the string to uppercase. Example 58.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "API_RESPONSE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "API_RESPONSE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00572 — Convert the string to lowercase. Example 58.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00573 — Remove leading and trailing spaces. Example 58.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00574 — Return the string length. Example 58.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00575 — Append -DW. Example 58.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "api_response-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00576 — Prefix with DW- Example 58.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-api_response"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00577 — Check whether the string contains a. Example 58.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00578 — Check whether the string starts with D. Example 58.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00579 — Check whether the string ends with e. Example 58.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00580 — Replace a with @. Example 58.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "@pi_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "@pi_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00581 — Convert the string to uppercase. Example 59.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00582 — Convert the string to lowercase. Example 59.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00583 — Remove leading and trailing spaces. Example 59.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00584 — Return the string length. Example 59.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00585 — Append -DW. Example 59.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "Hyderabad-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00586 — Prefix with DW- Example 59.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-Hyderabad"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00587 — Check whether the string contains a. Example 59.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00588 — Check whether the string starts with D. Example 59.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00589 — Check whether the string ends with e. Example 59.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00590 — Replace a with @. Example 59.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "Hyder@b@d"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyder@b@d"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00591 — Convert the string to uppercase. Example 60.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "INTEGRATION"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "INTEGRATION"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00592 — Convert the string to lowercase. Example 60.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00593 — Remove leading and trailing spaces. Example 60.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00594 — Return the string length. Example 60.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00595 — Append -DW. Example 60.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "integration-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00596 — Prefix with DW- Example 60.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-integration"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00597 — Check whether the string contains a. Example 60.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00598 — Check whether the string starts with D. Example 60.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00599 — Check whether the string ends with e. Example 60.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00600 — Replace a with @. Example 60.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "integr@tion"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integr@tion"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00601 — Convert the string to uppercase. Example 61.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00602 — Convert the string to lowercase. Example 61.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00603 — Remove leading and trailing spaces. Example 61.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00604 — Return the string length. Example 61.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00605 — Append -DW. Example 61.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "narsing-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00606 — Prefix with DW- Example 61.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-narsing"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00607 — Check whether the string contains a. Example 61.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00608 — Check whether the string starts with D. Example 61.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00609 — Check whether the string ends with e. Example 61.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00610 — Replace a with @. Example 61.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "n@rsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "n@rsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00611 — Convert the string to uppercase. Example 62.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "DATAWEAVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DATAWEAVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00612 — Convert the string to lowercase. Example 62.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "dataweave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "dataweave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00613 — Remove leading and trailing spaces. Example 62.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "DataWeave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00614 — Return the string length. Example 62.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00615 — Append -DW. Example 62.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "DataWeave-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00616 — Prefix with DW- Example 62.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00617 — Check whether the string contains a. Example 62.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00618 — Check whether the string starts with D. Example 62.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00619 — Check whether the string ends with e. Example 62.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00620 — Replace a with @. Example 62.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "D@t@We@ve"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "D@t@We@ve"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00621 — Convert the string to uppercase. Example 63.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MULESOFT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MULESOFT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00622 — Convert the string to lowercase. Example 63.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mulesoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mulesoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00623 — Remove leading and trailing spaces. Example 63.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00624 — Return the string length. Example 63.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00625 — Append -DW. Example 63.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MuleSoft-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00626 — Prefix with DW- Example 63.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MuleSoft"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00627 — Check whether the string contains a. Example 63.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00628 — Check whether the string starts with D. Example 63.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00629 — Check whether the string ends with e. Example 63.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00630 — Replace a with @. Example 63.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00631 — Convert the string to uppercase. Example 64.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HELLO WORLD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HELLO WORLD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00632 — Convert the string to lowercase. Example 64.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00633 — Remove leading and trailing spaces. Example 64.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00634 — Return the string length. Example 64.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00635 — Append -DW. Example 64.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "hello world-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00636 — Prefix with DW- Example 64.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-hello world"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00637 — Check whether the string contains a. Example 64.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00638 — Check whether the string starts with D. Example 64.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00639 — Check whether the string ends with e. Example 64.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00640 — Replace a with @. Example 64.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00641 — Convert the string to uppercase. Example 65.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "  TRIM ME  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  TRIM ME  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00642 — Convert the string to lowercase. Example 65.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00643 — Remove leading and trailing spaces. Example 65.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "trim me"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "trim me"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00644 — Return the string length. Example 65.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00645 — Append -DW. Example 65.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "  trim me  -DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  -DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00646 — Prefix with DW- Example 65.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-  trim me  "}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00647 — Check whether the string contains a. Example 65.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00648 — Check whether the string starts with D. Example 65.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00649 — Check whether the string ends with e. Example 65.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00650 — Replace a with @. Example 65.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00651 — Convert the string to uppercase. Example 66.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MIXED"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MIXED"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00652 — Convert the string to lowercase. Example 66.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mixed"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mixed"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00653 — Remove leading and trailing spaces. Example 66.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00654 — Return the string length. Example 66.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00655 — Append -DW. Example 66.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MiXeD-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00656 — Prefix with DW- Example 66.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MiXeD"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00657 — Check whether the string contains a. Example 66.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00658 — Check whether the string starts with D. Example 66.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00659 — Check whether the string ends with e. Example 66.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00660 — Replace a with @. Example 66.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00661 — Convert the string to uppercase. Example 67.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "CUSTOMER-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00662 — Convert the string to lowercase. Example 67.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00663 — Remove leading and trailing spaces. Example 67.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00664 — Return the string length. Example 67.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00665 — Append -DW. Example 67.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "customer-123-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00666 — Prefix with DW- Example 67.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-customer-123"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00667 — Check whether the string contains a. Example 67.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00668 — Check whether the string starts with D. Example 67.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00669 — Check whether the string ends with e. Example 67.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00670 — Replace a with @. Example 67.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00671 — Convert the string to uppercase. Example 68.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "API_RESPONSE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "API_RESPONSE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00672 — Convert the string to lowercase. Example 68.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00673 — Remove leading and trailing spaces. Example 68.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00674 — Return the string length. Example 68.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00675 — Append -DW. Example 68.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "api_response-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00676 — Prefix with DW- Example 68.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-api_response"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00677 — Check whether the string contains a. Example 68.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00678 — Check whether the string starts with D. Example 68.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00679 — Check whether the string ends with e. Example 68.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00680 — Replace a with @. Example 68.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "@pi_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "@pi_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00681 — Convert the string to uppercase. Example 69.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00682 — Convert the string to lowercase. Example 69.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00683 — Remove leading and trailing spaces. Example 69.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00684 — Return the string length. Example 69.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00685 — Append -DW. Example 69.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "Hyderabad-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00686 — Prefix with DW- Example 69.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-Hyderabad"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00687 — Check whether the string contains a. Example 69.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00688 — Check whether the string starts with D. Example 69.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00689 — Check whether the string ends with e. Example 69.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00690 — Replace a with @. Example 69.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "Hyder@b@d"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyder@b@d"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00691 — Convert the string to uppercase. Example 70.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "INTEGRATION"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "INTEGRATION"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00692 — Convert the string to lowercase. Example 70.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00693 — Remove leading and trailing spaces. Example 70.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00694 — Return the string length. Example 70.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00695 — Append -DW. Example 70.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "integration-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00696 — Prefix with DW- Example 70.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-integration"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00697 — Check whether the string contains a. Example 70.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00698 — Check whether the string starts with D. Example 70.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00699 — Check whether the string ends with e. Example 70.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00700 — Replace a with @. Example 70.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "integr@tion"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integr@tion"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00701 — Convert the string to uppercase. Example 71.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00702 — Convert the string to lowercase. Example 71.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00703 — Remove leading and trailing spaces. Example 71.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00704 — Return the string length. Example 71.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00705 — Append -DW. Example 71.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "narsing-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00706 — Prefix with DW- Example 71.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-narsing"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00707 — Check whether the string contains a. Example 71.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00708 — Check whether the string starts with D. Example 71.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00709 — Check whether the string ends with e. Example 71.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00710 — Replace a with @. Example 71.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "n@rsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "n@rsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00711 — Convert the string to uppercase. Example 72.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "DATAWEAVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DATAWEAVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00712 — Convert the string to lowercase. Example 72.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "dataweave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "dataweave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00713 — Remove leading and trailing spaces. Example 72.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "DataWeave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00714 — Return the string length. Example 72.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00715 — Append -DW. Example 72.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "DataWeave-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00716 — Prefix with DW- Example 72.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00717 — Check whether the string contains a. Example 72.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00718 — Check whether the string starts with D. Example 72.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00719 — Check whether the string ends with e. Example 72.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00720 — Replace a with @. Example 72.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "D@t@We@ve"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "D@t@We@ve"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00721 — Convert the string to uppercase. Example 73.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MULESOFT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MULESOFT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00722 — Convert the string to lowercase. Example 73.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mulesoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mulesoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00723 — Remove leading and trailing spaces. Example 73.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00724 — Return the string length. Example 73.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00725 — Append -DW. Example 73.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MuleSoft-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00726 — Prefix with DW- Example 73.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MuleSoft"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00727 — Check whether the string contains a. Example 73.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00728 — Check whether the string starts with D. Example 73.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00729 — Check whether the string ends with e. Example 73.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00730 — Replace a with @. Example 73.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00731 — Convert the string to uppercase. Example 74.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HELLO WORLD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HELLO WORLD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00732 — Convert the string to lowercase. Example 74.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00733 — Remove leading and trailing spaces. Example 74.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00734 — Return the string length. Example 74.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00735 — Append -DW. Example 74.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "hello world-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00736 — Prefix with DW- Example 74.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-hello world"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00737 — Check whether the string contains a. Example 74.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00738 — Check whether the string starts with D. Example 74.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00739 — Check whether the string ends with e. Example 74.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00740 — Replace a with @. Example 74.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00741 — Convert the string to uppercase. Example 75.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "  TRIM ME  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  TRIM ME  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00742 — Convert the string to lowercase. Example 75.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00743 — Remove leading and trailing spaces. Example 75.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "trim me"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "trim me"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00744 — Return the string length. Example 75.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00745 — Append -DW. Example 75.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "  trim me  -DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  -DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00746 — Prefix with DW- Example 75.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-  trim me  "}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00747 — Check whether the string contains a. Example 75.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00748 — Check whether the string starts with D. Example 75.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00749 — Check whether the string ends with e. Example 75.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00750 — Replace a with @. Example 75.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
