# Easy DataWeave Questions 00251-00500

Questions 251–500 of 4000 in the Easy level.

## DW-00251 — Convert the string to uppercase. Example 26.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MIXED"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MIXED"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00252 — Convert the string to lowercase. Example 26.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mixed"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mixed"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00253 — Remove leading and trailing spaces. Example 26.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00254 — Return the string length. Example 26.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00255 — Append -DW. Example 26.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MiXeD-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00256 — Prefix with DW- Example 26.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MiXeD"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00257 — Check whether the string contains a. Example 26.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00258 — Check whether the string starts with D. Example 26.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00259 — Check whether the string ends with e. Example 26.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00260 — Replace a with @. Example 26.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00261 — Convert the string to uppercase. Example 27.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "CUSTOMER-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00262 — Convert the string to lowercase. Example 27.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00263 — Remove leading and trailing spaces. Example 27.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00264 — Return the string length. Example 27.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00265 — Append -DW. Example 27.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "customer-123-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00266 — Prefix with DW- Example 27.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-customer-123"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00267 — Check whether the string contains a. Example 27.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00268 — Check whether the string starts with D. Example 27.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00269 — Check whether the string ends with e. Example 27.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00270 — Replace a with @. Example 27.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00271 — Convert the string to uppercase. Example 28.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "API_RESPONSE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "API_RESPONSE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00272 — Convert the string to lowercase. Example 28.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00273 — Remove leading and trailing spaces. Example 28.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00274 — Return the string length. Example 28.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00275 — Append -DW. Example 28.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "api_response-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00276 — Prefix with DW- Example 28.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-api_response"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00277 — Check whether the string contains a. Example 28.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00278 — Check whether the string starts with D. Example 28.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00279 — Check whether the string ends with e. Example 28.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00280 — Replace a with @. Example 28.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "@pi_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "@pi_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00281 — Convert the string to uppercase. Example 29.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00282 — Convert the string to lowercase. Example 29.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00283 — Remove leading and trailing spaces. Example 29.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00284 — Return the string length. Example 29.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00285 — Append -DW. Example 29.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "Hyderabad-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00286 — Prefix with DW- Example 29.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-Hyderabad"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00287 — Check whether the string contains a. Example 29.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00288 — Check whether the string starts with D. Example 29.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00289 — Check whether the string ends with e. Example 29.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00290 — Replace a with @. Example 29.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "Hyder@b@d"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyder@b@d"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00291 — Convert the string to uppercase. Example 30.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "INTEGRATION"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "INTEGRATION"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00292 — Convert the string to lowercase. Example 30.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00293 — Remove leading and trailing spaces. Example 30.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00294 — Return the string length. Example 30.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00295 — Append -DW. Example 30.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "integration-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00296 — Prefix with DW- Example 30.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-integration"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00297 — Check whether the string contains a. Example 30.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00298 — Check whether the string starts with D. Example 30.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00299 — Check whether the string ends with e. Example 30.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00300 — Replace a with @. Example 30.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "integr@tion"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integr@tion"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00301 — Convert the string to uppercase. Example 31.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00302 — Convert the string to lowercase. Example 31.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00303 — Remove leading and trailing spaces. Example 31.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00304 — Return the string length. Example 31.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00305 — Append -DW. Example 31.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "narsing-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00306 — Prefix with DW- Example 31.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-narsing"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00307 — Check whether the string contains a. Example 31.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00308 — Check whether the string starts with D. Example 31.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00309 — Check whether the string ends with e. Example 31.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00310 — Replace a with @. Example 31.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "n@rsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "n@rsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00311 — Convert the string to uppercase. Example 32.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "DATAWEAVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DATAWEAVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00312 — Convert the string to lowercase. Example 32.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "dataweave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "dataweave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00313 — Remove leading and trailing spaces. Example 32.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "DataWeave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00314 — Return the string length. Example 32.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00315 — Append -DW. Example 32.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "DataWeave-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00316 — Prefix with DW- Example 32.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00317 — Check whether the string contains a. Example 32.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00318 — Check whether the string starts with D. Example 32.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00319 — Check whether the string ends with e. Example 32.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00320 — Replace a with @. Example 32.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "D@t@We@ve"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "D@t@We@ve"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00321 — Convert the string to uppercase. Example 33.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MULESOFT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MULESOFT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00322 — Convert the string to lowercase. Example 33.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mulesoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mulesoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00323 — Remove leading and trailing spaces. Example 33.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00324 — Return the string length. Example 33.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00325 — Append -DW. Example 33.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MuleSoft-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00326 — Prefix with DW- Example 33.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MuleSoft"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00327 — Check whether the string contains a. Example 33.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00328 — Check whether the string starts with D. Example 33.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00329 — Check whether the string ends with e. Example 33.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00330 — Replace a with @. Example 33.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00331 — Convert the string to uppercase. Example 34.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HELLO WORLD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HELLO WORLD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00332 — Convert the string to lowercase. Example 34.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00333 — Remove leading and trailing spaces. Example 34.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00334 — Return the string length. Example 34.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00335 — Append -DW. Example 34.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "hello world-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00336 — Prefix with DW- Example 34.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-hello world"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00337 — Check whether the string contains a. Example 34.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00338 — Check whether the string starts with D. Example 34.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00339 — Check whether the string ends with e. Example 34.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00340 — Replace a with @. Example 34.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00341 — Convert the string to uppercase. Example 35.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "  TRIM ME  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  TRIM ME  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00342 — Convert the string to lowercase. Example 35.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00343 — Remove leading and trailing spaces. Example 35.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "trim me"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "trim me"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00344 — Return the string length. Example 35.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00345 — Append -DW. Example 35.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "  trim me  -DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  -DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00346 — Prefix with DW- Example 35.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-  trim me  "}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00347 — Check whether the string contains a. Example 35.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00348 — Check whether the string starts with D. Example 35.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00349 — Check whether the string ends with e. Example 35.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00350 — Replace a with @. Example 35.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00351 — Convert the string to uppercase. Example 36.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MIXED"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MIXED"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00352 — Convert the string to lowercase. Example 36.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mixed"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mixed"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00353 — Remove leading and trailing spaces. Example 36.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00354 — Return the string length. Example 36.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00355 — Append -DW. Example 36.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MiXeD-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00356 — Prefix with DW- Example 36.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MiXeD"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00357 — Check whether the string contains a. Example 36.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00358 — Check whether the string starts with D. Example 36.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00359 — Check whether the string ends with e. Example 36.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00360 — Replace a with @. Example 36.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00361 — Convert the string to uppercase. Example 37.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "CUSTOMER-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00362 — Convert the string to lowercase. Example 37.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00363 — Remove leading and trailing spaces. Example 37.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00364 — Return the string length. Example 37.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00365 — Append -DW. Example 37.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "customer-123-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00366 — Prefix with DW- Example 37.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-customer-123"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00367 — Check whether the string contains a. Example 37.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00368 — Check whether the string starts with D. Example 37.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00369 — Check whether the string ends with e. Example 37.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00370 — Replace a with @. Example 37.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00371 — Convert the string to uppercase. Example 38.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "API_RESPONSE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "API_RESPONSE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00372 — Convert the string to lowercase. Example 38.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00373 — Remove leading and trailing spaces. Example 38.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00374 — Return the string length. Example 38.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00375 — Append -DW. Example 38.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "api_response-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00376 — Prefix with DW- Example 38.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-api_response"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00377 — Check whether the string contains a. Example 38.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00378 — Check whether the string starts with D. Example 38.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00379 — Check whether the string ends with e. Example 38.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00380 — Replace a with @. Example 38.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "@pi_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "@pi_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00381 — Convert the string to uppercase. Example 39.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00382 — Convert the string to lowercase. Example 39.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00383 — Remove leading and trailing spaces. Example 39.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00384 — Return the string length. Example 39.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00385 — Append -DW. Example 39.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "Hyderabad-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00386 — Prefix with DW- Example 39.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-Hyderabad"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00387 — Check whether the string contains a. Example 39.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00388 — Check whether the string starts with D. Example 39.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00389 — Check whether the string ends with e. Example 39.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00390 — Replace a with @. Example 39.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "Hyder@b@d"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyder@b@d"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00391 — Convert the string to uppercase. Example 40.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "INTEGRATION"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "INTEGRATION"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00392 — Convert the string to lowercase. Example 40.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00393 — Remove leading and trailing spaces. Example 40.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00394 — Return the string length. Example 40.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00395 — Append -DW. Example 40.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "integration-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00396 — Prefix with DW- Example 40.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-integration"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00397 — Check whether the string contains a. Example 40.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00398 — Check whether the string starts with D. Example 40.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00399 — Check whether the string ends with e. Example 40.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00400 — Replace a with @. Example 40.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "integr@tion"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integr@tion"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00401 — Convert the string to uppercase. Example 41.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00402 — Convert the string to lowercase. Example 41.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00403 — Remove leading and trailing spaces. Example 41.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00404 — Return the string length. Example 41.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00405 — Append -DW. Example 41.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "narsing-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00406 — Prefix with DW- Example 41.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-narsing"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00407 — Check whether the string contains a. Example 41.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00408 — Check whether the string starts with D. Example 41.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00409 — Check whether the string ends with e. Example 41.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00410 — Replace a with @. Example 41.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "n@rsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "n@rsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00411 — Convert the string to uppercase. Example 42.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "DATAWEAVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DATAWEAVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00412 — Convert the string to lowercase. Example 42.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "dataweave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "dataweave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00413 — Remove leading and trailing spaces. Example 42.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "DataWeave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00414 — Return the string length. Example 42.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00415 — Append -DW. Example 42.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "DataWeave-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00416 — Prefix with DW- Example 42.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00417 — Check whether the string contains a. Example 42.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00418 — Check whether the string starts with D. Example 42.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00419 — Check whether the string ends with e. Example 42.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00420 — Replace a with @. Example 42.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "D@t@We@ve"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "D@t@We@ve"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00421 — Convert the string to uppercase. Example 43.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MULESOFT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MULESOFT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00422 — Convert the string to lowercase. Example 43.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mulesoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mulesoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00423 — Remove leading and trailing spaces. Example 43.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00424 — Return the string length. Example 43.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00425 — Append -DW. Example 43.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MuleSoft-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00426 — Prefix with DW- Example 43.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MuleSoft"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00427 — Check whether the string contains a. Example 43.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00428 — Check whether the string starts with D. Example 43.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00429 — Check whether the string ends with e. Example 43.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00430 — Replace a with @. Example 43.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00431 — Convert the string to uppercase. Example 44.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HELLO WORLD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HELLO WORLD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00432 — Convert the string to lowercase. Example 44.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00433 — Remove leading and trailing spaces. Example 44.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00434 — Return the string length. Example 44.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00435 — Append -DW. Example 44.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "hello world-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00436 — Prefix with DW- Example 44.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-hello world"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00437 — Check whether the string contains a. Example 44.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00438 — Check whether the string starts with D. Example 44.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00439 — Check whether the string ends with e. Example 44.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00440 — Replace a with @. Example 44.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00441 — Convert the string to uppercase. Example 45.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "  TRIM ME  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  TRIM ME  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00442 — Convert the string to lowercase. Example 45.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00443 — Remove leading and trailing spaces. Example 45.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "trim me"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "trim me"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00444 — Return the string length. Example 45.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00445 — Append -DW. Example 45.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "  trim me  -DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  -DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00446 — Prefix with DW- Example 45.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-  trim me  "}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00447 — Check whether the string contains a. Example 45.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00448 — Check whether the string starts with D. Example 45.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00449 — Check whether the string ends with e. Example 45.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00450 — Replace a with @. Example 45.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00451 — Convert the string to uppercase. Example 46.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MIXED"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MIXED"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00452 — Convert the string to lowercase. Example 46.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mixed"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mixed"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00453 — Remove leading and trailing spaces. Example 46.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00454 — Return the string length. Example 46.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00455 — Append -DW. Example 46.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MiXeD-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00456 — Prefix with DW- Example 46.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MiXeD"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00457 — Check whether the string contains a. Example 46.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00458 — Check whether the string starts with D. Example 46.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00459 — Check whether the string ends with e. Example 46.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00460 — Replace a with @. Example 46.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00461 — Convert the string to uppercase. Example 47.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "CUSTOMER-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00462 — Convert the string to lowercase. Example 47.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00463 — Remove leading and trailing spaces. Example 47.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00464 — Return the string length. Example 47.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00465 — Append -DW. Example 47.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "customer-123-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00466 — Prefix with DW- Example 47.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-customer-123"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00467 — Check whether the string contains a. Example 47.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00468 — Check whether the string starts with D. Example 47.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00469 — Check whether the string ends with e. Example 47.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00470 — Replace a with @. Example 47.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00471 — Convert the string to uppercase. Example 48.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "API_RESPONSE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "API_RESPONSE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00472 — Convert the string to lowercase. Example 48.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00473 — Remove leading and trailing spaces. Example 48.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00474 — Return the string length. Example 48.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00475 — Append -DW. Example 48.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "api_response-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00476 — Prefix with DW- Example 48.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-api_response"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00477 — Check whether the string contains a. Example 48.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00478 — Check whether the string starts with D. Example 48.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00479 — Check whether the string ends with e. Example 48.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00480 — Replace a with @. Example 48.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "@pi_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "@pi_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00481 — Convert the string to uppercase. Example 49.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00482 — Convert the string to lowercase. Example 49.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00483 — Remove leading and trailing spaces. Example 49.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00484 — Return the string length. Example 49.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00485 — Append -DW. Example 49.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "Hyderabad-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00486 — Prefix with DW- Example 49.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-Hyderabad"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00487 — Check whether the string contains a. Example 49.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00488 — Check whether the string starts with D. Example 49.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00489 — Check whether the string ends with e. Example 49.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00490 — Replace a with @. Example 49.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "Hyder@b@d"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyder@b@d"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00491 — Convert the string to uppercase. Example 50.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "INTEGRATION"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "INTEGRATION"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00492 — Convert the string to lowercase. Example 50.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00493 — Remove leading and trailing spaces. Example 50.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00494 — Return the string length. Example 50.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00495 — Append -DW. Example 50.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "integration-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00496 — Prefix with DW- Example 50.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-integration"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00497 — Check whether the string contains a. Example 50.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00498 — Check whether the string starts with D. Example 50.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00499 — Check whether the string ends with e. Example 50.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00500 — Replace a with @. Example 50.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "integr@tion"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integr@tion"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
