# Easy DataWeave Questions 02501-02750

Questions 2501–2750 of 4000 in the Easy level.

## DW-02501 — Double every item. Example 51.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02502 — Increment every item. Example 51.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02503 — Keep values greater than five. Example 51.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02504 — Keep even values. Example 51.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02505 — Count items. Example 51.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02506 — Sum values. Example 51.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02507 — Append 99. Example 51.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02508 — Prepend 0. Example 51.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02509 — Return the first item. Example 51.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02510 — Return the last item. Example 51.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02511 — Double every item. Example 52.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02512 — Increment every item. Example 52.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[6, 11, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 11, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02513 — Keep values greater than five. Example 52.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 15]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02514 — Keep even values. Example 52.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02515 — Count items. Example 52.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02516 — Sum values. Example 52.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02517 — Append 99. Example 52.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[5, 10, 15, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [5, 10, 15, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02518 — Prepend 0. Example 52.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 5, 10, 15]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 5, 10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02519 — Return the first item. Example 52.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
5
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 5.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02520 — Return the last item. Example 52.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
15
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 15.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02521 — Double every item. Example 53.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[0, 2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02522 — Increment every item. Example 53.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[1, 2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02523 — Keep values greater than five. Example 53.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02524 — Keep even values. Example 53.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[0, 2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02525 — Count items. Example 53.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02526 — Sum values. Example 53.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02527 — Append 99. Example 53.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[0, 1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02528 — Prepend 0. Example 53.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02529 — Return the first item. Example 53.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
0
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 0.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02530 — Return the last item. Example 53.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02531 — Double every item. Example 54.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[4, 8, 12, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 8, 12, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02532 — Increment every item. Example 54.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[3, 5, 7, 9]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [3, 5, 7, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02533 — Keep values greater than five. Example 54.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02534 — Keep even values. Example 54.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2, 4, 6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02535 — Count items. Example 54.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02536 — Sum values. Example 54.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02537 — Append 99. Example 54.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[2, 4, 6, 8, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02538 — Prepend 0. Example 54.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 2, 4, 6, 8]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02539 — Return the first item. Example 54.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02540 — Return the last item. Example 54.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
8
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 8.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02541 — Double every item. Example 55.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[20, 40, 60]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [20, 40, 60].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02542 — Increment every item. Example 55.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[11, 21, 31]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [11, 21, 31].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02543 — Keep values greater than five. Example 55.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02544 — Keep even values. Example 55.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02545 — Count items. Example 55.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02546 — Sum values. Example 55.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
60
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 60.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02547 — Append 99. Example 55.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[10, 20, 30, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02548 — Prepend 0. Example 55.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 10, 20, 30]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02549 — Return the first item. Example 55.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
10
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 10.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02550 — Return the last item. Example 55.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02551 — Double every item. Example 56.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[-4, -2, 0, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-4, -2, 0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02552 — Increment every item. Example 56.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[-1, 0, 1, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-1, 0, 1, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02553 — Keep values greater than five. Example 56.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02554 — Keep even values. Example 56.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[-2, 0]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [-2, 0].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02555 — Count items. Example 56.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02556 — Sum values. Example 56.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02557 — Append 99. Example 56.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[-2, -1, 0, 1, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [-2, -1, 0, 1, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02558 — Prepend 0. Example 56.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, -2, -1, 0, 1]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, -2, -1, 0, 1].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02559 — Return the first item. Example 56.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02560 — Return the last item. Example 56.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02561 — Double every item. Example 57.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[6, 12, 18]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 12, 18].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02562 — Increment every item. Example 57.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[4, 7, 10]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 7, 10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02563 — Keep values greater than five. Example 57.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 9]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02564 — Keep even values. Example 57.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[6]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02565 — Count items. Example 57.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02566 — Sum values. Example 57.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
18
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 18.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02567 — Append 99. Example 57.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[3, 6, 9, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [3, 6, 9, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02568 — Prepend 0. Example 57.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 3, 6, 9]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 3, 6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02569 — Return the first item. Example 57.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02570 — Return the last item. Example 57.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
9
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 9.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02571 — Double every item. Example 58.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[200, 400, 600]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [200, 400, 600].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02572 — Increment every item. Example 58.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[101, 201, 301]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [101, 201, 301].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02573 — Keep values greater than five. Example 58.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02574 — Keep even values. Example 58.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02575 — Count items. Example 58.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02576 — Sum values. Example 58.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
600
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 600.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02577 — Append 99. Example 58.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[100, 200, 300, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02578 — Prepend 0. Example 58.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 100, 200, 300]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02579 — Return the first item. Example 58.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
100
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 100.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02580 — Return the last item. Example 58.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
300
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 300.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02581 — Double every item. Example 59.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 10, 20, 40]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 10, 20, 40].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02582 — Increment every item. Example 59.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 6, 11, 21]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 6, 11, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02583 — Keep values greater than five. Example 59.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02584 — Keep even values. Example 59.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02585 — Count items. Example 59.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02586 — Sum values. Example 59.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
36
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 36.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02587 — Append 99. Example 59.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 5, 10, 20, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 5, 10, 20, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02588 — Prepend 0. Example 59.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 5, 10, 20]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 5, 10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02589 — Return the first item. Example 59.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02590 — Return the last item. Example 59.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02591 — Double every item. Example 60.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[14, 28, 42]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [14, 28, 42].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02592 — Increment every item. Example 60.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[8, 15, 22]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [8, 15, 22].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02593 — Keep values greater than five. Example 60.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[7, 14, 21]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02594 — Keep even values. Example 60.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[14]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [14].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02595 — Count items. Example 60.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02596 — Sum values. Example 60.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
42
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 42.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02597 — Append 99. Example 60.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[7, 14, 21, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02598 — Prepend 0. Example 60.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 7, 14, 21]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02599 — Return the first item. Example 60.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
7
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 7.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02600 — Return the last item. Example 60.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
21
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 21.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02601 — Double every item. Example 61.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02602 — Increment every item. Example 61.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02603 — Keep values greater than five. Example 61.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02604 — Keep even values. Example 61.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02605 — Count items. Example 61.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02606 — Sum values. Example 61.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02607 — Append 99. Example 61.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02608 — Prepend 0. Example 61.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02609 — Return the first item. Example 61.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02610 — Return the last item. Example 61.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02611 — Double every item. Example 62.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02612 — Increment every item. Example 62.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[6, 11, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 11, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02613 — Keep values greater than five. Example 62.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 15]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02614 — Keep even values. Example 62.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02615 — Count items. Example 62.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02616 — Sum values. Example 62.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02617 — Append 99. Example 62.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[5, 10, 15, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [5, 10, 15, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02618 — Prepend 0. Example 62.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 5, 10, 15]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 5, 10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02619 — Return the first item. Example 62.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
5
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 5.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02620 — Return the last item. Example 62.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
15
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 15.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02621 — Double every item. Example 63.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[0, 2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02622 — Increment every item. Example 63.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[1, 2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02623 — Keep values greater than five. Example 63.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02624 — Keep even values. Example 63.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[0, 2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02625 — Count items. Example 63.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02626 — Sum values. Example 63.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02627 — Append 99. Example 63.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[0, 1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02628 — Prepend 0. Example 63.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02629 — Return the first item. Example 63.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
0
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 0.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02630 — Return the last item. Example 63.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02631 — Double every item. Example 64.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[4, 8, 12, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 8, 12, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02632 — Increment every item. Example 64.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[3, 5, 7, 9]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [3, 5, 7, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02633 — Keep values greater than five. Example 64.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02634 — Keep even values. Example 64.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2, 4, 6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02635 — Count items. Example 64.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02636 — Sum values. Example 64.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02637 — Append 99. Example 64.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[2, 4, 6, 8, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02638 — Prepend 0. Example 64.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 2, 4, 6, 8]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02639 — Return the first item. Example 64.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02640 — Return the last item. Example 64.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
8
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 8.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02641 — Double every item. Example 65.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[20, 40, 60]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [20, 40, 60].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02642 — Increment every item. Example 65.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[11, 21, 31]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [11, 21, 31].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02643 — Keep values greater than five. Example 65.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02644 — Keep even values. Example 65.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02645 — Count items. Example 65.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02646 — Sum values. Example 65.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
60
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 60.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02647 — Append 99. Example 65.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[10, 20, 30, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02648 — Prepend 0. Example 65.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 10, 20, 30]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02649 — Return the first item. Example 65.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
10
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 10.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02650 — Return the last item. Example 65.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02651 — Double every item. Example 66.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[-4, -2, 0, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-4, -2, 0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02652 — Increment every item. Example 66.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[-1, 0, 1, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-1, 0, 1, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02653 — Keep values greater than five. Example 66.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02654 — Keep even values. Example 66.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[-2, 0]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [-2, 0].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02655 — Count items. Example 66.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02656 — Sum values. Example 66.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02657 — Append 99. Example 66.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[-2, -1, 0, 1, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [-2, -1, 0, 1, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02658 — Prepend 0. Example 66.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, -2, -1, 0, 1]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, -2, -1, 0, 1].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02659 — Return the first item. Example 66.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02660 — Return the last item. Example 66.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02661 — Double every item. Example 67.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[6, 12, 18]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 12, 18].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02662 — Increment every item. Example 67.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[4, 7, 10]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 7, 10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02663 — Keep values greater than five. Example 67.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 9]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02664 — Keep even values. Example 67.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[6]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02665 — Count items. Example 67.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02666 — Sum values. Example 67.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
18
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 18.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02667 — Append 99. Example 67.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[3, 6, 9, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [3, 6, 9, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02668 — Prepend 0. Example 67.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 3, 6, 9]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 3, 6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02669 — Return the first item. Example 67.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02670 — Return the last item. Example 67.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
9
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 9.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02671 — Double every item. Example 68.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[200, 400, 600]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [200, 400, 600].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02672 — Increment every item. Example 68.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[101, 201, 301]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [101, 201, 301].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02673 — Keep values greater than five. Example 68.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02674 — Keep even values. Example 68.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02675 — Count items. Example 68.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02676 — Sum values. Example 68.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
600
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 600.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02677 — Append 99. Example 68.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[100, 200, 300, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02678 — Prepend 0. Example 68.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 100, 200, 300]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02679 — Return the first item. Example 68.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
100
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 100.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02680 — Return the last item. Example 68.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
300
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 300.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02681 — Double every item. Example 69.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 10, 20, 40]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 10, 20, 40].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02682 — Increment every item. Example 69.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 6, 11, 21]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 6, 11, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02683 — Keep values greater than five. Example 69.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02684 — Keep even values. Example 69.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02685 — Count items. Example 69.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02686 — Sum values. Example 69.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
36
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 36.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02687 — Append 99. Example 69.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 5, 10, 20, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 5, 10, 20, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02688 — Prepend 0. Example 69.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 5, 10, 20]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 5, 10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02689 — Return the first item. Example 69.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02690 — Return the last item. Example 69.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02691 — Double every item. Example 70.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[14, 28, 42]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [14, 28, 42].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02692 — Increment every item. Example 70.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[8, 15, 22]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [8, 15, 22].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02693 — Keep values greater than five. Example 70.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[7, 14, 21]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02694 — Keep even values. Example 70.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[14]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [14].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02695 — Count items. Example 70.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02696 — Sum values. Example 70.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
42
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 42.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02697 — Append 99. Example 70.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[7, 14, 21, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02698 — Prepend 0. Example 70.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 7, 14, 21]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02699 — Return the first item. Example 70.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
7
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 7.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02700 — Return the last item. Example 70.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
21
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 21.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02701 — Double every item. Example 71.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02702 — Increment every item. Example 71.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02703 — Keep values greater than five. Example 71.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02704 — Keep even values. Example 71.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02705 — Count items. Example 71.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02706 — Sum values. Example 71.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02707 — Append 99. Example 71.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02708 — Prepend 0. Example 71.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02709 — Return the first item. Example 71.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02710 — Return the last item. Example 71.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02711 — Double every item. Example 72.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02712 — Increment every item. Example 72.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[6, 11, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 11, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02713 — Keep values greater than five. Example 72.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 15]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02714 — Keep even values. Example 72.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02715 — Count items. Example 72.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02716 — Sum values. Example 72.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02717 — Append 99. Example 72.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[5, 10, 15, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [5, 10, 15, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02718 — Prepend 0. Example 72.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 5, 10, 15]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 5, 10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02719 — Return the first item. Example 72.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
5
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 5.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02720 — Return the last item. Example 72.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
15
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 15.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02721 — Double every item. Example 73.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[0, 2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02722 — Increment every item. Example 73.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[1, 2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02723 — Keep values greater than five. Example 73.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02724 — Keep even values. Example 73.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[0, 2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02725 — Count items. Example 73.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02726 — Sum values. Example 73.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02727 — Append 99. Example 73.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[0, 1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02728 — Prepend 0. Example 73.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02729 — Return the first item. Example 73.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
0
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 0.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02730 — Return the last item. Example 73.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02731 — Double every item. Example 74.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[4, 8, 12, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 8, 12, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02732 — Increment every item. Example 74.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[3, 5, 7, 9]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [3, 5, 7, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02733 — Keep values greater than five. Example 74.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02734 — Keep even values. Example 74.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2, 4, 6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02735 — Count items. Example 74.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02736 — Sum values. Example 74.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02737 — Append 99. Example 74.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[2, 4, 6, 8, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02738 — Prepend 0. Example 74.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 2, 4, 6, 8]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02739 — Return the first item. Example 74.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02740 — Return the last item. Example 74.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
8
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 8.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02741 — Double every item. Example 75.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[20, 40, 60]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [20, 40, 60].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02742 — Increment every item. Example 75.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[11, 21, 31]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [11, 21, 31].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02743 — Keep values greater than five. Example 75.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02744 — Keep even values. Example 75.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02745 — Count items. Example 75.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02746 — Sum values. Example 75.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
60
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 60.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02747 — Append 99. Example 75.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[10, 20, 30, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02748 — Prepend 0. Example 75.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 10, 20, 30]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02749 — Return the first item. Example 75.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
10
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 10.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02750 — Return the last item. Example 75.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
