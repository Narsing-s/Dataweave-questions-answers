# Easy DataWeave Questions 00001-00250

Questions 1–250 of 4000 in the Easy level.

## DW-00001 — Convert the string to uppercase. Example 1.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00002 — Convert the string to lowercase. Example 1.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00003 — Remove leading and trailing spaces. Example 1.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00004 — Return the string length. Example 1.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00005 — Append -DW. Example 1.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "narsing-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00006 — Prefix with DW- Example 1.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-narsing"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00007 — Check whether the string contains a. Example 1.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00008 — Check whether the string starts with D. Example 1.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00009 — Check whether the string ends with e. Example 1.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00010 — Replace a with @. Example 1.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "n@rsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "n@rsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00011 — Convert the string to uppercase. Example 2.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "DATAWEAVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DATAWEAVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00012 — Convert the string to lowercase. Example 2.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "dataweave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "dataweave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00013 — Remove leading and trailing spaces. Example 2.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "DataWeave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00014 — Return the string length. Example 2.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00015 — Append -DW. Example 2.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "DataWeave-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00016 — Prefix with DW- Example 2.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00017 — Check whether the string contains a. Example 2.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00018 — Check whether the string starts with D. Example 2.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00019 — Check whether the string ends with e. Example 2.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00020 — Replace a with @. Example 2.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "D@t@We@ve"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "D@t@We@ve"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00021 — Convert the string to uppercase. Example 3.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MULESOFT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MULESOFT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00022 — Convert the string to lowercase. Example 3.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mulesoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mulesoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00023 — Remove leading and trailing spaces. Example 3.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00024 — Return the string length. Example 3.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00025 — Append -DW. Example 3.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MuleSoft-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00026 — Prefix with DW- Example 3.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MuleSoft"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00027 — Check whether the string contains a. Example 3.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00028 — Check whether the string starts with D. Example 3.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00029 — Check whether the string ends with e. Example 3.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00030 — Replace a with @. Example 3.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00031 — Convert the string to uppercase. Example 4.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HELLO WORLD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HELLO WORLD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00032 — Convert the string to lowercase. Example 4.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00033 — Remove leading and trailing spaces. Example 4.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00034 — Return the string length. Example 4.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00035 — Append -DW. Example 4.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "hello world-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00036 — Prefix with DW- Example 4.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-hello world"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00037 — Check whether the string contains a. Example 4.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00038 — Check whether the string starts with D. Example 4.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00039 — Check whether the string ends with e. Example 4.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00040 — Replace a with @. Example 4.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00041 — Convert the string to uppercase. Example 5.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "  TRIM ME  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  TRIM ME  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00042 — Convert the string to lowercase. Example 5.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00043 — Remove leading and trailing spaces. Example 5.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "trim me"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "trim me"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00044 — Return the string length. Example 5.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00045 — Append -DW. Example 5.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "  trim me  -DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  -DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00046 — Prefix with DW- Example 5.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-  trim me  "}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00047 — Check whether the string contains a. Example 5.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00048 — Check whether the string starts with D. Example 5.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00049 — Check whether the string ends with e. Example 5.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00050 — Replace a with @. Example 5.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00051 — Convert the string to uppercase. Example 6.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MIXED"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MIXED"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00052 — Convert the string to lowercase. Example 6.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mixed"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mixed"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00053 — Remove leading and trailing spaces. Example 6.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00054 — Return the string length. Example 6.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00055 — Append -DW. Example 6.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MiXeD-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00056 — Prefix with DW- Example 6.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MiXeD"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00057 — Check whether the string contains a. Example 6.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00058 — Check whether the string starts with D. Example 6.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00059 — Check whether the string ends with e. Example 6.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00060 — Replace a with @. Example 6.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00061 — Convert the string to uppercase. Example 7.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "CUSTOMER-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00062 — Convert the string to lowercase. Example 7.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00063 — Remove leading and trailing spaces. Example 7.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00064 — Return the string length. Example 7.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00065 — Append -DW. Example 7.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "customer-123-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00066 — Prefix with DW- Example 7.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-customer-123"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00067 — Check whether the string contains a. Example 7.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00068 — Check whether the string starts with D. Example 7.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00069 — Check whether the string ends with e. Example 7.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00070 — Replace a with @. Example 7.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00071 — Convert the string to uppercase. Example 8.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "API_RESPONSE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "API_RESPONSE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00072 — Convert the string to lowercase. Example 8.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00073 — Remove leading and trailing spaces. Example 8.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00074 — Return the string length. Example 8.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00075 — Append -DW. Example 8.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "api_response-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00076 — Prefix with DW- Example 8.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-api_response"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00077 — Check whether the string contains a. Example 8.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00078 — Check whether the string starts with D. Example 8.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00079 — Check whether the string ends with e. Example 8.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00080 — Replace a with @. Example 8.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "@pi_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "@pi_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00081 — Convert the string to uppercase. Example 9.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00082 — Convert the string to lowercase. Example 9.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00083 — Remove leading and trailing spaces. Example 9.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00084 — Return the string length. Example 9.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00085 — Append -DW. Example 9.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "Hyderabad-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00086 — Prefix with DW- Example 9.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-Hyderabad"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00087 — Check whether the string contains a. Example 9.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00088 — Check whether the string starts with D. Example 9.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00089 — Check whether the string ends with e. Example 9.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00090 — Replace a with @. Example 9.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "Hyder@b@d"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyder@b@d"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00091 — Convert the string to uppercase. Example 10.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "INTEGRATION"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "INTEGRATION"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00092 — Convert the string to lowercase. Example 10.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00093 — Remove leading and trailing spaces. Example 10.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00094 — Return the string length. Example 10.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00095 — Append -DW. Example 10.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "integration-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00096 — Prefix with DW- Example 10.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-integration"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00097 — Check whether the string contains a. Example 10.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00098 — Check whether the string starts with D. Example 10.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00099 — Check whether the string ends with e. Example 10.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00100 — Replace a with @. Example 10.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "integr@tion"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integr@tion"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00101 — Convert the string to uppercase. Example 11.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00102 — Convert the string to lowercase. Example 11.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00103 — Remove leading and trailing spaces. Example 11.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00104 — Return the string length. Example 11.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00105 — Append -DW. Example 11.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "narsing-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00106 — Prefix with DW- Example 11.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-narsing"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00107 — Check whether the string contains a. Example 11.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00108 — Check whether the string starts with D. Example 11.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00109 — Check whether the string ends with e. Example 11.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00110 — Replace a with @. Example 11.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "n@rsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "n@rsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00111 — Convert the string to uppercase. Example 12.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "DATAWEAVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DATAWEAVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00112 — Convert the string to lowercase. Example 12.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "dataweave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "dataweave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00113 — Remove leading and trailing spaces. Example 12.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "DataWeave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00114 — Return the string length. Example 12.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00115 — Append -DW. Example 12.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "DataWeave-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00116 — Prefix with DW- Example 12.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00117 — Check whether the string contains a. Example 12.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00118 — Check whether the string starts with D. Example 12.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00119 — Check whether the string ends with e. Example 12.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00120 — Replace a with @. Example 12.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "D@t@We@ve"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "D@t@We@ve"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00121 — Convert the string to uppercase. Example 13.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MULESOFT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MULESOFT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00122 — Convert the string to lowercase. Example 13.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mulesoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mulesoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00123 — Remove leading and trailing spaces. Example 13.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00124 — Return the string length. Example 13.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00125 — Append -DW. Example 13.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MuleSoft-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00126 — Prefix with DW- Example 13.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MuleSoft"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00127 — Check whether the string contains a. Example 13.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00128 — Check whether the string starts with D. Example 13.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00129 — Check whether the string ends with e. Example 13.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00130 — Replace a with @. Example 13.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00131 — Convert the string to uppercase. Example 14.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HELLO WORLD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HELLO WORLD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00132 — Convert the string to lowercase. Example 14.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00133 — Remove leading and trailing spaces. Example 14.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00134 — Return the string length. Example 14.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00135 — Append -DW. Example 14.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "hello world-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00136 — Prefix with DW- Example 14.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-hello world"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00137 — Check whether the string contains a. Example 14.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00138 — Check whether the string starts with D. Example 14.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00139 — Check whether the string ends with e. Example 14.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00140 — Replace a with @. Example 14.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00141 — Convert the string to uppercase. Example 15.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "  TRIM ME  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  TRIM ME  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00142 — Convert the string to lowercase. Example 15.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00143 — Remove leading and trailing spaces. Example 15.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "trim me"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "trim me"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00144 — Return the string length. Example 15.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00145 — Append -DW. Example 15.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "  trim me  -DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  -DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00146 — Prefix with DW- Example 15.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-  trim me  "}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00147 — Check whether the string contains a. Example 15.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00148 — Check whether the string starts with D. Example 15.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00149 — Check whether the string ends with e. Example 15.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00150 — Replace a with @. Example 15.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00151 — Convert the string to uppercase. Example 16.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MIXED"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MIXED"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00152 — Convert the string to lowercase. Example 16.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mixed"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mixed"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00153 — Remove leading and trailing spaces. Example 16.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00154 — Return the string length. Example 16.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00155 — Append -DW. Example 16.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MiXeD-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00156 — Prefix with DW- Example 16.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MiXeD"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00157 — Check whether the string contains a. Example 16.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00158 — Check whether the string starts with D. Example 16.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00159 — Check whether the string ends with e. Example 16.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00160 — Replace a with @. Example 16.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00161 — Convert the string to uppercase. Example 17.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "CUSTOMER-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00162 — Convert the string to lowercase. Example 17.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00163 — Remove leading and trailing spaces. Example 17.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00164 — Return the string length. Example 17.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00165 — Append -DW. Example 17.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "customer-123-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00166 — Prefix with DW- Example 17.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-customer-123"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00167 — Check whether the string contains a. Example 17.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00168 — Check whether the string starts with D. Example 17.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00169 — Check whether the string ends with e. Example 17.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00170 — Replace a with @. Example 17.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00171 — Convert the string to uppercase. Example 18.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "API_RESPONSE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "API_RESPONSE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00172 — Convert the string to lowercase. Example 18.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00173 — Remove leading and trailing spaces. Example 18.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00174 — Return the string length. Example 18.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00175 — Append -DW. Example 18.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "api_response-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00176 — Prefix with DW- Example 18.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-api_response"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00177 — Check whether the string contains a. Example 18.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00178 — Check whether the string starts with D. Example 18.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00179 — Check whether the string ends with e. Example 18.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00180 — Replace a with @. Example 18.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "@pi_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "@pi_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00181 — Convert the string to uppercase. Example 19.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00182 — Convert the string to lowercase. Example 19.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00183 — Remove leading and trailing spaces. Example 19.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00184 — Return the string length. Example 19.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00185 — Append -DW. Example 19.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "Hyderabad-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00186 — Prefix with DW- Example 19.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-Hyderabad"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00187 — Check whether the string contains a. Example 19.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00188 — Check whether the string starts with D. Example 19.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00189 — Check whether the string ends with e. Example 19.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00190 — Replace a with @. Example 19.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "Hyder@b@d"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyder@b@d"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00191 — Convert the string to uppercase. Example 20.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "INTEGRATION"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "INTEGRATION"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00192 — Convert the string to lowercase. Example 20.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00193 — Remove leading and trailing spaces. Example 20.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00194 — Return the string length. Example 20.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00195 — Append -DW. Example 20.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "integration-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00196 — Prefix with DW- Example 20.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-integration"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00197 — Check whether the string contains a. Example 20.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00198 — Check whether the string starts with D. Example 20.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00199 — Check whether the string ends with e. Example 20.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00200 — Replace a with @. Example 20.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "integr@tion"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integr@tion"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00201 — Convert the string to uppercase. Example 21.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00202 — Convert the string to lowercase. Example 21.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00203 — Remove leading and trailing spaces. Example 21.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00204 — Return the string length. Example 21.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00205 — Append -DW. Example 21.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "narsing-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00206 — Prefix with DW- Example 21.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-narsing"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00207 — Check whether the string contains a. Example 21.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00208 — Check whether the string starts with D. Example 21.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00209 — Check whether the string ends with e. Example 21.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00210 — Replace a with @. Example 21.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "n@rsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "n@rsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00211 — Convert the string to uppercase. Example 22.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "DATAWEAVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DATAWEAVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00212 — Convert the string to lowercase. Example 22.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "dataweave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "dataweave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00213 — Remove leading and trailing spaces. Example 22.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "DataWeave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00214 — Return the string length. Example 22.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00215 — Append -DW. Example 22.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "DataWeave-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00216 — Prefix with DW- Example 22.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00217 — Check whether the string contains a. Example 22.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00218 — Check whether the string starts with D. Example 22.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00219 — Check whether the string ends with e. Example 22.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00220 — Replace a with @. Example 22.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "D@t@We@ve"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "D@t@We@ve"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00221 — Convert the string to uppercase. Example 23.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MULESOFT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MULESOFT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00222 — Convert the string to lowercase. Example 23.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mulesoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mulesoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00223 — Remove leading and trailing spaces. Example 23.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00224 — Return the string length. Example 23.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00225 — Append -DW. Example 23.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MuleSoft-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00226 — Prefix with DW- Example 23.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MuleSoft"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00227 — Check whether the string contains a. Example 23.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00228 — Check whether the string starts with D. Example 23.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00229 — Check whether the string ends with e. Example 23.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00230 — Replace a with @. Example 23.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00231 — Convert the string to uppercase. Example 24.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HELLO WORLD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HELLO WORLD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00232 — Convert the string to lowercase. Example 24.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00233 — Remove leading and trailing spaces. Example 24.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00234 — Return the string length. Example 24.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00235 — Append -DW. Example 24.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "hello world-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00236 — Prefix with DW- Example 24.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-hello world"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00237 — Check whether the string contains a. Example 24.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00238 — Check whether the string starts with D. Example 24.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00239 — Check whether the string ends with e. Example 24.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00240 — Replace a with @. Example 24.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00241 — Convert the string to uppercase. Example 25.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "  TRIM ME  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  TRIM ME  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00242 — Convert the string to lowercase. Example 25.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00243 — Remove leading and trailing spaces. Example 25.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "trim me"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "trim me"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00244 — Return the string length. Example 25.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00245 — Append -DW. Example 25.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "  trim me  -DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  -DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00246 — Prefix with DW- Example 25.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-  trim me  "}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00247 — Check whether the string contains a. Example 25.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00248 — Check whether the string starts with D. Example 25.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00249 — Check whether the string ends with e. Example 25.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00250 — Replace a with @. Example 25.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
