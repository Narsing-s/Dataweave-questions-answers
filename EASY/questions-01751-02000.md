# Easy DataWeave Questions 01751-02000

Questions 1751–2000 of 4000 in the Easy level.

## DW-01751 — Add two numbers. Example 76.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 76, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 81}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 81}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01752 — Subtract b from a. Example 76.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 76, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 71}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 71}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01753 — Multiply two numbers. Example 76.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 76, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 380}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 380}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01754 — Divide a by b. Example 76.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 76, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 15.2}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 15.2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01755 — Calculate the remainder. Example 76.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 76, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01756 — Compare the numbers. Example 76.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 76, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01757 — Classify a as HIGH or LOW. Example 76.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 76, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01758 — Apply an 18 percent multiplier. Example 76.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 76, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 89.67999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 89.67999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01759 — Round the division result. Example 76.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 76, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 15}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 15}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01760 — Check whether a is even. Example 76.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 76, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01761 — Add two numbers. Example 77.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 77, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 83}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 83}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01762 — Subtract b from a. Example 77.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 77, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 71}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 71}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01763 — Multiply two numbers. Example 77.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 77, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 462}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 462}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01764 — Divide a by b. Example 77.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 77, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 12.833333333333334}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12.833333333333334}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01765 — Calculate the remainder. Example 77.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 77, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01766 — Compare the numbers. Example 77.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 77, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01767 — Classify a as HIGH or LOW. Example 77.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 77, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01768 — Apply an 18 percent multiplier. Example 77.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 77, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 90.86}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 90.86}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01769 — Round the division result. Example 77.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 77, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 13}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01770 — Check whether a is even. Example 77.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 77, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01771 — Add two numbers. Example 78.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 78, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 85}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 85}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01772 — Subtract b from a. Example 78.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 78, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 71}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 71}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01773 — Multiply two numbers. Example 78.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 78, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 546}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 546}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01774 — Divide a by b. Example 78.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 78, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 11.142857142857142}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 11.142857142857142}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01775 — Calculate the remainder. Example 78.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 78, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01776 — Compare the numbers. Example 78.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 78, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01777 — Classify a as HIGH or LOW. Example 78.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 78, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01778 — Apply an 18 percent multiplier. Example 78.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 78, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 92.03999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 92.03999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01779 — Round the division result. Example 78.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 78, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 11}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01780 — Check whether a is even. Example 78.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 78, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01781 — Add two numbers. Example 79.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 79, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 87}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 87}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01782 — Subtract b from a. Example 79.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 79, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 71}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 71}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01783 — Multiply two numbers. Example 79.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 79, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 632}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 632}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01784 — Divide a by b. Example 79.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 79, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 9.875}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9.875}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01785 — Calculate the remainder. Example 79.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 79, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 7}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01786 — Compare the numbers. Example 79.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 79, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01787 — Classify a as HIGH or LOW. Example 79.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 79, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01788 — Apply an 18 percent multiplier. Example 79.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 79, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 93.22}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 93.22}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01789 — Round the division result. Example 79.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 79, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 10}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 10}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01790 — Check whether a is even. Example 79.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 79, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01791 — Add two numbers. Example 80.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 80, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 89}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 89}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01792 — Subtract b from a. Example 80.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 80, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 71}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 71}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01793 — Multiply two numbers. Example 80.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 80, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 720}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 720}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01794 — Divide a by b. Example 80.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 80, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 8.88888888888889}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8.88888888888889}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01795 — Calculate the remainder. Example 80.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 80, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01796 — Compare the numbers. Example 80.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 80, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01797 — Classify a as HIGH or LOW. Example 80.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 80, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01798 — Apply an 18 percent multiplier. Example 80.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 80, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 94.39999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 94.39999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01799 — Round the division result. Example 80.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 80, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 9}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01800 — Check whether a is even. Example 80.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 80, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01801 — Add two numbers. Example 81.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 81, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 91}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 91}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01802 — Subtract b from a. Example 81.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 81, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 71}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 71}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01803 — Multiply two numbers. Example 81.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 81, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 810}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 810}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01804 — Divide a by b. Example 81.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 81, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 8.1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8.1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01805 — Calculate the remainder. Example 81.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 81, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01806 — Compare the numbers. Example 81.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 81, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01807 — Classify a as HIGH or LOW. Example 81.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 81, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01808 — Apply an 18 percent multiplier. Example 81.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 81, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 95.58}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 95.58}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01809 — Round the division result. Example 81.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 81, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01810 — Check whether a is even. Example 81.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 81, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01811 — Add two numbers. Example 82.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 82, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 84}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 84}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01812 — Subtract b from a. Example 82.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 82, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 80}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 80}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01813 — Multiply two numbers. Example 82.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 82, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 164}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 164}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01814 — Divide a by b. Example 82.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 82, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 41.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 41.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01815 — Calculate the remainder. Example 82.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 82, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01816 — Compare the numbers. Example 82.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 82, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01817 — Classify a as HIGH or LOW. Example 82.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 82, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01818 — Apply an 18 percent multiplier. Example 82.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 82, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 96.75999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 96.75999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01819 — Round the division result. Example 82.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 82, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 41}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 41}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01820 — Check whether a is even. Example 82.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 82, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01821 — Add two numbers. Example 83.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 83, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 86}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 86}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01822 — Subtract b from a. Example 83.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 83, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 80}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 80}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01823 — Multiply two numbers. Example 83.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 83, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 249}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 249}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01824 — Divide a by b. Example 83.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 83, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 27.666666666666668}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 27.666666666666668}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01825 — Calculate the remainder. Example 83.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 83, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01826 — Compare the numbers. Example 83.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 83, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01827 — Classify a as HIGH or LOW. Example 83.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 83, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01828 — Apply an 18 percent multiplier. Example 83.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 83, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 97.94}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 97.94}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01829 — Round the division result. Example 83.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 83, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 28}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 28}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01830 — Check whether a is even. Example 83.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 83, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01831 — Add two numbers. Example 84.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 84, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 88}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 88}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01832 — Subtract b from a. Example 84.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 84, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 80}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 80}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01833 — Multiply two numbers. Example 84.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 84, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 336}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 336}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01834 — Divide a by b. Example 84.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 84, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 21.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 21.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01835 — Calculate the remainder. Example 84.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 84, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01836 — Compare the numbers. Example 84.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 84, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01837 — Classify a as HIGH or LOW. Example 84.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 84, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01838 — Apply an 18 percent multiplier. Example 84.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 84, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 99.11999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 99.11999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01839 — Round the division result. Example 84.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 84, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 21}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 21}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01840 — Check whether a is even. Example 84.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 84, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01841 — Add two numbers. Example 85.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 85, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 90}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 90}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01842 — Subtract b from a. Example 85.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 85, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 80}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 80}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01843 — Multiply two numbers. Example 85.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 85, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 425}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 425}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01844 — Divide a by b. Example 85.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 85, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 17.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01845 — Calculate the remainder. Example 85.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 85, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01846 — Compare the numbers. Example 85.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 85, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01847 — Classify a as HIGH or LOW. Example 85.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 85, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01848 — Apply an 18 percent multiplier. Example 85.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 85, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 100.3}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 100.3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01849 — Round the division result. Example 85.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 85, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 17}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01850 — Check whether a is even. Example 85.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 85, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01851 — Add two numbers. Example 86.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 86, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 92}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 92}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01852 — Subtract b from a. Example 86.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 86, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 80}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 80}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01853 — Multiply two numbers. Example 86.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 86, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 516}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 516}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01854 — Divide a by b. Example 86.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 86, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 14.333333333333334}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 14.333333333333334}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01855 — Calculate the remainder. Example 86.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 86, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01856 — Compare the numbers. Example 86.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 86, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01857 — Classify a as HIGH or LOW. Example 86.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 86, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01858 — Apply an 18 percent multiplier. Example 86.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 86, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 101.47999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 101.47999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01859 — Round the division result. Example 86.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 86, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 14}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 14}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01860 — Check whether a is even. Example 86.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 86, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01861 — Add two numbers. Example 87.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 87, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 94}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 94}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01862 — Subtract b from a. Example 87.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 87, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 80}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 80}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01863 — Multiply two numbers. Example 87.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 87, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 609}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 609}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01864 — Divide a by b. Example 87.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 87, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 12.428571428571429}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12.428571428571429}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01865 — Calculate the remainder. Example 87.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 87, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01866 — Compare the numbers. Example 87.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 87, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01867 — Classify a as HIGH or LOW. Example 87.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 87, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01868 — Apply an 18 percent multiplier. Example 87.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 87, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 102.66}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 102.66}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01869 — Round the division result. Example 87.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 87, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 12}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01870 — Check whether a is even. Example 87.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 87, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01871 — Add two numbers. Example 88.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 88, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 96}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 96}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01872 — Subtract b from a. Example 88.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 88, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 80}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 80}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01873 — Multiply two numbers. Example 88.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 88, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 704}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 704}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01874 — Divide a by b. Example 88.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 88, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 11.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 11.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01875 — Calculate the remainder. Example 88.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 88, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01876 — Compare the numbers. Example 88.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 88, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01877 — Classify a as HIGH or LOW. Example 88.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 88, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01878 — Apply an 18 percent multiplier. Example 88.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 88, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 103.83999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 103.83999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01879 — Round the division result. Example 88.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 88, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 11}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01880 — Check whether a is even. Example 88.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 88, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01881 — Add two numbers. Example 89.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 89, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 98}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 98}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01882 — Subtract b from a. Example 89.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 89, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 80}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 80}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01883 — Multiply two numbers. Example 89.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 89, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 801}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 801}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01884 — Divide a by b. Example 89.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 89, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 9.88888888888889}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9.88888888888889}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01885 — Calculate the remainder. Example 89.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 89, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01886 — Compare the numbers. Example 89.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 89, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01887 — Classify a as HIGH or LOW. Example 89.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 89, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01888 — Apply an 18 percent multiplier. Example 89.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 89, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 105.02}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 105.02}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01889 — Round the division result. Example 89.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 89, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 10}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 10}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01890 — Check whether a is even. Example 89.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 89, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01891 — Add two numbers. Example 90.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 90, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 100}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 100}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01892 — Subtract b from a. Example 90.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 90, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 80}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 80}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01893 — Multiply two numbers. Example 90.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 90, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 900}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 900}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01894 — Divide a by b. Example 90.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 90, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 9.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01895 — Calculate the remainder. Example 90.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 90, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01896 — Compare the numbers. Example 90.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 90, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01897 — Classify a as HIGH or LOW. Example 90.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 90, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01898 — Apply an 18 percent multiplier. Example 90.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 90, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 106.19999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 106.19999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01899 — Round the division result. Example 90.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 90, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 9}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01900 — Check whether a is even. Example 90.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 90, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01901 — Add two numbers. Example 91.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 91, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 93}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 93}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01902 — Subtract b from a. Example 91.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 91, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 89}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 89}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01903 — Multiply two numbers. Example 91.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 91, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 182}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 182}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01904 — Divide a by b. Example 91.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 91, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 45.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 45.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01905 — Calculate the remainder. Example 91.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 91, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01906 — Compare the numbers. Example 91.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 91, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01907 — Classify a as HIGH or LOW. Example 91.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 91, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01908 — Apply an 18 percent multiplier. Example 91.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 91, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 107.38}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 107.38}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01909 — Round the division result. Example 91.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 91, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 46}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 46}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01910 — Check whether a is even. Example 91.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 91, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01911 — Add two numbers. Example 92.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 92, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 95}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 95}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01912 — Subtract b from a. Example 92.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 92, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 89}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 89}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01913 — Multiply two numbers. Example 92.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 92, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 276}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 276}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01914 — Divide a by b. Example 92.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 92, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 30.666666666666668}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 30.666666666666668}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01915 — Calculate the remainder. Example 92.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 92, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01916 — Compare the numbers. Example 92.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 92, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01917 — Classify a as HIGH or LOW. Example 92.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 92, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01918 — Apply an 18 percent multiplier. Example 92.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 92, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 108.55999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 108.55999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01919 — Round the division result. Example 92.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 92, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 31}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 31}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01920 — Check whether a is even. Example 92.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 92, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01921 — Add two numbers. Example 93.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 93, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 97}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 97}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01922 — Subtract b from a. Example 93.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 93, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 89}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 89}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01923 — Multiply two numbers. Example 93.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 93, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 372}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 372}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01924 — Divide a by b. Example 93.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 93, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 23.25}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 23.25}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01925 — Calculate the remainder. Example 93.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 93, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01926 — Compare the numbers. Example 93.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 93, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01927 — Classify a as HIGH or LOW. Example 93.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 93, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01928 — Apply an 18 percent multiplier. Example 93.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 93, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 109.74}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 109.74}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01929 — Round the division result. Example 93.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 93, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 23}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 23}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01930 — Check whether a is even. Example 93.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 93, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01931 — Add two numbers. Example 94.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 94, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 99}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 99}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01932 — Subtract b from a. Example 94.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 94, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 89}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 89}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01933 — Multiply two numbers. Example 94.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 94, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 470}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 470}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01934 — Divide a by b. Example 94.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 94, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 18.8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 18.8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01935 — Calculate the remainder. Example 94.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 94, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01936 — Compare the numbers. Example 94.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 94, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01937 — Classify a as HIGH or LOW. Example 94.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 94, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01938 — Apply an 18 percent multiplier. Example 94.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 94, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 110.91999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 110.91999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01939 — Round the division result. Example 94.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 94, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 19}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 19}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01940 — Check whether a is even. Example 94.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 94, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01941 — Add two numbers. Example 95.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 95, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 101}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 101}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01942 — Subtract b from a. Example 95.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 95, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 89}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 89}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01943 — Multiply two numbers. Example 95.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 95, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 570}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 570}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01944 — Divide a by b. Example 95.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 95, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 15.833333333333334}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 15.833333333333334}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01945 — Calculate the remainder. Example 95.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 95, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01946 — Compare the numbers. Example 95.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 95, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01947 — Classify a as HIGH or LOW. Example 95.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 95, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01948 — Apply an 18 percent multiplier. Example 95.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 95, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 112.1}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 112.1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01949 — Round the division result. Example 95.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 95, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 16}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 16}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01950 — Check whether a is even. Example 95.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 95, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01951 — Add two numbers. Example 96.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 96, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 103}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 103}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01952 — Subtract b from a. Example 96.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 96, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 89}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 89}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01953 — Multiply two numbers. Example 96.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 96, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 672}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 672}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01954 — Divide a by b. Example 96.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 96, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 13.714285714285714}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 13.714285714285714}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01955 — Calculate the remainder. Example 96.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 96, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01956 — Compare the numbers. Example 96.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 96, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01957 — Classify a as HIGH or LOW. Example 96.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 96, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01958 — Apply an 18 percent multiplier. Example 96.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 96, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 113.28}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 113.28}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01959 — Round the division result. Example 96.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 96, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 14}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 14}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01960 — Check whether a is even. Example 96.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 96, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01961 — Add two numbers. Example 97.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 97, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 105}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 105}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01962 — Subtract b from a. Example 97.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 97, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 89}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 89}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01963 — Multiply two numbers. Example 97.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 97, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 776}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 776}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01964 — Divide a by b. Example 97.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 97, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 12.125}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12.125}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01965 — Calculate the remainder. Example 97.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 97, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01966 — Compare the numbers. Example 97.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 97, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01967 — Classify a as HIGH or LOW. Example 97.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 97, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01968 — Apply an 18 percent multiplier. Example 97.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 97, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 114.46}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 114.46}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01969 — Round the division result. Example 97.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 97, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 12}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01970 — Check whether a is even. Example 97.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 97, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01971 — Add two numbers. Example 98.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 98, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 107}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 107}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01972 — Subtract b from a. Example 98.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 98, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 89}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 89}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01973 — Multiply two numbers. Example 98.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 98, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 882}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 882}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01974 — Divide a by b. Example 98.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 98, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 10.88888888888889}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 10.88888888888889}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01975 — Calculate the remainder. Example 98.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 98, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01976 — Compare the numbers. Example 98.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 98, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01977 — Classify a as HIGH or LOW. Example 98.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 98, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01978 — Apply an 18 percent multiplier. Example 98.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 98, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 115.64}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 115.64}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01979 — Round the division result. Example 98.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 98, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 11}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01980 — Check whether a is even. Example 98.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 98, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01981 — Add two numbers. Example 99.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 99, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 109}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 109}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01982 — Subtract b from a. Example 99.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 99, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 89}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 89}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01983 — Multiply two numbers. Example 99.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 99, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 990}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 990}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01984 — Divide a by b. Example 99.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 99, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 9.9}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9.9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01985 — Calculate the remainder. Example 99.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 99, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 9}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01986 — Compare the numbers. Example 99.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 99, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01987 — Classify a as HIGH or LOW. Example 99.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 99, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01988 — Apply an 18 percent multiplier. Example 99.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 99, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 116.82}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 116.82}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01989 — Round the division result. Example 99.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 99, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 10}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 10}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01990 — Check whether a is even. Example 99.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 99, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01991 — Add two numbers. Example 100.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 100, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 102}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 102}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01992 — Subtract b from a. Example 100.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 100, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 98}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 98}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01993 — Multiply two numbers. Example 100.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 100, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 200}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 200}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01994 — Divide a by b. Example 100.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 100, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 50.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 50.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01995 — Calculate the remainder. Example 100.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 100, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01996 — Compare the numbers. Example 100.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 100, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01997 — Classify a as HIGH or LOW. Example 100.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 100, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01998 — Apply an 18 percent multiplier. Example 100.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 100, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 118.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 118.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01999 — Round the division result. Example 100.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 100, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 50}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 50}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02000 — Check whether a is even. Example 100.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 100, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
