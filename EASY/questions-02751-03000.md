# Easy DataWeave Questions 02751-03000

Questions 2751–3000 of 4000 in the Easy level.

## DW-02751 — Double every item. Example 76.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[-4, -2, 0, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-4, -2, 0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02752 — Increment every item. Example 76.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[-1, 0, 1, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-1, 0, 1, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02753 — Keep values greater than five. Example 76.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02754 — Keep even values. Example 76.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[-2, 0]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [-2, 0].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02755 — Count items. Example 76.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02756 — Sum values. Example 76.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02757 — Append 99. Example 76.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[-2, -1, 0, 1, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [-2, -1, 0, 1, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02758 — Prepend 0. Example 76.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, -2, -1, 0, 1]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, -2, -1, 0, 1].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02759 — Return the first item. Example 76.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02760 — Return the last item. Example 76.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02761 — Double every item. Example 77.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[6, 12, 18]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 12, 18].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02762 — Increment every item. Example 77.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[4, 7, 10]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 7, 10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02763 — Keep values greater than five. Example 77.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 9]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02764 — Keep even values. Example 77.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[6]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02765 — Count items. Example 77.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02766 — Sum values. Example 77.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
18
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 18.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02767 — Append 99. Example 77.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[3, 6, 9, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [3, 6, 9, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02768 — Prepend 0. Example 77.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 3, 6, 9]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 3, 6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02769 — Return the first item. Example 77.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02770 — Return the last item. Example 77.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
9
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 9.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02771 — Double every item. Example 78.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[200, 400, 600]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [200, 400, 600].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02772 — Increment every item. Example 78.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[101, 201, 301]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [101, 201, 301].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02773 — Keep values greater than five. Example 78.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02774 — Keep even values. Example 78.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02775 — Count items. Example 78.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02776 — Sum values. Example 78.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
600
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 600.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02777 — Append 99. Example 78.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[100, 200, 300, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02778 — Prepend 0. Example 78.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 100, 200, 300]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02779 — Return the first item. Example 78.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
100
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 100.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02780 — Return the last item. Example 78.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
300
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 300.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02781 — Double every item. Example 79.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 10, 20, 40]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 10, 20, 40].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02782 — Increment every item. Example 79.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 6, 11, 21]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 6, 11, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02783 — Keep values greater than five. Example 79.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02784 — Keep even values. Example 79.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02785 — Count items. Example 79.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02786 — Sum values. Example 79.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
36
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 36.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02787 — Append 99. Example 79.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 5, 10, 20, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 5, 10, 20, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02788 — Prepend 0. Example 79.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 5, 10, 20]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 5, 10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02789 — Return the first item. Example 79.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02790 — Return the last item. Example 79.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02791 — Double every item. Example 80.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[14, 28, 42]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [14, 28, 42].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02792 — Increment every item. Example 80.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[8, 15, 22]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [8, 15, 22].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02793 — Keep values greater than five. Example 80.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[7, 14, 21]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02794 — Keep even values. Example 80.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[14]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [14].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02795 — Count items. Example 80.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02796 — Sum values. Example 80.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
42
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 42.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02797 — Append 99. Example 80.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[7, 14, 21, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02798 — Prepend 0. Example 80.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 7, 14, 21]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02799 — Return the first item. Example 80.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
7
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 7.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02800 — Return the last item. Example 80.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
21
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 21.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02801 — Double every item. Example 81.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02802 — Increment every item. Example 81.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02803 — Keep values greater than five. Example 81.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02804 — Keep even values. Example 81.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02805 — Count items. Example 81.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02806 — Sum values. Example 81.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02807 — Append 99. Example 81.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02808 — Prepend 0. Example 81.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02809 — Return the first item. Example 81.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02810 — Return the last item. Example 81.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02811 — Double every item. Example 82.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02812 — Increment every item. Example 82.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[6, 11, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 11, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02813 — Keep values greater than five. Example 82.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 15]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02814 — Keep even values. Example 82.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02815 — Count items. Example 82.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02816 — Sum values. Example 82.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02817 — Append 99. Example 82.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[5, 10, 15, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [5, 10, 15, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02818 — Prepend 0. Example 82.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 5, 10, 15]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 5, 10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02819 — Return the first item. Example 82.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
5
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 5.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02820 — Return the last item. Example 82.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
15
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 15.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02821 — Double every item. Example 83.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[0, 2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02822 — Increment every item. Example 83.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[1, 2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02823 — Keep values greater than five. Example 83.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02824 — Keep even values. Example 83.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[0, 2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02825 — Count items. Example 83.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02826 — Sum values. Example 83.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02827 — Append 99. Example 83.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[0, 1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02828 — Prepend 0. Example 83.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02829 — Return the first item. Example 83.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
0
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 0.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02830 — Return the last item. Example 83.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02831 — Double every item. Example 84.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[4, 8, 12, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 8, 12, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02832 — Increment every item. Example 84.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[3, 5, 7, 9]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [3, 5, 7, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02833 — Keep values greater than five. Example 84.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02834 — Keep even values. Example 84.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2, 4, 6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02835 — Count items. Example 84.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02836 — Sum values. Example 84.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02837 — Append 99. Example 84.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[2, 4, 6, 8, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02838 — Prepend 0. Example 84.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 2, 4, 6, 8]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02839 — Return the first item. Example 84.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02840 — Return the last item. Example 84.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
8
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 8.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02841 — Double every item. Example 85.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[20, 40, 60]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [20, 40, 60].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02842 — Increment every item. Example 85.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[11, 21, 31]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [11, 21, 31].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02843 — Keep values greater than five. Example 85.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02844 — Keep even values. Example 85.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02845 — Count items. Example 85.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02846 — Sum values. Example 85.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
60
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 60.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02847 — Append 99. Example 85.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[10, 20, 30, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02848 — Prepend 0. Example 85.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 10, 20, 30]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02849 — Return the first item. Example 85.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
10
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 10.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02850 — Return the last item. Example 85.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02851 — Double every item. Example 86.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[-4, -2, 0, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-4, -2, 0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02852 — Increment every item. Example 86.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[-1, 0, 1, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-1, 0, 1, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02853 — Keep values greater than five. Example 86.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02854 — Keep even values. Example 86.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[-2, 0]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [-2, 0].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02855 — Count items. Example 86.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02856 — Sum values. Example 86.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02857 — Append 99. Example 86.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[-2, -1, 0, 1, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [-2, -1, 0, 1, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02858 — Prepend 0. Example 86.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, -2, -1, 0, 1]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, -2, -1, 0, 1].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02859 — Return the first item. Example 86.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02860 — Return the last item. Example 86.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02861 — Double every item. Example 87.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[6, 12, 18]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 12, 18].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02862 — Increment every item. Example 87.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[4, 7, 10]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 7, 10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02863 — Keep values greater than five. Example 87.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 9]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02864 — Keep even values. Example 87.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[6]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02865 — Count items. Example 87.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02866 — Sum values. Example 87.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
18
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 18.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02867 — Append 99. Example 87.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[3, 6, 9, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [3, 6, 9, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02868 — Prepend 0. Example 87.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 3, 6, 9]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 3, 6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02869 — Return the first item. Example 87.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02870 — Return the last item. Example 87.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
9
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 9.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02871 — Double every item. Example 88.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[200, 400, 600]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [200, 400, 600].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02872 — Increment every item. Example 88.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[101, 201, 301]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [101, 201, 301].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02873 — Keep values greater than five. Example 88.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02874 — Keep even values. Example 88.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02875 — Count items. Example 88.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02876 — Sum values. Example 88.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
600
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 600.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02877 — Append 99. Example 88.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[100, 200, 300, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02878 — Prepend 0. Example 88.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 100, 200, 300]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02879 — Return the first item. Example 88.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
100
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 100.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02880 — Return the last item. Example 88.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
300
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 300.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02881 — Double every item. Example 89.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 10, 20, 40]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 10, 20, 40].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02882 — Increment every item. Example 89.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 6, 11, 21]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 6, 11, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02883 — Keep values greater than five. Example 89.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02884 — Keep even values. Example 89.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02885 — Count items. Example 89.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02886 — Sum values. Example 89.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
36
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 36.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02887 — Append 99. Example 89.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 5, 10, 20, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 5, 10, 20, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02888 — Prepend 0. Example 89.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 5, 10, 20]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 5, 10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02889 — Return the first item. Example 89.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02890 — Return the last item. Example 89.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02891 — Double every item. Example 90.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[14, 28, 42]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [14, 28, 42].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02892 — Increment every item. Example 90.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[8, 15, 22]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [8, 15, 22].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02893 — Keep values greater than five. Example 90.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[7, 14, 21]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02894 — Keep even values. Example 90.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[14]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [14].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02895 — Count items. Example 90.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02896 — Sum values. Example 90.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
42
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 42.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02897 — Append 99. Example 90.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[7, 14, 21, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02898 — Prepend 0. Example 90.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 7, 14, 21]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02899 — Return the first item. Example 90.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
7
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 7.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02900 — Return the last item. Example 90.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
21
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 21.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02901 — Double every item. Example 91.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02902 — Increment every item. Example 91.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02903 — Keep values greater than five. Example 91.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02904 — Keep even values. Example 91.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02905 — Count items. Example 91.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02906 — Sum values. Example 91.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02907 — Append 99. Example 91.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02908 — Prepend 0. Example 91.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02909 — Return the first item. Example 91.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02910 — Return the last item. Example 91.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02911 — Double every item. Example 92.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02912 — Increment every item. Example 92.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[6, 11, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 11, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02913 — Keep values greater than five. Example 92.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 15]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02914 — Keep even values. Example 92.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02915 — Count items. Example 92.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02916 — Sum values. Example 92.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02917 — Append 99. Example 92.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[5, 10, 15, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [5, 10, 15, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02918 — Prepend 0. Example 92.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 5, 10, 15]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 5, 10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02919 — Return the first item. Example 92.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
5
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 5.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02920 — Return the last item. Example 92.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
15
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 15.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02921 — Double every item. Example 93.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[0, 2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02922 — Increment every item. Example 93.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[1, 2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02923 — Keep values greater than five. Example 93.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02924 — Keep even values. Example 93.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[0, 2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02925 — Count items. Example 93.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02926 — Sum values. Example 93.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02927 — Append 99. Example 93.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[0, 1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02928 — Prepend 0. Example 93.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02929 — Return the first item. Example 93.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
0
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 0.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02930 — Return the last item. Example 93.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02931 — Double every item. Example 94.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[4, 8, 12, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 8, 12, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02932 — Increment every item. Example 94.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[3, 5, 7, 9]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [3, 5, 7, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02933 — Keep values greater than five. Example 94.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02934 — Keep even values. Example 94.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2, 4, 6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02935 — Count items. Example 94.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02936 — Sum values. Example 94.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02937 — Append 99. Example 94.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[2, 4, 6, 8, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02938 — Prepend 0. Example 94.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 2, 4, 6, 8]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02939 — Return the first item. Example 94.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02940 — Return the last item. Example 94.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
8
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 8.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02941 — Double every item. Example 95.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[20, 40, 60]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [20, 40, 60].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02942 — Increment every item. Example 95.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[11, 21, 31]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [11, 21, 31].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02943 — Keep values greater than five. Example 95.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02944 — Keep even values. Example 95.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02945 — Count items. Example 95.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02946 — Sum values. Example 95.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
60
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 60.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02947 — Append 99. Example 95.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[10, 20, 30, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02948 — Prepend 0. Example 95.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 10, 20, 30]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02949 — Return the first item. Example 95.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
10
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 10.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02950 — Return the last item. Example 95.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02951 — Double every item. Example 96.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[-4, -2, 0, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-4, -2, 0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02952 — Increment every item. Example 96.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[-1, 0, 1, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-1, 0, 1, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02953 — Keep values greater than five. Example 96.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02954 — Keep even values. Example 96.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[-2, 0]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [-2, 0].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02955 — Count items. Example 96.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02956 — Sum values. Example 96.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02957 — Append 99. Example 96.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[-2, -1, 0, 1, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [-2, -1, 0, 1, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02958 — Prepend 0. Example 96.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, -2, -1, 0, 1]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, -2, -1, 0, 1].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02959 — Return the first item. Example 96.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02960 — Return the last item. Example 96.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02961 — Double every item. Example 97.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[6, 12, 18]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 12, 18].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02962 — Increment every item. Example 97.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[4, 7, 10]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 7, 10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02963 — Keep values greater than five. Example 97.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 9]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02964 — Keep even values. Example 97.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[6]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02965 — Count items. Example 97.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02966 — Sum values. Example 97.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
18
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 18.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02967 — Append 99. Example 97.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[3, 6, 9, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [3, 6, 9, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02968 — Prepend 0. Example 97.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 3, 6, 9]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 3, 6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02969 — Return the first item. Example 97.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02970 — Return the last item. Example 97.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
9
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 9.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02971 — Double every item. Example 98.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[200, 400, 600]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [200, 400, 600].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02972 — Increment every item. Example 98.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[101, 201, 301]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [101, 201, 301].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02973 — Keep values greater than five. Example 98.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02974 — Keep even values. Example 98.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02975 — Count items. Example 98.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02976 — Sum values. Example 98.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
600
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 600.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02977 — Append 99. Example 98.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[100, 200, 300, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02978 — Prepend 0. Example 98.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 100, 200, 300]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02979 — Return the first item. Example 98.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
100
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 100.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02980 — Return the last item. Example 98.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
300
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 300.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02981 — Double every item. Example 99.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 10, 20, 40]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 10, 20, 40].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02982 — Increment every item. Example 99.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 6, 11, 21]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 6, 11, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02983 — Keep values greater than five. Example 99.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02984 — Keep even values. Example 99.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02985 — Count items. Example 99.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02986 — Sum values. Example 99.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
36
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 36.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02987 — Append 99. Example 99.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 5, 10, 20, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 5, 10, 20, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02988 — Prepend 0. Example 99.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 5, 10, 20]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 5, 10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02989 — Return the first item. Example 99.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02990 — Return the last item. Example 99.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02991 — Double every item. Example 100.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[14, 28, 42]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [14, 28, 42].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02992 — Increment every item. Example 100.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[8, 15, 22]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [8, 15, 22].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02993 — Keep values greater than five. Example 100.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[7, 14, 21]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02994 — Keep even values. Example 100.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[14]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [14].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02995 — Count items. Example 100.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02996 — Sum values. Example 100.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
42
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 42.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02997 — Append 99. Example 100.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[7, 14, 21, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02998 — Prepend 0. Example 100.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 7, 14, 21]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02999 — Return the first item. Example 100.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
7
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 7.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03000 — Return the last item. Example 100.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
21
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 21.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
