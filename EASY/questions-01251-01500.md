# Easy DataWeave Questions 01251-01500

Questions 1251–1500 of 4000 in the Easy level.

## DW-01251 — Add two numbers. Example 26.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 26, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 35}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 35}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01252 — Subtract b from a. Example 26.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 26, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 17}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01253 — Multiply two numbers. Example 26.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 26, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 234}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 234}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01254 — Divide a by b. Example 26.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 26, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 2.888888888888889}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2.888888888888889}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01255 — Calculate the remainder. Example 26.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 26, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01256 — Compare the numbers. Example 26.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 26, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01257 — Classify a as HIGH or LOW. Example 26.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 26, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01258 — Apply an 18 percent multiplier. Example 26.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 26, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 30.68}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 30.68}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01259 — Round the division result. Example 26.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 26, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01260 — Check whether a is even. Example 26.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 26, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01261 — Add two numbers. Example 27.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 27, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 37}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 37}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01262 — Subtract b from a. Example 27.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 27, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 17}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 17}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01263 — Multiply two numbers. Example 27.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 27, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 270}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 270}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01264 — Divide a by b. Example 27.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 27, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 2.7}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2.7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01265 — Calculate the remainder. Example 27.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 27, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 7}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01266 — Compare the numbers. Example 27.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 27, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01267 — Classify a as HIGH or LOW. Example 27.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 27, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01268 — Apply an 18 percent multiplier. Example 27.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 27, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 31.86}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 31.86}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01269 — Round the division result. Example 27.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 27, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01270 — Check whether a is even. Example 27.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 27, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01271 — Add two numbers. Example 28.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 28, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 30}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 30}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01272 — Subtract b from a. Example 28.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 28, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 26}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 26}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01273 — Multiply two numbers. Example 28.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 28, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 56}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 56}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01274 — Divide a by b. Example 28.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 28, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 14.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 14.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01275 — Calculate the remainder. Example 28.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 28, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01276 — Compare the numbers. Example 28.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 28, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01277 — Classify a as HIGH or LOW. Example 28.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 28, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01278 — Apply an 18 percent multiplier. Example 28.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 28, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 33.04}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 33.04}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01279 — Round the division result. Example 28.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 28, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 14}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 14}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01280 — Check whether a is even. Example 28.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 28, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01281 — Add two numbers. Example 29.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 29, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 32}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 32}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01282 — Subtract b from a. Example 29.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 29, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 26}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 26}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01283 — Multiply two numbers. Example 29.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 29, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 87}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 87}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01284 — Divide a by b. Example 29.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 29, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 9.666666666666666}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9.666666666666666}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01285 — Calculate the remainder. Example 29.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 29, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01286 — Compare the numbers. Example 29.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 29, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01287 — Classify a as HIGH or LOW. Example 29.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 29, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01288 — Apply an 18 percent multiplier. Example 29.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 29, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 34.22}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 34.22}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01289 — Round the division result. Example 29.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 29, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 10}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 10}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01290 — Check whether a is even. Example 29.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 29, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01291 — Add two numbers. Example 30.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 30, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 34}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 34}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01292 — Subtract b from a. Example 30.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 30, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 26}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 26}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01293 — Multiply two numbers. Example 30.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 30, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 120}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 120}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01294 — Divide a by b. Example 30.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 30, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 7.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01295 — Calculate the remainder. Example 30.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 30, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01296 — Compare the numbers. Example 30.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 30, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01297 — Classify a as HIGH or LOW. Example 30.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 30, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01298 — Apply an 18 percent multiplier. Example 30.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 30, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 35.4}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 35.4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01299 — Round the division result. Example 30.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 30, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01300 — Check whether a is even. Example 30.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 30, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01301 — Add two numbers. Example 31.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 31, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 36}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 36}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01302 — Subtract b from a. Example 31.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 31, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 26}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 26}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01303 — Multiply two numbers. Example 31.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 31, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 155}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 155}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01304 — Divide a by b. Example 31.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 31, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 6.2}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6.2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01305 — Calculate the remainder. Example 31.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 31, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01306 — Compare the numbers. Example 31.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 31, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01307 — Classify a as HIGH or LOW. Example 31.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 31, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01308 — Apply an 18 percent multiplier. Example 31.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 31, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 36.58}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 36.58}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01309 — Round the division result. Example 31.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 31, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 6}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01310 — Check whether a is even. Example 31.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 31, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01311 — Add two numbers. Example 32.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 32, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 38}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 38}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01312 — Subtract b from a. Example 32.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 32, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 26}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 26}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01313 — Multiply two numbers. Example 32.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 32, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 192}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 192}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01314 — Divide a by b. Example 32.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 32, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 5.333333333333333}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5.333333333333333}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01315 — Calculate the remainder. Example 32.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 32, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01316 — Compare the numbers. Example 32.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 32, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01317 — Classify a as HIGH or LOW. Example 32.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 32, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01318 — Apply an 18 percent multiplier. Example 32.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 32, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 37.76}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 37.76}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01319 — Round the division result. Example 32.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 32, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01320 — Check whether a is even. Example 32.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 32, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01321 — Add two numbers. Example 33.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 33, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 40}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 40}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01322 — Subtract b from a. Example 33.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 33, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 26}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 26}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01323 — Multiply two numbers. Example 33.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 33, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 231}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 231}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01324 — Divide a by b. Example 33.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 33, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 4.714285714285714}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4.714285714285714}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01325 — Calculate the remainder. Example 33.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 33, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01326 — Compare the numbers. Example 33.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 33, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01327 — Classify a as HIGH or LOW. Example 33.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 33, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01328 — Apply an 18 percent multiplier. Example 33.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 33, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 38.94}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 38.94}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01329 — Round the division result. Example 33.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 33, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01330 — Check whether a is even. Example 33.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 33, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01331 — Add two numbers. Example 34.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 34, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 42}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 42}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01332 — Subtract b from a. Example 34.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 34, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 26}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 26}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01333 — Multiply two numbers. Example 34.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 34, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 272}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 272}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01334 — Divide a by b. Example 34.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 34, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 4.25}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4.25}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01335 — Calculate the remainder. Example 34.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 34, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01336 — Compare the numbers. Example 34.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 34, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01337 — Classify a as HIGH or LOW. Example 34.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 34, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01338 — Apply an 18 percent multiplier. Example 34.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 34, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 40.12}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 40.12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01339 — Round the division result. Example 34.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 34, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01340 — Check whether a is even. Example 34.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 34, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01341 — Add two numbers. Example 35.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 35, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01342 — Subtract b from a. Example 35.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 35, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 26}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 26}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01343 — Multiply two numbers. Example 35.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 35, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 315}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 315}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01344 — Divide a by b. Example 35.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 35, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 3.888888888888889}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3.888888888888889}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01345 — Calculate the remainder. Example 35.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 35, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01346 — Compare the numbers. Example 35.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 35, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01347 — Classify a as HIGH or LOW. Example 35.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 35, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01348 — Apply an 18 percent multiplier. Example 35.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 35, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 41.3}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 41.3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01349 — Round the division result. Example 35.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 35, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01350 — Check whether a is even. Example 35.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 35, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01351 — Add two numbers. Example 36.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 36, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 46}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 46}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01352 — Subtract b from a. Example 36.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 36, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 26}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 26}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01353 — Multiply two numbers. Example 36.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 36, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 360}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 360}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01354 — Divide a by b. Example 36.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 36, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 3.6}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3.6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01355 — Calculate the remainder. Example 36.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 36, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 6}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01356 — Compare the numbers. Example 36.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 36, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01357 — Classify a as HIGH or LOW. Example 36.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 36, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01358 — Apply an 18 percent multiplier. Example 36.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 36, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 42.48}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 42.48}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01359 — Round the division result. Example 36.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 36, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01360 — Check whether a is even. Example 36.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 36, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01361 — Add two numbers. Example 37.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 37, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 39}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 39}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01362 — Subtract b from a. Example 37.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 37, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 35}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 35}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01363 — Multiply two numbers. Example 37.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 37, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 74}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 74}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01364 — Divide a by b. Example 37.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 37, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 18.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 18.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01365 — Calculate the remainder. Example 37.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 37, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01366 — Compare the numbers. Example 37.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 37, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01367 — Classify a as HIGH or LOW. Example 37.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 37, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01368 — Apply an 18 percent multiplier. Example 37.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 37, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 43.66}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 43.66}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01369 — Round the division result. Example 37.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 37, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 18}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 18}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01370 — Check whether a is even. Example 37.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 37, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01371 — Add two numbers. Example 38.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 38, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 41}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 41}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01372 — Subtract b from a. Example 38.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 38, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 35}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 35}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01373 — Multiply two numbers. Example 38.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 38, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 114}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 114}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01374 — Divide a by b. Example 38.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 38, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 12.666666666666666}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12.666666666666666}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01375 — Calculate the remainder. Example 38.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 38, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01376 — Compare the numbers. Example 38.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 38, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01377 — Classify a as HIGH or LOW. Example 38.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 38, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01378 — Apply an 18 percent multiplier. Example 38.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 38, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 44.839999999999996}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 44.839999999999996}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01379 — Round the division result. Example 38.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 38, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 13}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01380 — Check whether a is even. Example 38.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 38, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01381 — Add two numbers. Example 39.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 39, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 43}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 43}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01382 — Subtract b from a. Example 39.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 39, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 35}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 35}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01383 — Multiply two numbers. Example 39.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 39, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 156}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 156}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01384 — Divide a by b. Example 39.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 39, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 9.75}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9.75}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01385 — Calculate the remainder. Example 39.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 39, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01386 — Compare the numbers. Example 39.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 39, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01387 — Classify a as HIGH or LOW. Example 39.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 39, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01388 — Apply an 18 percent multiplier. Example 39.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 39, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 46.019999999999996}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 46.019999999999996}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01389 — Round the division result. Example 39.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 39, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 10}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 10}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01390 — Check whether a is even. Example 39.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 39, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01391 — Add two numbers. Example 40.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 40, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 45}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 45}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01392 — Subtract b from a. Example 40.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 40, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 35}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 35}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01393 — Multiply two numbers. Example 40.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 40, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 200}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 200}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01394 — Divide a by b. Example 40.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 40, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 8.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01395 — Calculate the remainder. Example 40.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 40, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01396 — Compare the numbers. Example 40.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 40, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01397 — Classify a as HIGH or LOW. Example 40.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 40, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01398 — Apply an 18 percent multiplier. Example 40.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 40, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 47.199999999999996}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 47.199999999999996}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01399 — Round the division result. Example 40.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 40, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01400 — Check whether a is even. Example 40.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 40, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01401 — Add two numbers. Example 41.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 41, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 47}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 47}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01402 — Subtract b from a. Example 41.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 41, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 35}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 35}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01403 — Multiply two numbers. Example 41.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 41, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 246}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 246}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01404 — Divide a by b. Example 41.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 41, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 6.833333333333333}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6.833333333333333}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01405 — Calculate the remainder. Example 41.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 41, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01406 — Compare the numbers. Example 41.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 41, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01407 — Classify a as HIGH or LOW. Example 41.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 41, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01408 — Apply an 18 percent multiplier. Example 41.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 41, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 48.379999999999995}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 48.379999999999995}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01409 — Round the division result. Example 41.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 41, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 7}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01410 — Check whether a is even. Example 41.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 41, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01411 — Add two numbers. Example 42.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 42, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 49}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 49}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01412 — Subtract b from a. Example 42.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 42, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 35}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 35}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01413 — Multiply two numbers. Example 42.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 42, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 294}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 294}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01414 — Divide a by b. Example 42.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 42, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 6.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01415 — Calculate the remainder. Example 42.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 42, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01416 — Compare the numbers. Example 42.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 42, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01417 — Classify a as HIGH or LOW. Example 42.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 42, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01418 — Apply an 18 percent multiplier. Example 42.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 42, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 49.559999999999995}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 49.559999999999995}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01419 — Round the division result. Example 42.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 42, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 6}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01420 — Check whether a is even. Example 42.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 42, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01421 — Add two numbers. Example 43.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 43, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 51}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 51}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01422 — Subtract b from a. Example 43.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 43, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 35}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 35}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01423 — Multiply two numbers. Example 43.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 43, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 344}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 344}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01424 — Divide a by b. Example 43.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 43, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 5.375}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5.375}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01425 — Calculate the remainder. Example 43.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 43, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01426 — Compare the numbers. Example 43.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 43, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01427 — Classify a as HIGH or LOW. Example 43.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 43, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01428 — Apply an 18 percent multiplier. Example 43.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 43, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 50.739999999999995}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 50.739999999999995}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01429 — Round the division result. Example 43.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 43, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01430 — Check whether a is even. Example 43.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 43, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01431 — Add two numbers. Example 44.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 44, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 53}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 53}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01432 — Subtract b from a. Example 44.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 44, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 35}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 35}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01433 — Multiply two numbers. Example 44.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 44, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 396}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 396}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01434 — Divide a by b. Example 44.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 44, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 4.888888888888889}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4.888888888888889}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01435 — Calculate the remainder. Example 44.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 44, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01436 — Compare the numbers. Example 44.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 44, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01437 — Classify a as HIGH or LOW. Example 44.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 44, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01438 — Apply an 18 percent multiplier. Example 44.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 44, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 51.919999999999995}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 51.919999999999995}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01439 — Round the division result. Example 44.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 44, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01440 — Check whether a is even. Example 44.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 44, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01441 — Add two numbers. Example 45.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 45, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 55}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 55}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01442 — Subtract b from a. Example 45.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 45, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 35}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 35}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01443 — Multiply two numbers. Example 45.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 45, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 450}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 450}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01444 — Divide a by b. Example 45.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 45, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 4.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01445 — Calculate the remainder. Example 45.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 45, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01446 — Compare the numbers. Example 45.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 45, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01447 — Classify a as HIGH or LOW. Example 45.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 45, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01448 — Apply an 18 percent multiplier. Example 45.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 45, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 53.099999999999994}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 53.099999999999994}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01449 — Round the division result. Example 45.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 45, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01450 — Check whether a is even. Example 45.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 45, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01451 — Add two numbers. Example 46.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 46, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 48}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 48}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01452 — Subtract b from a. Example 46.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 46, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01453 — Multiply two numbers. Example 46.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 46, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 92}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 92}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01454 — Divide a by b. Example 46.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 46, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 23.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 23.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01455 — Calculate the remainder. Example 46.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 46, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01456 — Compare the numbers. Example 46.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 46, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01457 — Classify a as HIGH or LOW. Example 46.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 46, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01458 — Apply an 18 percent multiplier. Example 46.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 46, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 54.279999999999994}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 54.279999999999994}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01459 — Round the division result. Example 46.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 46, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 23}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 23}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01460 — Check whether a is even. Example 46.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 46, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01461 — Add two numbers. Example 47.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 47, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 50}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 50}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01462 — Subtract b from a. Example 47.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 47, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01463 — Multiply two numbers. Example 47.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 47, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 141}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 141}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01464 — Divide a by b. Example 47.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 47, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 15.666666666666666}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 15.666666666666666}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01465 — Calculate the remainder. Example 47.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 47, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01466 — Compare the numbers. Example 47.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 47, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01467 — Classify a as HIGH or LOW. Example 47.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 47, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01468 — Apply an 18 percent multiplier. Example 47.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 47, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 55.459999999999994}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 55.459999999999994}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01469 — Round the division result. Example 47.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 47, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 16}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 16}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01470 — Check whether a is even. Example 47.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 47, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01471 — Add two numbers. Example 48.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 48, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 52}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 52}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01472 — Subtract b from a. Example 48.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 48, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01473 — Multiply two numbers. Example 48.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 48, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 192}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 192}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01474 — Divide a by b. Example 48.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 48, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 12.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01475 — Calculate the remainder. Example 48.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 48, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01476 — Compare the numbers. Example 48.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 48, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01477 — Classify a as HIGH or LOW. Example 48.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 48, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01478 — Apply an 18 percent multiplier. Example 48.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 48, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 56.64}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 56.64}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01479 — Round the division result. Example 48.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 48, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 12}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01480 — Check whether a is even. Example 48.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 48, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01481 — Add two numbers. Example 49.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 49, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 54}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 54}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01482 — Subtract b from a. Example 49.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 49, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01483 — Multiply two numbers. Example 49.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 49, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 245}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 245}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01484 — Divide a by b. Example 49.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 49, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 9.8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9.8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01485 — Calculate the remainder. Example 49.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 49, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01486 — Compare the numbers. Example 49.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 49, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01487 — Classify a as HIGH or LOW. Example 49.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 49, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "LOW"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "LOW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01488 — Apply an 18 percent multiplier. Example 49.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 49, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 57.82}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 57.82}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01489 — Round the division result. Example 49.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 49, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 10}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 10}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01490 — Check whether a is even. Example 49.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 49, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01491 — Add two numbers. Example 50.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 50, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 56}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 56}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01492 — Subtract b from a. Example 50.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 50, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01493 — Multiply two numbers. Example 50.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 50, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 300}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 300}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01494 — Divide a by b. Example 50.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 50, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 8.333333333333334}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8.333333333333334}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01495 — Calculate the remainder. Example 50.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 50, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01496 — Compare the numbers. Example 50.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 50, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01497 — Classify a as HIGH or LOW. Example 50.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 50, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01498 — Apply an 18 percent multiplier. Example 50.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 50, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 59.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 59.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01499 — Round the division result. Example 50.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 50, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01500 — Check whether a is even. Example 50.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 50, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
