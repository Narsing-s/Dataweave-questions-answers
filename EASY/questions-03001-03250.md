# Easy DataWeave Questions 03001-03250

Questions 3001–3250 of 4000 in the Easy level.

## DW-03001 — Select name. Example 1, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 1}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.name
```

### Expected Output
```json
"Narsing"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Narsing".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03002 — Project only name. Example 1, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 1}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ name: payload.name }
```

### Expected Output
```json
{"name": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03003 — Wrap name under value. Example 1, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 1}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03004 — Uppercase name. Example 1, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 1}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.name) }
```

### Expected Output
```json
{"upper": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03005 — Return the length of name. Example 1, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 1}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.name) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03006 — Read name with a default. Example 1, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 1}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03007 — Check whether name is non-null. Example 1, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 1}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.name != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03008 — Add source metadata. Example 1, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 1}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"name": "Narsing", "index": 1, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing", "index": 1, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03009 — Return field metadata. Example 1, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 1}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "name", keyValue: payload.name }
```

### Expected Output
```json
{"keyName": "name", "keyValue": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "name", "keyValue": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03010 — Check whether name is a String. Example 1, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 1}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.name is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03011 — Select city. Example 2, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.city
```

### Expected Output
```json
"Hyderabad"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Hyderabad".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03012 — Project only city. Example 2, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ city: payload.city }
```

### Expected Output
```json
{"city": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03013 — Wrap city under value. Example 2, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03014 — Uppercase city. Example 2, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.city) }
```

### Expected Output
```json
{"upper": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03015 — Return the length of city. Example 2, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.city) }
```

### Expected Output
```json
{"length": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03016 — Read city with a default. Example 2, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03017 — Check whether city is non-null. Example 2, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.city != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03018 — Add source metadata. Example 2, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"city": "Hyderabad", "index": 2, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad", "index": 2, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03019 — Return field metadata. Example 2, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "city", keyValue: payload.city }
```

### Expected Output
```json
{"keyName": "city", "keyValue": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "city", "keyValue": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03020 — Check whether city is a String. Example 2, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.city is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03021 — Select country. Example 3, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.country
```

### Expected Output
```json
"India"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "India".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03022 — Project only country. Example 3, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ country: payload.country }
```

### Expected Output
```json
{"country": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03023 — Wrap country under value. Example 3, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03024 — Uppercase country. Example 3, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.country) }
```

### Expected Output
```json
{"upper": "INDIA"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "INDIA"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03025 — Return the length of country. Example 3, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.country) }
```

### Expected Output
```json
{"length": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03026 — Read country with a default. Example 3, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country default "UNKNOWN" }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03027 — Check whether country is non-null. Example 3, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.country != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03028 — Add source metadata. Example 3, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"country": "India", "index": 3, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India", "index": 3, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03029 — Return field metadata. Example 3, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "country", keyValue: payload.country }
```

### Expected Output
```json
{"keyName": "country", "keyValue": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "country", "keyValue": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03030 — Check whether country is a String. Example 3, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.country is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03031 — Select status. Example 4, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.status
```

### Expected Output
```json
"ACTIVE"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "ACTIVE".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03032 — Project only status. Example 4, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ status: payload.status }
```

### Expected Output
```json
{"status": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03033 — Wrap status under value. Example 4, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03034 — Uppercase status. Example 4, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.status) }
```

### Expected Output
```json
{"upper": "ACTIVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03035 — Return the length of status. Example 4, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.status) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03036 — Read status with a default. Example 4, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status default "UNKNOWN" }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03037 — Check whether status is non-null. Example 4, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.status != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03038 — Add source metadata. Example 4, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"status": "ACTIVE", "index": 4, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE", "index": 4, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03039 — Return field metadata. Example 4, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "status", keyValue: payload.status }
```

### Expected Output
```json
{"keyName": "status", "keyValue": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "status", "keyValue": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03040 — Check whether status is a String. Example 4, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.status is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03041 — Select category. Example 5, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.category
```

### Expected Output
```json
"PREMIUM"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "PREMIUM".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03042 — Project only category. Example 5, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ category: payload.category }
```

### Expected Output
```json
{"category": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03043 — Wrap category under value. Example 5, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03044 — Uppercase category. Example 5, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.category) }
```

### Expected Output
```json
{"upper": "PREMIUM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03045 — Return the length of category. Example 5, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.category) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03046 — Read category with a default. Example 5, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category default "UNKNOWN" }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03047 — Check whether category is non-null. Example 5, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.category != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03048 — Add source metadata. Example 5, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"category": "PREMIUM", "index": 5, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM", "index": 5, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03049 — Return field metadata. Example 5, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "category", keyValue: payload.category }
```

### Expected Output
```json
{"keyName": "category", "keyValue": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "category", "keyValue": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03050 — Check whether category is a String. Example 5, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.category is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03051 — Select department. Example 6, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.department
```

### Expected Output
```json
"IT"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "IT".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03052 — Project only department. Example 6, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ department: payload.department }
```

### Expected Output
```json
{"department": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03053 — Wrap department under value. Example 6, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03054 — Uppercase department. Example 6, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.department) }
```

### Expected Output
```json
{"upper": "IT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03055 — Return the length of department. Example 6, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.department) }
```

### Expected Output
```json
{"length": 2}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03056 — Read department with a default. Example 6, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department default "UNKNOWN" }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03057 — Check whether department is non-null. Example 6, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.department != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03058 — Add source metadata. Example 6, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"department": "IT", "index": 6, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT", "index": 6, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03059 — Return field metadata. Example 6, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "department", keyValue: payload.department }
```

### Expected Output
```json
{"keyName": "department", "keyValue": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "department", "keyValue": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03060 — Check whether department is a String. Example 6, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.department is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03061 — Select code. Example 7, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.code
```

### Expected Output
```json
"C100"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "C100".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03062 — Project only code. Example 7, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ code: payload.code }
```

### Expected Output
```json
{"code": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03063 — Wrap code under value. Example 7, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03064 — Uppercase code. Example 7, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.code) }
```

### Expected Output
```json
{"upper": "C100"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03065 — Return the length of code. Example 7, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.code) }
```

### Expected Output
```json
{"length": 4}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03066 — Read code with a default. Example 7, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code default "UNKNOWN" }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03067 — Check whether code is non-null. Example 7, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.code != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03068 — Add source metadata. Example 7, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"code": "C100", "index": 7, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100", "index": 7, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03069 — Return field metadata. Example 7, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "code", keyValue: payload.code }
```

### Expected Output
```json
{"keyName": "code", "keyValue": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "code", "keyValue": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03070 — Check whether code is a String. Example 7, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.code is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03071 — Select type. Example 8, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.type
```

### Expected Output
```json
"CUSTOMER"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "CUSTOMER".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03072 — Project only type. Example 8, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ type: payload.type }
```

### Expected Output
```json
{"type": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03073 — Wrap type under value. Example 8, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03074 — Uppercase type. Example 8, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.type) }
```

### Expected Output
```json
{"upper": "CUSTOMER"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03075 — Return the length of type. Example 8, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.type) }
```

### Expected Output
```json
{"length": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03076 — Read type with a default. Example 8, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type default "UNKNOWN" }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03077 — Check whether type is non-null. Example 8, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.type != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03078 — Add source metadata. Example 8, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"type": "CUSTOMER", "index": 8, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER", "index": 8, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03079 — Return field metadata. Example 8, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "type", keyValue: payload.type }
```

### Expected Output
```json
{"keyName": "type", "keyValue": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "type", "keyValue": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03080 — Check whether type is a String. Example 8, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.type is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03081 — Select email. Example 9, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.email
```

### Expected Output
```json
"a@example.com"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "a@example.com".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03082 — Project only email. Example 9, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ email: payload.email }
```

### Expected Output
```json
{"email": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03083 — Wrap email under value. Example 9, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03084 — Uppercase email. Example 9, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.email) }
```

### Expected Output
```json
{"upper": "A@EXAMPLE.COM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "A@EXAMPLE.COM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03085 — Return the length of email. Example 9, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.email) }
```

### Expected Output
```json
{"length": 13}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03086 — Read email with a default. Example 9, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email default "UNKNOWN" }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03087 — Check whether email is non-null. Example 9, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.email != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03088 — Add source metadata. Example 9, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"email": "a@example.com", "index": 9, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com", "index": 9, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03089 — Return field metadata. Example 9, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "email", keyValue: payload.email }
```

### Expected Output
```json
{"keyName": "email", "keyValue": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "email", "keyValue": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03090 — Check whether email is a String. Example 9, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.email is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03091 — Select description. Example 10, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.description
```

### Expected Output
```json
"Sample"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Sample".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03092 — Project only description. Example 10, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ description: payload.description }
```

### Expected Output
```json
{"description": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03093 — Wrap description under value. Example 10, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03094 — Uppercase description. Example 10, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.description) }
```

### Expected Output
```json
{"upper": "SAMPLE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "SAMPLE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03095 — Return the length of description. Example 10, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.description) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03096 — Read description with a default. Example 10, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03097 — Check whether description is non-null. Example 10, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.description != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03098 — Add source metadata. Example 10, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"description": "Sample", "index": 10, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample", "index": 10, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03099 — Return field metadata. Example 10, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "description", keyValue: payload.description }
```

### Expected Output
```json
{"keyName": "description", "keyValue": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "description", "keyValue": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03100 — Check whether description is a String. Example 10, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.description is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03101 — Select name. Example 11, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 11}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.name
```

### Expected Output
```json
"Narsing"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Narsing".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03102 — Project only name. Example 11, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 11}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ name: payload.name }
```

### Expected Output
```json
{"name": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03103 — Wrap name under value. Example 11, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 11}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03104 — Uppercase name. Example 11, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 11}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.name) }
```

### Expected Output
```json
{"upper": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03105 — Return the length of name. Example 11, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 11}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.name) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03106 — Read name with a default. Example 11, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 11}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03107 — Check whether name is non-null. Example 11, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 11}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.name != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03108 — Add source metadata. Example 11, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 11}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"name": "Narsing", "index": 11, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing", "index": 11, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03109 — Return field metadata. Example 11, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 11}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "name", keyValue: payload.name }
```

### Expected Output
```json
{"keyName": "name", "keyValue": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "name", "keyValue": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03110 — Check whether name is a String. Example 11, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 11}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.name is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03111 — Select city. Example 12, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 12}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.city
```

### Expected Output
```json
"Hyderabad"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Hyderabad".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03112 — Project only city. Example 12, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 12}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ city: payload.city }
```

### Expected Output
```json
{"city": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03113 — Wrap city under value. Example 12, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 12}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03114 — Uppercase city. Example 12, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 12}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.city) }
```

### Expected Output
```json
{"upper": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03115 — Return the length of city. Example 12, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 12}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.city) }
```

### Expected Output
```json
{"length": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03116 — Read city with a default. Example 12, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 12}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03117 — Check whether city is non-null. Example 12, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 12}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.city != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03118 — Add source metadata. Example 12, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 12}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"city": "Hyderabad", "index": 12, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad", "index": 12, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03119 — Return field metadata. Example 12, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 12}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "city", keyValue: payload.city }
```

### Expected Output
```json
{"keyName": "city", "keyValue": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "city", "keyValue": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03120 — Check whether city is a String. Example 12, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 12}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.city is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03121 — Select country. Example 13, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 13}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.country
```

### Expected Output
```json
"India"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "India".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03122 — Project only country. Example 13, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 13}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ country: payload.country }
```

### Expected Output
```json
{"country": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03123 — Wrap country under value. Example 13, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 13}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03124 — Uppercase country. Example 13, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 13}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.country) }
```

### Expected Output
```json
{"upper": "INDIA"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "INDIA"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03125 — Return the length of country. Example 13, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 13}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.country) }
```

### Expected Output
```json
{"length": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03126 — Read country with a default. Example 13, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 13}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country default "UNKNOWN" }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03127 — Check whether country is non-null. Example 13, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 13}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.country != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03128 — Add source metadata. Example 13, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 13}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"country": "India", "index": 13, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India", "index": 13, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03129 — Return field metadata. Example 13, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 13}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "country", keyValue: payload.country }
```

### Expected Output
```json
{"keyName": "country", "keyValue": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "country", "keyValue": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03130 — Check whether country is a String. Example 13, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 13}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.country is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03131 — Select status. Example 14, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 14}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.status
```

### Expected Output
```json
"ACTIVE"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "ACTIVE".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03132 — Project only status. Example 14, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 14}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ status: payload.status }
```

### Expected Output
```json
{"status": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03133 — Wrap status under value. Example 14, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 14}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03134 — Uppercase status. Example 14, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 14}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.status) }
```

### Expected Output
```json
{"upper": "ACTIVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03135 — Return the length of status. Example 14, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 14}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.status) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03136 — Read status with a default. Example 14, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 14}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status default "UNKNOWN" }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03137 — Check whether status is non-null. Example 14, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 14}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.status != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03138 — Add source metadata. Example 14, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 14}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"status": "ACTIVE", "index": 14, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE", "index": 14, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03139 — Return field metadata. Example 14, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 14}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "status", keyValue: payload.status }
```

### Expected Output
```json
{"keyName": "status", "keyValue": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "status", "keyValue": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03140 — Check whether status is a String. Example 14, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 14}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.status is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03141 — Select category. Example 15, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 15}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.category
```

### Expected Output
```json
"PREMIUM"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "PREMIUM".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03142 — Project only category. Example 15, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 15}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ category: payload.category }
```

### Expected Output
```json
{"category": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03143 — Wrap category under value. Example 15, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 15}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03144 — Uppercase category. Example 15, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 15}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.category) }
```

### Expected Output
```json
{"upper": "PREMIUM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03145 — Return the length of category. Example 15, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 15}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.category) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03146 — Read category with a default. Example 15, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 15}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category default "UNKNOWN" }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03147 — Check whether category is non-null. Example 15, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 15}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.category != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03148 — Add source metadata. Example 15, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 15}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"category": "PREMIUM", "index": 15, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM", "index": 15, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03149 — Return field metadata. Example 15, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 15}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "category", keyValue: payload.category }
```

### Expected Output
```json
{"keyName": "category", "keyValue": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "category", "keyValue": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03150 — Check whether category is a String. Example 15, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 15}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.category is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03151 — Select department. Example 16, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 16}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.department
```

### Expected Output
```json
"IT"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "IT".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03152 — Project only department. Example 16, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 16}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ department: payload.department }
```

### Expected Output
```json
{"department": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03153 — Wrap department under value. Example 16, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 16}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03154 — Uppercase department. Example 16, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 16}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.department) }
```

### Expected Output
```json
{"upper": "IT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03155 — Return the length of department. Example 16, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 16}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.department) }
```

### Expected Output
```json
{"length": 2}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03156 — Read department with a default. Example 16, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 16}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department default "UNKNOWN" }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03157 — Check whether department is non-null. Example 16, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 16}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.department != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03158 — Add source metadata. Example 16, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 16}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"department": "IT", "index": 16, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT", "index": 16, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03159 — Return field metadata. Example 16, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 16}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "department", keyValue: payload.department }
```

### Expected Output
```json
{"keyName": "department", "keyValue": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "department", "keyValue": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03160 — Check whether department is a String. Example 16, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 16}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.department is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03161 — Select code. Example 17, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 17}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.code
```

### Expected Output
```json
"C100"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "C100".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03162 — Project only code. Example 17, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 17}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ code: payload.code }
```

### Expected Output
```json
{"code": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03163 — Wrap code under value. Example 17, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 17}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03164 — Uppercase code. Example 17, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 17}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.code) }
```

### Expected Output
```json
{"upper": "C100"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03165 — Return the length of code. Example 17, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 17}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.code) }
```

### Expected Output
```json
{"length": 4}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03166 — Read code with a default. Example 17, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 17}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code default "UNKNOWN" }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03167 — Check whether code is non-null. Example 17, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 17}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.code != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03168 — Add source metadata. Example 17, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 17}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"code": "C100", "index": 17, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100", "index": 17, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03169 — Return field metadata. Example 17, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 17}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "code", keyValue: payload.code }
```

### Expected Output
```json
{"keyName": "code", "keyValue": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "code", "keyValue": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03170 — Check whether code is a String. Example 17, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 17}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.code is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03171 — Select type. Example 18, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 18}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.type
```

### Expected Output
```json
"CUSTOMER"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "CUSTOMER".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03172 — Project only type. Example 18, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 18}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ type: payload.type }
```

### Expected Output
```json
{"type": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03173 — Wrap type under value. Example 18, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 18}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03174 — Uppercase type. Example 18, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 18}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.type) }
```

### Expected Output
```json
{"upper": "CUSTOMER"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03175 — Return the length of type. Example 18, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 18}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.type) }
```

### Expected Output
```json
{"length": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03176 — Read type with a default. Example 18, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 18}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type default "UNKNOWN" }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03177 — Check whether type is non-null. Example 18, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 18}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.type != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03178 — Add source metadata. Example 18, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 18}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"type": "CUSTOMER", "index": 18, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER", "index": 18, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03179 — Return field metadata. Example 18, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 18}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "type", keyValue: payload.type }
```

### Expected Output
```json
{"keyName": "type", "keyValue": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "type", "keyValue": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03180 — Check whether type is a String. Example 18, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 18}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.type is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03181 — Select email. Example 19, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 19}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.email
```

### Expected Output
```json
"a@example.com"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "a@example.com".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03182 — Project only email. Example 19, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 19}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ email: payload.email }
```

### Expected Output
```json
{"email": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03183 — Wrap email under value. Example 19, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 19}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03184 — Uppercase email. Example 19, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 19}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.email) }
```

### Expected Output
```json
{"upper": "A@EXAMPLE.COM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "A@EXAMPLE.COM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03185 — Return the length of email. Example 19, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 19}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.email) }
```

### Expected Output
```json
{"length": 13}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03186 — Read email with a default. Example 19, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 19}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email default "UNKNOWN" }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03187 — Check whether email is non-null. Example 19, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 19}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.email != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03188 — Add source metadata. Example 19, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 19}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"email": "a@example.com", "index": 19, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com", "index": 19, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03189 — Return field metadata. Example 19, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 19}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "email", keyValue: payload.email }
```

### Expected Output
```json
{"keyName": "email", "keyValue": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "email", "keyValue": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03190 — Check whether email is a String. Example 19, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 19}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.email is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03191 — Select description. Example 20, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 20}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.description
```

### Expected Output
```json
"Sample"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Sample".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03192 — Project only description. Example 20, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 20}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ description: payload.description }
```

### Expected Output
```json
{"description": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03193 — Wrap description under value. Example 20, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 20}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03194 — Uppercase description. Example 20, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 20}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.description) }
```

### Expected Output
```json
{"upper": "SAMPLE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "SAMPLE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03195 — Return the length of description. Example 20, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 20}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.description) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03196 — Read description with a default. Example 20, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 20}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03197 — Check whether description is non-null. Example 20, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 20}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.description != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03198 — Add source metadata. Example 20, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 20}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"description": "Sample", "index": 20, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample", "index": 20, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03199 — Return field metadata. Example 20, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 20}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "description", keyValue: payload.description }
```

### Expected Output
```json
{"keyName": "description", "keyValue": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "description", "keyValue": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03200 — Check whether description is a String. Example 20, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 20}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.description is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03201 — Select name. Example 21, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 21}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.name
```

### Expected Output
```json
"Narsing"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Narsing".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03202 — Project only name. Example 21, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 21}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ name: payload.name }
```

### Expected Output
```json
{"name": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03203 — Wrap name under value. Example 21, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 21}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03204 — Uppercase name. Example 21, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 21}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.name) }
```

### Expected Output
```json
{"upper": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03205 — Return the length of name. Example 21, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 21}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.name) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03206 — Read name with a default. Example 21, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 21}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03207 — Check whether name is non-null. Example 21, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 21}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.name != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03208 — Add source metadata. Example 21, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 21}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"name": "Narsing", "index": 21, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing", "index": 21, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03209 — Return field metadata. Example 21, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 21}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "name", keyValue: payload.name }
```

### Expected Output
```json
{"keyName": "name", "keyValue": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "name", "keyValue": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03210 — Check whether name is a String. Example 21, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 21}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.name is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03211 — Select city. Example 22, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 22}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.city
```

### Expected Output
```json
"Hyderabad"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Hyderabad".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03212 — Project only city. Example 22, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 22}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ city: payload.city }
```

### Expected Output
```json
{"city": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03213 — Wrap city under value. Example 22, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 22}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03214 — Uppercase city. Example 22, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 22}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.city) }
```

### Expected Output
```json
{"upper": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03215 — Return the length of city. Example 22, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 22}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.city) }
```

### Expected Output
```json
{"length": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03216 — Read city with a default. Example 22, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 22}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03217 — Check whether city is non-null. Example 22, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 22}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.city != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03218 — Add source metadata. Example 22, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 22}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"city": "Hyderabad", "index": 22, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad", "index": 22, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03219 — Return field metadata. Example 22, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 22}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "city", keyValue: payload.city }
```

### Expected Output
```json
{"keyName": "city", "keyValue": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "city", "keyValue": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03220 — Check whether city is a String. Example 22, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 22}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.city is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03221 — Select country. Example 23, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 23}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.country
```

### Expected Output
```json
"India"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "India".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03222 — Project only country. Example 23, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 23}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ country: payload.country }
```

### Expected Output
```json
{"country": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03223 — Wrap country under value. Example 23, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 23}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03224 — Uppercase country. Example 23, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 23}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.country) }
```

### Expected Output
```json
{"upper": "INDIA"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "INDIA"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03225 — Return the length of country. Example 23, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 23}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.country) }
```

### Expected Output
```json
{"length": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03226 — Read country with a default. Example 23, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 23}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country default "UNKNOWN" }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03227 — Check whether country is non-null. Example 23, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 23}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.country != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03228 — Add source metadata. Example 23, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 23}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"country": "India", "index": 23, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India", "index": 23, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03229 — Return field metadata. Example 23, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 23}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "country", keyValue: payload.country }
```

### Expected Output
```json
{"keyName": "country", "keyValue": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "country", "keyValue": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03230 — Check whether country is a String. Example 23, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 23}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.country is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03231 — Select status. Example 24, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 24}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.status
```

### Expected Output
```json
"ACTIVE"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "ACTIVE".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03232 — Project only status. Example 24, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 24}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ status: payload.status }
```

### Expected Output
```json
{"status": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03233 — Wrap status under value. Example 24, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 24}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03234 — Uppercase status. Example 24, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 24}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.status) }
```

### Expected Output
```json
{"upper": "ACTIVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03235 — Return the length of status. Example 24, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 24}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.status) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03236 — Read status with a default. Example 24, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 24}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status default "UNKNOWN" }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03237 — Check whether status is non-null. Example 24, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 24}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.status != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03238 — Add source metadata. Example 24, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 24}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"status": "ACTIVE", "index": 24, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE", "index": 24, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03239 — Return field metadata. Example 24, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 24}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "status", keyValue: payload.status }
```

### Expected Output
```json
{"keyName": "status", "keyValue": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "status", "keyValue": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03240 — Check whether status is a String. Example 24, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 24}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.status is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03241 — Select category. Example 25, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 25}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.category
```

### Expected Output
```json
"PREMIUM"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "PREMIUM".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03242 — Project only category. Example 25, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 25}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ category: payload.category }
```

### Expected Output
```json
{"category": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03243 — Wrap category under value. Example 25, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 25}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03244 — Uppercase category. Example 25, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 25}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.category) }
```

### Expected Output
```json
{"upper": "PREMIUM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03245 — Return the length of category. Example 25, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 25}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.category) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03246 — Read category with a default. Example 25, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 25}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category default "UNKNOWN" }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03247 — Check whether category is non-null. Example 25, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 25}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.category != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03248 — Add source metadata. Example 25, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 25}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"category": "PREMIUM", "index": 25, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM", "index": 25, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03249 — Return field metadata. Example 25, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 25}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "category", keyValue: payload.category }
```

### Expected Output
```json
{"keyName": "category", "keyValue": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "category", "keyValue": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03250 — Check whether category is a String. Example 25, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 25}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.category is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
