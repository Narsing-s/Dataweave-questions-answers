# Easy DataWeave Questions 03501-03750

Questions 3501–3750 of 4000 in the Easy level.

## DW-03501 — Select name. Example 51, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.name
```

### Expected Output
```json
"Narsing"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Narsing".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03502 — Project only name. Example 51, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ name: payload.name }
```

### Expected Output
```json
{"name": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03503 — Wrap name under value. Example 51, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03504 — Uppercase name. Example 51, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.name) }
```

### Expected Output
```json
{"upper": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03505 — Return the length of name. Example 51, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.name) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03506 — Read name with a default. Example 51, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03507 — Check whether name is non-null. Example 51, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.name != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03508 — Add source metadata. Example 51, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"name": "Narsing", "index": 51, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing", "index": 51, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03509 — Return field metadata. Example 51, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "name", keyValue: payload.name }
```

### Expected Output
```json
{"keyName": "name", "keyValue": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "name", "keyValue": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03510 — Check whether name is a String. Example 51, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 51}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.name is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03511 — Select city. Example 52, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.city
```

### Expected Output
```json
"Hyderabad"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Hyderabad".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03512 — Project only city. Example 52, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ city: payload.city }
```

### Expected Output
```json
{"city": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03513 — Wrap city under value. Example 52, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03514 — Uppercase city. Example 52, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.city) }
```

### Expected Output
```json
{"upper": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03515 — Return the length of city. Example 52, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.city) }
```

### Expected Output
```json
{"length": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03516 — Read city with a default. Example 52, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03517 — Check whether city is non-null. Example 52, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.city != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03518 — Add source metadata. Example 52, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"city": "Hyderabad", "index": 52, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad", "index": 52, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03519 — Return field metadata. Example 52, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "city", keyValue: payload.city }
```

### Expected Output
```json
{"keyName": "city", "keyValue": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "city", "keyValue": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03520 — Check whether city is a String. Example 52, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 52}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.city is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03521 — Select country. Example 53, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.country
```

### Expected Output
```json
"India"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "India".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03522 — Project only country. Example 53, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ country: payload.country }
```

### Expected Output
```json
{"country": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03523 — Wrap country under value. Example 53, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03524 — Uppercase country. Example 53, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.country) }
```

### Expected Output
```json
{"upper": "INDIA"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "INDIA"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03525 — Return the length of country. Example 53, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.country) }
```

### Expected Output
```json
{"length": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03526 — Read country with a default. Example 53, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country default "UNKNOWN" }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03527 — Check whether country is non-null. Example 53, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.country != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03528 — Add source metadata. Example 53, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"country": "India", "index": 53, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India", "index": 53, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03529 — Return field metadata. Example 53, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "country", keyValue: payload.country }
```

### Expected Output
```json
{"keyName": "country", "keyValue": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "country", "keyValue": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03530 — Check whether country is a String. Example 53, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 53}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.country is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03531 — Select status. Example 54, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.status
```

### Expected Output
```json
"ACTIVE"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "ACTIVE".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03532 — Project only status. Example 54, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ status: payload.status }
```

### Expected Output
```json
{"status": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03533 — Wrap status under value. Example 54, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03534 — Uppercase status. Example 54, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.status) }
```

### Expected Output
```json
{"upper": "ACTIVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03535 — Return the length of status. Example 54, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.status) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03536 — Read status with a default. Example 54, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status default "UNKNOWN" }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03537 — Check whether status is non-null. Example 54, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.status != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03538 — Add source metadata. Example 54, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"status": "ACTIVE", "index": 54, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE", "index": 54, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03539 — Return field metadata. Example 54, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "status", keyValue: payload.status }
```

### Expected Output
```json
{"keyName": "status", "keyValue": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "status", "keyValue": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03540 — Check whether status is a String. Example 54, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 54}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.status is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03541 — Select category. Example 55, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.category
```

### Expected Output
```json
"PREMIUM"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "PREMIUM".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03542 — Project only category. Example 55, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ category: payload.category }
```

### Expected Output
```json
{"category": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03543 — Wrap category under value. Example 55, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03544 — Uppercase category. Example 55, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.category) }
```

### Expected Output
```json
{"upper": "PREMIUM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03545 — Return the length of category. Example 55, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.category) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03546 — Read category with a default. Example 55, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category default "UNKNOWN" }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03547 — Check whether category is non-null. Example 55, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.category != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03548 — Add source metadata. Example 55, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"category": "PREMIUM", "index": 55, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM", "index": 55, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03549 — Return field metadata. Example 55, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "category", keyValue: payload.category }
```

### Expected Output
```json
{"keyName": "category", "keyValue": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "category", "keyValue": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03550 — Check whether category is a String. Example 55, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 55}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.category is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03551 — Select department. Example 56, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.department
```

### Expected Output
```json
"IT"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "IT".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03552 — Project only department. Example 56, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ department: payload.department }
```

### Expected Output
```json
{"department": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03553 — Wrap department under value. Example 56, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03554 — Uppercase department. Example 56, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.department) }
```

### Expected Output
```json
{"upper": "IT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03555 — Return the length of department. Example 56, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.department) }
```

### Expected Output
```json
{"length": 2}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03556 — Read department with a default. Example 56, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department default "UNKNOWN" }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03557 — Check whether department is non-null. Example 56, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.department != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03558 — Add source metadata. Example 56, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"department": "IT", "index": 56, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT", "index": 56, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03559 — Return field metadata. Example 56, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "department", keyValue: payload.department }
```

### Expected Output
```json
{"keyName": "department", "keyValue": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "department", "keyValue": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03560 — Check whether department is a String. Example 56, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 56}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.department is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03561 — Select code. Example 57, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.code
```

### Expected Output
```json
"C100"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "C100".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03562 — Project only code. Example 57, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ code: payload.code }
```

### Expected Output
```json
{"code": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03563 — Wrap code under value. Example 57, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03564 — Uppercase code. Example 57, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.code) }
```

### Expected Output
```json
{"upper": "C100"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03565 — Return the length of code. Example 57, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.code) }
```

### Expected Output
```json
{"length": 4}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03566 — Read code with a default. Example 57, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code default "UNKNOWN" }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03567 — Check whether code is non-null. Example 57, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.code != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03568 — Add source metadata. Example 57, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"code": "C100", "index": 57, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100", "index": 57, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03569 — Return field metadata. Example 57, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "code", keyValue: payload.code }
```

### Expected Output
```json
{"keyName": "code", "keyValue": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "code", "keyValue": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03570 — Check whether code is a String. Example 57, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 57}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.code is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03571 — Select type. Example 58, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.type
```

### Expected Output
```json
"CUSTOMER"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "CUSTOMER".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03572 — Project only type. Example 58, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ type: payload.type }
```

### Expected Output
```json
{"type": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03573 — Wrap type under value. Example 58, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03574 — Uppercase type. Example 58, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.type) }
```

### Expected Output
```json
{"upper": "CUSTOMER"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03575 — Return the length of type. Example 58, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.type) }
```

### Expected Output
```json
{"length": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03576 — Read type with a default. Example 58, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type default "UNKNOWN" }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03577 — Check whether type is non-null. Example 58, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.type != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03578 — Add source metadata. Example 58, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"type": "CUSTOMER", "index": 58, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER", "index": 58, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03579 — Return field metadata. Example 58, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "type", keyValue: payload.type }
```

### Expected Output
```json
{"keyName": "type", "keyValue": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "type", "keyValue": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03580 — Check whether type is a String. Example 58, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 58}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.type is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03581 — Select email. Example 59, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.email
```

### Expected Output
```json
"a@example.com"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "a@example.com".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03582 — Project only email. Example 59, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ email: payload.email }
```

### Expected Output
```json
{"email": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03583 — Wrap email under value. Example 59, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03584 — Uppercase email. Example 59, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.email) }
```

### Expected Output
```json
{"upper": "A@EXAMPLE.COM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "A@EXAMPLE.COM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03585 — Return the length of email. Example 59, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.email) }
```

### Expected Output
```json
{"length": 13}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03586 — Read email with a default. Example 59, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email default "UNKNOWN" }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03587 — Check whether email is non-null. Example 59, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.email != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03588 — Add source metadata. Example 59, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"email": "a@example.com", "index": 59, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com", "index": 59, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03589 — Return field metadata. Example 59, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "email", keyValue: payload.email }
```

### Expected Output
```json
{"keyName": "email", "keyValue": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "email", "keyValue": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03590 — Check whether email is a String. Example 59, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 59}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.email is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03591 — Select description. Example 60, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.description
```

### Expected Output
```json
"Sample"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Sample".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03592 — Project only description. Example 60, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ description: payload.description }
```

### Expected Output
```json
{"description": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03593 — Wrap description under value. Example 60, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03594 — Uppercase description. Example 60, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.description) }
```

### Expected Output
```json
{"upper": "SAMPLE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "SAMPLE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03595 — Return the length of description. Example 60, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.description) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03596 — Read description with a default. Example 60, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03597 — Check whether description is non-null. Example 60, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.description != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03598 — Add source metadata. Example 60, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"description": "Sample", "index": 60, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample", "index": 60, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03599 — Return field metadata. Example 60, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "description", keyValue: payload.description }
```

### Expected Output
```json
{"keyName": "description", "keyValue": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "description", "keyValue": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03600 — Check whether description is a String. Example 60, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 60}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.description is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03601 — Select name. Example 61, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.name
```

### Expected Output
```json
"Narsing"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Narsing".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03602 — Project only name. Example 61, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ name: payload.name }
```

### Expected Output
```json
{"name": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03603 — Wrap name under value. Example 61, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03604 — Uppercase name. Example 61, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.name) }
```

### Expected Output
```json
{"upper": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03605 — Return the length of name. Example 61, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.name) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03606 — Read name with a default. Example 61, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03607 — Check whether name is non-null. Example 61, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.name != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03608 — Add source metadata. Example 61, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"name": "Narsing", "index": 61, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing", "index": 61, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03609 — Return field metadata. Example 61, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "name", keyValue: payload.name }
```

### Expected Output
```json
{"keyName": "name", "keyValue": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "name", "keyValue": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03610 — Check whether name is a String. Example 61, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 61}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.name is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03611 — Select city. Example 62, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.city
```

### Expected Output
```json
"Hyderabad"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Hyderabad".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03612 — Project only city. Example 62, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ city: payload.city }
```

### Expected Output
```json
{"city": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03613 — Wrap city under value. Example 62, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03614 — Uppercase city. Example 62, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.city) }
```

### Expected Output
```json
{"upper": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03615 — Return the length of city. Example 62, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.city) }
```

### Expected Output
```json
{"length": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03616 — Read city with a default. Example 62, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03617 — Check whether city is non-null. Example 62, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.city != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03618 — Add source metadata. Example 62, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"city": "Hyderabad", "index": 62, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad", "index": 62, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03619 — Return field metadata. Example 62, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "city", keyValue: payload.city }
```

### Expected Output
```json
{"keyName": "city", "keyValue": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "city", "keyValue": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03620 — Check whether city is a String. Example 62, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 62}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.city is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03621 — Select country. Example 63, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.country
```

### Expected Output
```json
"India"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "India".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03622 — Project only country. Example 63, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ country: payload.country }
```

### Expected Output
```json
{"country": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03623 — Wrap country under value. Example 63, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03624 — Uppercase country. Example 63, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.country) }
```

### Expected Output
```json
{"upper": "INDIA"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "INDIA"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03625 — Return the length of country. Example 63, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.country) }
```

### Expected Output
```json
{"length": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03626 — Read country with a default. Example 63, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country default "UNKNOWN" }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03627 — Check whether country is non-null. Example 63, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.country != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03628 — Add source metadata. Example 63, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"country": "India", "index": 63, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India", "index": 63, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03629 — Return field metadata. Example 63, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "country", keyValue: payload.country }
```

### Expected Output
```json
{"keyName": "country", "keyValue": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "country", "keyValue": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03630 — Check whether country is a String. Example 63, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 63}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.country is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03631 — Select status. Example 64, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.status
```

### Expected Output
```json
"ACTIVE"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "ACTIVE".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03632 — Project only status. Example 64, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ status: payload.status }
```

### Expected Output
```json
{"status": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03633 — Wrap status under value. Example 64, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03634 — Uppercase status. Example 64, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.status) }
```

### Expected Output
```json
{"upper": "ACTIVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03635 — Return the length of status. Example 64, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.status) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03636 — Read status with a default. Example 64, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status default "UNKNOWN" }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03637 — Check whether status is non-null. Example 64, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.status != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03638 — Add source metadata. Example 64, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"status": "ACTIVE", "index": 64, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE", "index": 64, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03639 — Return field metadata. Example 64, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "status", keyValue: payload.status }
```

### Expected Output
```json
{"keyName": "status", "keyValue": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "status", "keyValue": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03640 — Check whether status is a String. Example 64, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 64}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.status is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03641 — Select category. Example 65, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.category
```

### Expected Output
```json
"PREMIUM"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "PREMIUM".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03642 — Project only category. Example 65, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ category: payload.category }
```

### Expected Output
```json
{"category": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03643 — Wrap category under value. Example 65, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03644 — Uppercase category. Example 65, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.category) }
```

### Expected Output
```json
{"upper": "PREMIUM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03645 — Return the length of category. Example 65, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.category) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03646 — Read category with a default. Example 65, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category default "UNKNOWN" }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03647 — Check whether category is non-null. Example 65, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.category != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03648 — Add source metadata. Example 65, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"category": "PREMIUM", "index": 65, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM", "index": 65, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03649 — Return field metadata. Example 65, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "category", keyValue: payload.category }
```

### Expected Output
```json
{"keyName": "category", "keyValue": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "category", "keyValue": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03650 — Check whether category is a String. Example 65, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 65}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.category is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03651 — Select department. Example 66, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.department
```

### Expected Output
```json
"IT"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "IT".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03652 — Project only department. Example 66, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ department: payload.department }
```

### Expected Output
```json
{"department": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03653 — Wrap department under value. Example 66, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03654 — Uppercase department. Example 66, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.department) }
```

### Expected Output
```json
{"upper": "IT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03655 — Return the length of department. Example 66, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.department) }
```

### Expected Output
```json
{"length": 2}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03656 — Read department with a default. Example 66, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.department default "UNKNOWN" }
```

### Expected Output
```json
{"value": "IT"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03657 — Check whether department is non-null. Example 66, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.department != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03658 — Add source metadata. Example 66, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"department": "IT", "index": 66, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"department": "IT", "index": 66, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03659 — Return field metadata. Example 66, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "department", keyValue: payload.department }
```

### Expected Output
```json
{"keyName": "department", "keyValue": "IT"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "department", "keyValue": "IT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03660 — Check whether department is a String. Example 66, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"department": "IT", "index": 66}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.department is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03661 — Select code. Example 67, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.code
```

### Expected Output
```json
"C100"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "C100".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03662 — Project only code. Example 67, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ code: payload.code }
```

### Expected Output
```json
{"code": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03663 — Wrap code under value. Example 67, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03664 — Uppercase code. Example 67, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.code) }
```

### Expected Output
```json
{"upper": "C100"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03665 — Return the length of code. Example 67, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.code) }
```

### Expected Output
```json
{"length": 4}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03666 — Read code with a default. Example 67, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.code default "UNKNOWN" }
```

### Expected Output
```json
{"value": "C100"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03667 — Check whether code is non-null. Example 67, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.code != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03668 — Add source metadata. Example 67, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"code": "C100", "index": 67, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"code": "C100", "index": 67, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03669 — Return field metadata. Example 67, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "code", keyValue: payload.code }
```

### Expected Output
```json
{"keyName": "code", "keyValue": "C100"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "code", "keyValue": "C100"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03670 — Check whether code is a String. Example 67, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"code": "C100", "index": 67}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.code is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03671 — Select type. Example 68, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.type
```

### Expected Output
```json
"CUSTOMER"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "CUSTOMER".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03672 — Project only type. Example 68, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ type: payload.type }
```

### Expected Output
```json
{"type": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03673 — Wrap type under value. Example 68, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03674 — Uppercase type. Example 68, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.type) }
```

### Expected Output
```json
{"upper": "CUSTOMER"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03675 — Return the length of type. Example 68, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.type) }
```

### Expected Output
```json
{"length": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03676 — Read type with a default. Example 68, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.type default "UNKNOWN" }
```

### Expected Output
```json
{"value": "CUSTOMER"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03677 — Check whether type is non-null. Example 68, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.type != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03678 — Add source metadata. Example 68, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"type": "CUSTOMER", "index": 68, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"type": "CUSTOMER", "index": 68, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03679 — Return field metadata. Example 68, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "type", keyValue: payload.type }
```

### Expected Output
```json
{"keyName": "type", "keyValue": "CUSTOMER"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "type", "keyValue": "CUSTOMER"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03680 — Check whether type is a String. Example 68, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"type": "CUSTOMER", "index": 68}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.type is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03681 — Select email. Example 69, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.email
```

### Expected Output
```json
"a@example.com"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "a@example.com".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03682 — Project only email. Example 69, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ email: payload.email }
```

### Expected Output
```json
{"email": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03683 — Wrap email under value. Example 69, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03684 — Uppercase email. Example 69, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.email) }
```

### Expected Output
```json
{"upper": "A@EXAMPLE.COM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "A@EXAMPLE.COM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03685 — Return the length of email. Example 69, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.email) }
```

### Expected Output
```json
{"length": 13}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03686 — Read email with a default. Example 69, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.email default "UNKNOWN" }
```

### Expected Output
```json
{"value": "a@example.com"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03687 — Check whether email is non-null. Example 69, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.email != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03688 — Add source metadata. Example 69, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"email": "a@example.com", "index": 69, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"email": "a@example.com", "index": 69, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03689 — Return field metadata. Example 69, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "email", keyValue: payload.email }
```

### Expected Output
```json
{"keyName": "email", "keyValue": "a@example.com"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "email", "keyValue": "a@example.com"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03690 — Check whether email is a String. Example 69, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"email": "a@example.com", "index": 69}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.email is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03691 — Select description. Example 70, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.description
```

### Expected Output
```json
"Sample"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Sample".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03692 — Project only description. Example 70, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ description: payload.description }
```

### Expected Output
```json
{"description": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03693 — Wrap description under value. Example 70, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03694 — Uppercase description. Example 70, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.description) }
```

### Expected Output
```json
{"upper": "SAMPLE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "SAMPLE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03695 — Return the length of description. Example 70, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.description) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03696 — Read description with a default. Example 70, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.description default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Sample"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03697 — Check whether description is non-null. Example 70, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.description != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03698 — Add source metadata. Example 70, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"description": "Sample", "index": 70, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"description": "Sample", "index": 70, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03699 — Return field metadata. Example 70, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "description", keyValue: payload.description }
```

### Expected Output
```json
{"keyName": "description", "keyValue": "Sample"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "description", "keyValue": "Sample"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03700 — Check whether description is a String. Example 70, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"description": "Sample", "index": 70}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.description is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03701 — Select name. Example 71, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.name
```

### Expected Output
```json
"Narsing"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Narsing".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03702 — Project only name. Example 71, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ name: payload.name }
```

### Expected Output
```json
{"name": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03703 — Wrap name under value. Example 71, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03704 — Uppercase name. Example 71, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.name) }
```

### Expected Output
```json
{"upper": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03705 — Return the length of name. Example 71, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.name) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03706 — Read name with a default. Example 71, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.name default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Narsing"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03707 — Check whether name is non-null. Example 71, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.name != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03708 — Add source metadata. Example 71, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"name": "Narsing", "index": 71, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"name": "Narsing", "index": 71, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03709 — Return field metadata. Example 71, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "name", keyValue: payload.name }
```

### Expected Output
```json
{"keyName": "name", "keyValue": "Narsing"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "name", "keyValue": "Narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03710 — Check whether name is a String. Example 71, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"name": "Narsing", "index": 71}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.name is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03711 — Select city. Example 72, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.city
```

### Expected Output
```json
"Hyderabad"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "Hyderabad".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03712 — Project only city. Example 72, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ city: payload.city }
```

### Expected Output
```json
{"city": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03713 — Wrap city under value. Example 72, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03714 — Uppercase city. Example 72, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.city) }
```

### Expected Output
```json
{"upper": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03715 — Return the length of city. Example 72, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.city) }
```

### Expected Output
```json
{"length": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03716 — Read city with a default. Example 72, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.city default "UNKNOWN" }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03717 — Check whether city is non-null. Example 72, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.city != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03718 — Add source metadata. Example 72, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"city": "Hyderabad", "index": 72, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"city": "Hyderabad", "index": 72, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03719 — Return field metadata. Example 72, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "city", keyValue: payload.city }
```

### Expected Output
```json
{"keyName": "city", "keyValue": "Hyderabad"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "city", "keyValue": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03720 — Check whether city is a String. Example 72, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"city": "Hyderabad", "index": 72}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.city is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03721 — Select country. Example 73, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.country
```

### Expected Output
```json
"India"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "India".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03722 — Project only country. Example 73, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ country: payload.country }
```

### Expected Output
```json
{"country": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03723 — Wrap country under value. Example 73, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03724 — Uppercase country. Example 73, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.country) }
```

### Expected Output
```json
{"upper": "INDIA"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "INDIA"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03725 — Return the length of country. Example 73, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.country) }
```

### Expected Output
```json
{"length": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03726 — Read country with a default. Example 73, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.country default "UNKNOWN" }
```

### Expected Output
```json
{"value": "India"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03727 — Check whether country is non-null. Example 73, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.country != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03728 — Add source metadata. Example 73, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"country": "India", "index": 73, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"country": "India", "index": 73, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03729 — Return field metadata. Example 73, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "country", keyValue: payload.country }
```

### Expected Output
```json
{"keyName": "country", "keyValue": "India"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "country", "keyValue": "India"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03730 — Check whether country is a String. Example 73, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"country": "India", "index": 73}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.country is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03731 — Select status. Example 74, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.status
```

### Expected Output
```json
"ACTIVE"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "ACTIVE".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03732 — Project only status. Example 74, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ status: payload.status }
```

### Expected Output
```json
{"status": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03733 — Wrap status under value. Example 74, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03734 — Uppercase status. Example 74, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.status) }
```

### Expected Output
```json
{"upper": "ACTIVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03735 — Return the length of status. Example 74, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.status) }
```

### Expected Output
```json
{"length": 6}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03736 — Read status with a default. Example 74, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.status default "UNKNOWN" }
```

### Expected Output
```json
{"value": "ACTIVE"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03737 — Check whether status is non-null. Example 74, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.status != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03738 — Add source metadata. Example 74, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"status": "ACTIVE", "index": 74, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"status": "ACTIVE", "index": 74, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03739 — Return field metadata. Example 74, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "status", keyValue: payload.status }
```

### Expected Output
```json
{"keyName": "status", "keyValue": "ACTIVE"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "status", "keyValue": "ACTIVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03740 — Check whether status is a String. Example 74, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"status": "ACTIVE", "index": 74}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.status is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03741 — Select category. Example 75, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload.category
```

### Expected Output
```json
"PREMIUM"
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces "PREMIUM".

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03742 — Project only category. Example 75, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ category: payload.category }
```

### Expected Output
```json
{"category": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03743 — Wrap category under value. Example 75, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03744 — Uppercase category. Example 75, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ upper: upper(payload.category) }
```

### Expected Output
```json
{"upper": "PREMIUM"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"upper": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03745 — Return the length of category. Example 75, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ length: sizeOf(payload.category) }
```

### Expected Output
```json
{"length": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"length": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03746 — Read category with a default. Example 75, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.category default "UNKNOWN" }
```

### Expected Output
```json
{"value": "PREMIUM"}
```

### Explanation
What this teaches: default supplies a fallback when a value is null.

How it works: 1. Select the field. 2. Check for null. 3. Use the fallback when null. 4. Put the result in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03747 — Check whether category is non-null. Example 75, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ present: payload.category != null }
```

### Expected Output
```json
{"present": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"present": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03748 — Add source metadata. Example 75, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ { source: "DataWeave" }
```

### Expected Output
```json
{"category": "PREMIUM", "index": 75, "source": "DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"category": "PREMIUM", "index": 75, "source": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03749 — Return field metadata. Example 75, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ keyName: "category", keyValue: payload.category }
```

### Expected Output
```json
{"keyName": "category", "keyValue": "PREMIUM"}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"keyName": "category", "keyValue": "PREMIUM"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-03750 — Check whether category is a String. Example 75, family 1.

**Difficulty:** Easy
**Topic:** Fundamentals & Objects

### Input
```json
{"category": "PREMIUM", "index": 75}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ isString: payload.category is String }
```

### Expected Output
```json
{"isString": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"isString": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check nullability and the actual input schema.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
