# Easy DataWeave Questions 02251-02500

Questions 2251–2500 of 4000 in the Easy level.

## DW-02251 — Double every item. Example 26.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[-4, -2, 0, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-4, -2, 0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02252 — Increment every item. Example 26.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[-1, 0, 1, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-1, 0, 1, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02253 — Keep values greater than five. Example 26.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02254 — Keep even values. Example 26.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[-2, 0]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [-2, 0].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02255 — Count items. Example 26.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02256 — Sum values. Example 26.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02257 — Append 99. Example 26.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[-2, -1, 0, 1, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [-2, -1, 0, 1, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02258 — Prepend 0. Example 26.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, -2, -1, 0, 1]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, -2, -1, 0, 1].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02259 — Return the first item. Example 26.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02260 — Return the last item. Example 26.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02261 — Double every item. Example 27.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[6, 12, 18]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 12, 18].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02262 — Increment every item. Example 27.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[4, 7, 10]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 7, 10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02263 — Keep values greater than five. Example 27.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 9]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02264 — Keep even values. Example 27.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[6]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02265 — Count items. Example 27.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02266 — Sum values. Example 27.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
18
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 18.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02267 — Append 99. Example 27.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[3, 6, 9, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [3, 6, 9, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02268 — Prepend 0. Example 27.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 3, 6, 9]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 3, 6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02269 — Return the first item. Example 27.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02270 — Return the last item. Example 27.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
9
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 9.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02271 — Double every item. Example 28.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[200, 400, 600]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [200, 400, 600].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02272 — Increment every item. Example 28.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[101, 201, 301]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [101, 201, 301].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02273 — Keep values greater than five. Example 28.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02274 — Keep even values. Example 28.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02275 — Count items. Example 28.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02276 — Sum values. Example 28.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
600
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 600.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02277 — Append 99. Example 28.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[100, 200, 300, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02278 — Prepend 0. Example 28.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 100, 200, 300]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02279 — Return the first item. Example 28.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
100
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 100.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02280 — Return the last item. Example 28.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
300
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 300.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02281 — Double every item. Example 29.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 10, 20, 40]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 10, 20, 40].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02282 — Increment every item. Example 29.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 6, 11, 21]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 6, 11, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02283 — Keep values greater than five. Example 29.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02284 — Keep even values. Example 29.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02285 — Count items. Example 29.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02286 — Sum values. Example 29.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
36
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 36.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02287 — Append 99. Example 29.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 5, 10, 20, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 5, 10, 20, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02288 — Prepend 0. Example 29.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 5, 10, 20]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 5, 10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02289 — Return the first item. Example 29.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02290 — Return the last item. Example 29.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02291 — Double every item. Example 30.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[14, 28, 42]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [14, 28, 42].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02292 — Increment every item. Example 30.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[8, 15, 22]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [8, 15, 22].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02293 — Keep values greater than five. Example 30.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[7, 14, 21]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02294 — Keep even values. Example 30.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[14]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [14].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02295 — Count items. Example 30.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02296 — Sum values. Example 30.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
42
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 42.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02297 — Append 99. Example 30.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[7, 14, 21, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02298 — Prepend 0. Example 30.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 7, 14, 21]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02299 — Return the first item. Example 30.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
7
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 7.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02300 — Return the last item. Example 30.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
21
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 21.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02301 — Double every item. Example 31.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02302 — Increment every item. Example 31.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02303 — Keep values greater than five. Example 31.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02304 — Keep even values. Example 31.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02305 — Count items. Example 31.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02306 — Sum values. Example 31.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02307 — Append 99. Example 31.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02308 — Prepend 0. Example 31.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02309 — Return the first item. Example 31.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02310 — Return the last item. Example 31.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02311 — Double every item. Example 32.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02312 — Increment every item. Example 32.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[6, 11, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 11, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02313 — Keep values greater than five. Example 32.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 15]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02314 — Keep even values. Example 32.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02315 — Count items. Example 32.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02316 — Sum values. Example 32.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02317 — Append 99. Example 32.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[5, 10, 15, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [5, 10, 15, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02318 — Prepend 0. Example 32.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 5, 10, 15]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 5, 10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02319 — Return the first item. Example 32.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
5
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 5.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02320 — Return the last item. Example 32.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
15
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 15.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02321 — Double every item. Example 33.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[0, 2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02322 — Increment every item. Example 33.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[1, 2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02323 — Keep values greater than five. Example 33.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02324 — Keep even values. Example 33.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[0, 2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02325 — Count items. Example 33.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02326 — Sum values. Example 33.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02327 — Append 99. Example 33.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[0, 1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02328 — Prepend 0. Example 33.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02329 — Return the first item. Example 33.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
0
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 0.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02330 — Return the last item. Example 33.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02331 — Double every item. Example 34.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[4, 8, 12, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 8, 12, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02332 — Increment every item. Example 34.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[3, 5, 7, 9]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [3, 5, 7, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02333 — Keep values greater than five. Example 34.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02334 — Keep even values. Example 34.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2, 4, 6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02335 — Count items. Example 34.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02336 — Sum values. Example 34.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02337 — Append 99. Example 34.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[2, 4, 6, 8, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02338 — Prepend 0. Example 34.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 2, 4, 6, 8]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02339 — Return the first item. Example 34.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02340 — Return the last item. Example 34.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
8
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 8.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02341 — Double every item. Example 35.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[20, 40, 60]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [20, 40, 60].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02342 — Increment every item. Example 35.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[11, 21, 31]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [11, 21, 31].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02343 — Keep values greater than five. Example 35.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02344 — Keep even values. Example 35.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02345 — Count items. Example 35.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02346 — Sum values. Example 35.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
60
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 60.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02347 — Append 99. Example 35.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[10, 20, 30, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02348 — Prepend 0. Example 35.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 10, 20, 30]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02349 — Return the first item. Example 35.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
10
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 10.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02350 — Return the last item. Example 35.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02351 — Double every item. Example 36.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[-4, -2, 0, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-4, -2, 0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02352 — Increment every item. Example 36.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[-1, 0, 1, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-1, 0, 1, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02353 — Keep values greater than five. Example 36.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02354 — Keep even values. Example 36.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[-2, 0]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [-2, 0].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02355 — Count items. Example 36.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02356 — Sum values. Example 36.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02357 — Append 99. Example 36.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[-2, -1, 0, 1, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [-2, -1, 0, 1, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02358 — Prepend 0. Example 36.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, -2, -1, 0, 1]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, -2, -1, 0, 1].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02359 — Return the first item. Example 36.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02360 — Return the last item. Example 36.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02361 — Double every item. Example 37.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[6, 12, 18]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 12, 18].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02362 — Increment every item. Example 37.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[4, 7, 10]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 7, 10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02363 — Keep values greater than five. Example 37.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 9]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02364 — Keep even values. Example 37.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[6]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02365 — Count items. Example 37.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02366 — Sum values. Example 37.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
18
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 18.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02367 — Append 99. Example 37.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[3, 6, 9, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [3, 6, 9, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02368 — Prepend 0. Example 37.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 3, 6, 9]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 3, 6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02369 — Return the first item. Example 37.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02370 — Return the last item. Example 37.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
9
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 9.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02371 — Double every item. Example 38.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[200, 400, 600]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [200, 400, 600].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02372 — Increment every item. Example 38.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[101, 201, 301]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [101, 201, 301].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02373 — Keep values greater than five. Example 38.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02374 — Keep even values. Example 38.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02375 — Count items. Example 38.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02376 — Sum values. Example 38.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
600
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 600.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02377 — Append 99. Example 38.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[100, 200, 300, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02378 — Prepend 0. Example 38.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 100, 200, 300]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02379 — Return the first item. Example 38.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
100
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 100.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02380 — Return the last item. Example 38.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
300
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 300.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02381 — Double every item. Example 39.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 10, 20, 40]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 10, 20, 40].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02382 — Increment every item. Example 39.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 6, 11, 21]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 6, 11, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02383 — Keep values greater than five. Example 39.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02384 — Keep even values. Example 39.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02385 — Count items. Example 39.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02386 — Sum values. Example 39.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
36
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 36.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02387 — Append 99. Example 39.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 5, 10, 20, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 5, 10, 20, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02388 — Prepend 0. Example 39.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 5, 10, 20]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 5, 10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02389 — Return the first item. Example 39.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02390 — Return the last item. Example 39.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02391 — Double every item. Example 40.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[14, 28, 42]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [14, 28, 42].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02392 — Increment every item. Example 40.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[8, 15, 22]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [8, 15, 22].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02393 — Keep values greater than five. Example 40.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[7, 14, 21]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02394 — Keep even values. Example 40.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[14]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [14].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02395 — Count items. Example 40.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02396 — Sum values. Example 40.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
42
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 42.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02397 — Append 99. Example 40.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[7, 14, 21, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02398 — Prepend 0. Example 40.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 7, 14, 21]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02399 — Return the first item. Example 40.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
7
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 7.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02400 — Return the last item. Example 40.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
21
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 21.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02401 — Double every item. Example 41.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02402 — Increment every item. Example 41.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02403 — Keep values greater than five. Example 41.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02404 — Keep even values. Example 41.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02405 — Count items. Example 41.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02406 — Sum values. Example 41.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02407 — Append 99. Example 41.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02408 — Prepend 0. Example 41.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02409 — Return the first item. Example 41.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02410 — Return the last item. Example 41.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02411 — Double every item. Example 42.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02412 — Increment every item. Example 42.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[6, 11, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 11, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02413 — Keep values greater than five. Example 42.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 15]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02414 — Keep even values. Example 42.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02415 — Count items. Example 42.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02416 — Sum values. Example 42.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02417 — Append 99. Example 42.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[5, 10, 15, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [5, 10, 15, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02418 — Prepend 0. Example 42.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 5, 10, 15]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 5, 10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02419 — Return the first item. Example 42.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
5
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 5.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02420 — Return the last item. Example 42.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
15
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 15.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02421 — Double every item. Example 43.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[0, 2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02422 — Increment every item. Example 43.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[1, 2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02423 — Keep values greater than five. Example 43.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02424 — Keep even values. Example 43.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[0, 2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02425 — Count items. Example 43.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02426 — Sum values. Example 43.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02427 — Append 99. Example 43.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[0, 1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02428 — Prepend 0. Example 43.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02429 — Return the first item. Example 43.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
0
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 0.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02430 — Return the last item. Example 43.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02431 — Double every item. Example 44.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[4, 8, 12, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 8, 12, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02432 — Increment every item. Example 44.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[3, 5, 7, 9]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [3, 5, 7, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02433 — Keep values greater than five. Example 44.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02434 — Keep even values. Example 44.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2, 4, 6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02435 — Count items. Example 44.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02436 — Sum values. Example 44.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02437 — Append 99. Example 44.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[2, 4, 6, 8, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02438 — Prepend 0. Example 44.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 2, 4, 6, 8]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02439 — Return the first item. Example 44.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02440 — Return the last item. Example 44.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
8
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 8.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02441 — Double every item. Example 45.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[20, 40, 60]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [20, 40, 60].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02442 — Increment every item. Example 45.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[11, 21, 31]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [11, 21, 31].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02443 — Keep values greater than five. Example 45.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02444 — Keep even values. Example 45.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02445 — Count items. Example 45.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02446 — Sum values. Example 45.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
60
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 60.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02447 — Append 99. Example 45.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[10, 20, 30, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02448 — Prepend 0. Example 45.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 10, 20, 30]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02449 — Return the first item. Example 45.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
10
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 10.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02450 — Return the last item. Example 45.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02451 — Double every item. Example 46.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[-4, -2, 0, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-4, -2, 0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02452 — Increment every item. Example 46.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[-1, 0, 1, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-1, 0, 1, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02453 — Keep values greater than five. Example 46.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02454 — Keep even values. Example 46.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[-2, 0]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [-2, 0].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02455 — Count items. Example 46.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02456 — Sum values. Example 46.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02457 — Append 99. Example 46.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[-2, -1, 0, 1, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [-2, -1, 0, 1, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02458 — Prepend 0. Example 46.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, -2, -1, 0, 1]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, -2, -1, 0, 1].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02459 — Return the first item. Example 46.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02460 — Return the last item. Example 46.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02461 — Double every item. Example 47.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[6, 12, 18]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 12, 18].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02462 — Increment every item. Example 47.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[4, 7, 10]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 7, 10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02463 — Keep values greater than five. Example 47.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 9]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02464 — Keep even values. Example 47.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[6]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02465 — Count items. Example 47.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02466 — Sum values. Example 47.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
18
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 18.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02467 — Append 99. Example 47.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[3, 6, 9, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [3, 6, 9, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02468 — Prepend 0. Example 47.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 3, 6, 9]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 3, 6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02469 — Return the first item. Example 47.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02470 — Return the last item. Example 47.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
9
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 9.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02471 — Double every item. Example 48.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[200, 400, 600]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [200, 400, 600].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02472 — Increment every item. Example 48.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[101, 201, 301]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [101, 201, 301].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02473 — Keep values greater than five. Example 48.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02474 — Keep even values. Example 48.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02475 — Count items. Example 48.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02476 — Sum values. Example 48.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
600
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 600.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02477 — Append 99. Example 48.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[100, 200, 300, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02478 — Prepend 0. Example 48.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 100, 200, 300]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02479 — Return the first item. Example 48.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
100
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 100.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02480 — Return the last item. Example 48.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
300
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 300.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02481 — Double every item. Example 49.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 10, 20, 40]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 10, 20, 40].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02482 — Increment every item. Example 49.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 6, 11, 21]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 6, 11, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02483 — Keep values greater than five. Example 49.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02484 — Keep even values. Example 49.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02485 — Count items. Example 49.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02486 — Sum values. Example 49.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
36
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 36.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02487 — Append 99. Example 49.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 5, 10, 20, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 5, 10, 20, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02488 — Prepend 0. Example 49.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 5, 10, 20]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 5, 10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02489 — Return the first item. Example 49.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02490 — Return the last item. Example 49.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02491 — Double every item. Example 50.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[14, 28, 42]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [14, 28, 42].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02492 — Increment every item. Example 50.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[8, 15, 22]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [8, 15, 22].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02493 — Keep values greater than five. Example 50.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[7, 14, 21]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02494 — Keep even values. Example 50.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[14]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [14].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02495 — Count items. Example 50.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02496 — Sum values. Example 50.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
42
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 42.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02497 — Append 99. Example 50.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[7, 14, 21, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02498 — Prepend 0. Example 50.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 7, 14, 21]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02499 — Return the first item. Example 50.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
7
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 7.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02500 — Return the last item. Example 50.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
21
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 21.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
