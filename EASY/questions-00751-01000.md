# Easy DataWeave Questions 00751-01000

Questions 751–1000 of 4000 in the Easy level.

## DW-00751 — Convert the string to uppercase. Example 76.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MIXED"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MIXED"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00752 — Convert the string to lowercase. Example 76.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mixed"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mixed"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00753 — Remove leading and trailing spaces. Example 76.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00754 — Return the string length. Example 76.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00755 — Append -DW. Example 76.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MiXeD-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00756 — Prefix with DW- Example 76.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MiXeD"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00757 — Check whether the string contains a. Example 76.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00758 — Check whether the string starts with D. Example 76.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00759 — Check whether the string ends with e. Example 76.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00760 — Replace a with @. Example 76.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00761 — Convert the string to uppercase. Example 77.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "CUSTOMER-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00762 — Convert the string to lowercase. Example 77.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00763 — Remove leading and trailing spaces. Example 77.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00764 — Return the string length. Example 77.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00765 — Append -DW. Example 77.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "customer-123-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00766 — Prefix with DW- Example 77.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-customer-123"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00767 — Check whether the string contains a. Example 77.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00768 — Check whether the string starts with D. Example 77.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00769 — Check whether the string ends with e. Example 77.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00770 — Replace a with @. Example 77.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00771 — Convert the string to uppercase. Example 78.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "API_RESPONSE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "API_RESPONSE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00772 — Convert the string to lowercase. Example 78.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00773 — Remove leading and trailing spaces. Example 78.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00774 — Return the string length. Example 78.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00775 — Append -DW. Example 78.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "api_response-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00776 — Prefix with DW- Example 78.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-api_response"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00777 — Check whether the string contains a. Example 78.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00778 — Check whether the string starts with D. Example 78.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00779 — Check whether the string ends with e. Example 78.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00780 — Replace a with @. Example 78.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "@pi_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "@pi_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00781 — Convert the string to uppercase. Example 79.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00782 — Convert the string to lowercase. Example 79.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00783 — Remove leading and trailing spaces. Example 79.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00784 — Return the string length. Example 79.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00785 — Append -DW. Example 79.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "Hyderabad-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00786 — Prefix with DW- Example 79.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-Hyderabad"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00787 — Check whether the string contains a. Example 79.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00788 — Check whether the string starts with D. Example 79.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00789 — Check whether the string ends with e. Example 79.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00790 — Replace a with @. Example 79.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "Hyder@b@d"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyder@b@d"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00791 — Convert the string to uppercase. Example 80.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "INTEGRATION"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "INTEGRATION"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00792 — Convert the string to lowercase. Example 80.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00793 — Remove leading and trailing spaces. Example 80.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00794 — Return the string length. Example 80.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00795 — Append -DW. Example 80.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "integration-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00796 — Prefix with DW- Example 80.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-integration"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00797 — Check whether the string contains a. Example 80.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00798 — Check whether the string starts with D. Example 80.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00799 — Check whether the string ends with e. Example 80.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00800 — Replace a with @. Example 80.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "integr@tion"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integr@tion"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00801 — Convert the string to uppercase. Example 81.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00802 — Convert the string to lowercase. Example 81.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00803 — Remove leading and trailing spaces. Example 81.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00804 — Return the string length. Example 81.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00805 — Append -DW. Example 81.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "narsing-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00806 — Prefix with DW- Example 81.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-narsing"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00807 — Check whether the string contains a. Example 81.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00808 — Check whether the string starts with D. Example 81.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00809 — Check whether the string ends with e. Example 81.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00810 — Replace a with @. Example 81.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "n@rsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "n@rsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00811 — Convert the string to uppercase. Example 82.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "DATAWEAVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DATAWEAVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00812 — Convert the string to lowercase. Example 82.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "dataweave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "dataweave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00813 — Remove leading and trailing spaces. Example 82.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "DataWeave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00814 — Return the string length. Example 82.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00815 — Append -DW. Example 82.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "DataWeave-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00816 — Prefix with DW- Example 82.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00817 — Check whether the string contains a. Example 82.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00818 — Check whether the string starts with D. Example 82.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00819 — Check whether the string ends with e. Example 82.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00820 — Replace a with @. Example 82.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "D@t@We@ve"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "D@t@We@ve"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00821 — Convert the string to uppercase. Example 83.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MULESOFT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MULESOFT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00822 — Convert the string to lowercase. Example 83.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mulesoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mulesoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00823 — Remove leading and trailing spaces. Example 83.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00824 — Return the string length. Example 83.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00825 — Append -DW. Example 83.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MuleSoft-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00826 — Prefix with DW- Example 83.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MuleSoft"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00827 — Check whether the string contains a. Example 83.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00828 — Check whether the string starts with D. Example 83.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00829 — Check whether the string ends with e. Example 83.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00830 — Replace a with @. Example 83.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00831 — Convert the string to uppercase. Example 84.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HELLO WORLD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HELLO WORLD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00832 — Convert the string to lowercase. Example 84.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00833 — Remove leading and trailing spaces. Example 84.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00834 — Return the string length. Example 84.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00835 — Append -DW. Example 84.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "hello world-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00836 — Prefix with DW- Example 84.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-hello world"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00837 — Check whether the string contains a. Example 84.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00838 — Check whether the string starts with D. Example 84.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00839 — Check whether the string ends with e. Example 84.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00840 — Replace a with @. Example 84.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00841 — Convert the string to uppercase. Example 85.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "  TRIM ME  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  TRIM ME  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00842 — Convert the string to lowercase. Example 85.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00843 — Remove leading and trailing spaces. Example 85.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "trim me"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "trim me"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00844 — Return the string length. Example 85.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00845 — Append -DW. Example 85.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "  trim me  -DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  -DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00846 — Prefix with DW- Example 85.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-  trim me  "}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00847 — Check whether the string contains a. Example 85.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00848 — Check whether the string starts with D. Example 85.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00849 — Check whether the string ends with e. Example 85.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00850 — Replace a with @. Example 85.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00851 — Convert the string to uppercase. Example 86.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MIXED"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MIXED"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00852 — Convert the string to lowercase. Example 86.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mixed"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mixed"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00853 — Remove leading and trailing spaces. Example 86.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00854 — Return the string length. Example 86.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00855 — Append -DW. Example 86.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MiXeD-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00856 — Prefix with DW- Example 86.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MiXeD"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00857 — Check whether the string contains a. Example 86.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00858 — Check whether the string starts with D. Example 86.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00859 — Check whether the string ends with e. Example 86.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00860 — Replace a with @. Example 86.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00861 — Convert the string to uppercase. Example 87.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "CUSTOMER-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00862 — Convert the string to lowercase. Example 87.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00863 — Remove leading and trailing spaces. Example 87.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00864 — Return the string length. Example 87.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00865 — Append -DW. Example 87.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "customer-123-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00866 — Prefix with DW- Example 87.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-customer-123"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00867 — Check whether the string contains a. Example 87.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00868 — Check whether the string starts with D. Example 87.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00869 — Check whether the string ends with e. Example 87.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00870 — Replace a with @. Example 87.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00871 — Convert the string to uppercase. Example 88.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "API_RESPONSE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "API_RESPONSE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00872 — Convert the string to lowercase. Example 88.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00873 — Remove leading and trailing spaces. Example 88.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00874 — Return the string length. Example 88.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00875 — Append -DW. Example 88.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "api_response-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00876 — Prefix with DW- Example 88.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-api_response"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00877 — Check whether the string contains a. Example 88.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00878 — Check whether the string starts with D. Example 88.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00879 — Check whether the string ends with e. Example 88.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00880 — Replace a with @. Example 88.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "@pi_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "@pi_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00881 — Convert the string to uppercase. Example 89.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00882 — Convert the string to lowercase. Example 89.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00883 — Remove leading and trailing spaces. Example 89.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00884 — Return the string length. Example 89.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00885 — Append -DW. Example 89.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "Hyderabad-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00886 — Prefix with DW- Example 89.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-Hyderabad"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00887 — Check whether the string contains a. Example 89.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00888 — Check whether the string starts with D. Example 89.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00889 — Check whether the string ends with e. Example 89.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00890 — Replace a with @. Example 89.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "Hyder@b@d"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyder@b@d"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00891 — Convert the string to uppercase. Example 90.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "INTEGRATION"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "INTEGRATION"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00892 — Convert the string to lowercase. Example 90.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00893 — Remove leading and trailing spaces. Example 90.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00894 — Return the string length. Example 90.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00895 — Append -DW. Example 90.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "integration-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00896 — Prefix with DW- Example 90.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-integration"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00897 — Check whether the string contains a. Example 90.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00898 — Check whether the string starts with D. Example 90.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00899 — Check whether the string ends with e. Example 90.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00900 — Replace a with @. Example 90.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "integr@tion"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integr@tion"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00901 — Convert the string to uppercase. Example 91.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "NARSING"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "NARSING"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00902 — Convert the string to lowercase. Example 91.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00903 — Remove leading and trailing spaces. Example 91.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "narsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00904 — Return the string length. Example 91.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 7}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00905 — Append -DW. Example 91.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "narsing-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "narsing-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00906 — Prefix with DW- Example 91.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-narsing"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-narsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00907 — Check whether the string contains a. Example 91.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00908 — Check whether the string starts with D. Example 91.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00909 — Check whether the string ends with e. Example 91.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00910 — Replace a with @. Example 91.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "narsing"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "n@rsing"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "n@rsing"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00911 — Convert the string to uppercase. Example 92.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "DATAWEAVE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DATAWEAVE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00912 — Convert the string to lowercase. Example 92.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "dataweave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "dataweave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00913 — Remove leading and trailing spaces. Example 92.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "DataWeave"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00914 — Return the string length. Example 92.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00915 — Append -DW. Example 92.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "DataWeave-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DataWeave-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00916 — Prefix with DW- Example 92.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-DataWeave"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-DataWeave"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00917 — Check whether the string contains a. Example 92.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00918 — Check whether the string starts with D. Example 92.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00919 — Check whether the string ends with e. Example 92.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00920 — Replace a with @. Example 92.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "DataWeave"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "D@t@We@ve"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "D@t@We@ve"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00921 — Convert the string to uppercase. Example 93.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MULESOFT"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MULESOFT"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00922 — Convert the string to lowercase. Example 93.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mulesoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mulesoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00923 — Remove leading and trailing spaces. Example 93.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00924 — Return the string length. Example 93.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 8}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00925 — Append -DW. Example 93.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MuleSoft-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00926 — Prefix with DW- Example 93.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MuleSoft"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00927 — Check whether the string contains a. Example 93.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00928 — Check whether the string starts with D. Example 93.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00929 — Check whether the string ends with e. Example 93.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00930 — Replace a with @. Example 93.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MuleSoft"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MuleSoft"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MuleSoft"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00931 — Convert the string to uppercase. Example 94.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HELLO WORLD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HELLO WORLD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00932 — Convert the string to lowercase. Example 94.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00933 — Remove leading and trailing spaces. Example 94.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00934 — Return the string length. Example 94.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00935 — Append -DW. Example 94.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "hello world-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00936 — Prefix with DW- Example 94.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-hello world"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00937 — Check whether the string contains a. Example 94.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00938 — Check whether the string starts with D. Example 94.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00939 — Check whether the string ends with e. Example 94.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00940 — Replace a with @. Example 94.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "hello world"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "hello world"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hello world"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00941 — Convert the string to uppercase. Example 95.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "  TRIM ME  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  TRIM ME  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00942 — Convert the string to lowercase. Example 95.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00943 — Remove leading and trailing spaces. Example 95.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "trim me"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "trim me"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00944 — Return the string length. Example 95.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00945 — Append -DW. Example 95.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "  trim me  -DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  -DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00946 — Prefix with DW- Example 95.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-  trim me  "}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00947 — Check whether the string contains a. Example 95.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00948 — Check whether the string starts with D. Example 95.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00949 — Check whether the string ends with e. Example 95.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00950 — Replace a with @. Example 95.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "  trim me  "}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "  trim me  "}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "  trim me  "}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00951 — Convert the string to uppercase. Example 96.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "MIXED"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MIXED"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00952 — Convert the string to lowercase. Example 96.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "mixed"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "mixed"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00953 — Remove leading and trailing spaces. Example 96.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00954 — Return the string length. Example 96.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 5}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00955 — Append -DW. Example 96.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "MiXeD-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00956 — Prefix with DW- Example 96.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-MiXeD"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00957 — Check whether the string contains a. Example 96.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00958 — Check whether the string starts with D. Example 96.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00959 — Check whether the string ends with e. Example 96.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00960 — Replace a with @. Example 96.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "MiXeD"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "MiXeD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "MiXeD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00961 — Convert the string to uppercase. Example 97.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "CUSTOMER-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "CUSTOMER-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00962 — Convert the string to lowercase. Example 97.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00963 — Remove leading and trailing spaces. Example 97.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00964 — Return the string length. Example 97.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00965 — Append -DW. Example 97.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "customer-123-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00966 — Prefix with DW- Example 97.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-customer-123"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00967 — Check whether the string contains a. Example 97.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00968 — Check whether the string starts with D. Example 97.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00969 — Check whether the string ends with e. Example 97.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00970 — Replace a with @. Example 97.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "customer-123"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "customer-123"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "customer-123"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00971 — Convert the string to uppercase. Example 98.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "API_RESPONSE"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "API_RESPONSE"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00972 — Convert the string to lowercase. Example 98.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00973 — Remove leading and trailing spaces. Example 98.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "api_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00974 — Return the string length. Example 98.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 12}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00975 — Append -DW. Example 98.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "api_response-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "api_response-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00976 — Prefix with DW- Example 98.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-api_response"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-api_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00977 — Check whether the string contains a. Example 98.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00978 — Check whether the string starts with D. Example 98.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00979 — Check whether the string ends with e. Example 98.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00980 — Replace a with @. Example 98.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "api_response"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "@pi_response"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "@pi_response"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00981 — Convert the string to uppercase. Example 99.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "HYDERABAD"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "HYDERABAD"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00982 — Convert the string to lowercase. Example 99.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00983 — Remove leading and trailing spaces. Example 99.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "Hyderabad"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00984 — Return the string length. Example 99.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 9}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00985 — Append -DW. Example 99.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "Hyderabad-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyderabad-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00986 — Prefix with DW- Example 99.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-Hyderabad"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-Hyderabad"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00987 — Check whether the string contains a. Example 99.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00988 — Check whether the string starts with D. Example 99.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00989 — Check whether the string ends with e. Example 99.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00990 — Replace a with @. Example 99.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "Hyderabad"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "Hyder@b@d"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "Hyder@b@d"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00991 — Convert the string to uppercase. Example 100.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: upper(payload.value) }
```

### Expected Output
```json
{"value": "INTEGRATION"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "INTEGRATION"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00992 — Convert the string to lowercase. Example 100.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: lower(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00993 — Remove leading and trailing spaces. Example 100.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: trim(payload.value) }
```

### Expected Output
```json
{"value": "integration"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00994 — Return the string length. Example 100.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: sizeOf(payload.value) }
```

### Expected Output
```json
{"value": 11}
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces {"value": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00995 — Append -DW. Example 100.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value ++ "-DW" }
```

### Expected Output
```json
{"value": "integration-DW"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integration-DW"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00996 — Prefix with DW- Example 100.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: "DW-" ++ payload.value }
```

### Expected Output
```json
{"value": "DW-integration"}
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces {"value": "DW-integration"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00997 — Check whether the string contains a. Example 100.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value contains "a" }
```

### Expected Output
```json
{"value": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00998 — Check whether the string starts with D. Example 100.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value startsWith "D" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-00999 — Check whether the string ends with e. Example 100.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value endsWith "e" }
```

### Expected Output
```json
{"value": false}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"value": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01000 — Replace a with @. Example 100.

**Difficulty:** Easy
**Topic:** Strings

### Input
```json
{"value": "integration"}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ value: payload.value replace "a" with "@" }
```

### Expected Output
```json
{"value": "integr@tion"}
```

### Explanation
What this teaches: A DataWeave string function transforms the selected text.

How it works: 1. Select the source string. 2. Apply the function. 3. Use the returned string in the output.

Why this output: the expression is evaluated against the supplied input and produces {"value": "integr@tion"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check null values and input types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
