# Easy DataWeave Questions 01501-01750

Questions 1501–1750 of 4000 in the Easy level.

## DW-01501 — Add two numbers. Example 51.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 51, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 58}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 58}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01502 — Subtract b from a. Example 51.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 51, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01503 — Multiply two numbers. Example 51.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 51, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 357}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 357}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01504 — Divide a by b. Example 51.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 51, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 7.285714285714286}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7.285714285714286}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01505 — Calculate the remainder. Example 51.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 51, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01506 — Compare the numbers. Example 51.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 51, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01507 — Classify a as HIGH or LOW. Example 51.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 51, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01508 — Apply an 18 percent multiplier. Example 51.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 51, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 60.18}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 60.18}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01509 — Round the division result. Example 51.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 51, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 7}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01510 — Check whether a is even. Example 51.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 51, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01511 — Add two numbers. Example 52.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 52, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 60}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 60}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01512 — Subtract b from a. Example 52.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 52, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01513 — Multiply two numbers. Example 52.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 52, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 416}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 416}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01514 — Divide a by b. Example 52.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 52, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 6.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01515 — Calculate the remainder. Example 52.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 52, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01516 — Compare the numbers. Example 52.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 52, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01517 — Classify a as HIGH or LOW. Example 52.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 52, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01518 — Apply an 18 percent multiplier. Example 52.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 52, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 61.36}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 61.36}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01519 — Round the division result. Example 52.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 52, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 6}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01520 — Check whether a is even. Example 52.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 52, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01521 — Add two numbers. Example 53.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 53, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 62}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 62}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01522 — Subtract b from a. Example 53.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 53, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01523 — Multiply two numbers. Example 53.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 53, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 477}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 477}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01524 — Divide a by b. Example 53.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 53, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 5.888888888888889}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5.888888888888889}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01525 — Calculate the remainder. Example 53.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 53, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01526 — Compare the numbers. Example 53.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 53, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01527 — Classify a as HIGH or LOW. Example 53.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 53, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01528 — Apply an 18 percent multiplier. Example 53.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 53, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 62.54}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 62.54}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01529 — Round the division result. Example 53.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 53, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 6}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01530 — Check whether a is even. Example 53.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 53, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01531 — Add two numbers. Example 54.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 54, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 64}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 64}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01532 — Subtract b from a. Example 54.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 54, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01533 — Multiply two numbers. Example 54.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 54, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 540}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 540}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01534 — Divide a by b. Example 54.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 54, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 5.4}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5.4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01535 — Calculate the remainder. Example 54.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 54, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01536 — Compare the numbers. Example 54.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 54, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01537 — Classify a as HIGH or LOW. Example 54.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 54, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01538 — Apply an 18 percent multiplier. Example 54.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 54, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 63.72}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 63.72}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01539 — Round the division result. Example 54.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 54, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01540 — Check whether a is even. Example 54.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 54, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01541 — Add two numbers. Example 55.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 55, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 57}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 57}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01542 — Subtract b from a. Example 55.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 55, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 53}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 53}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01543 — Multiply two numbers. Example 55.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 55, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 110}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 110}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01544 — Divide a by b. Example 55.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 55, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 27.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 27.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01545 — Calculate the remainder. Example 55.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 55, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01546 — Compare the numbers. Example 55.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 55, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01547 — Classify a as HIGH or LOW. Example 55.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 55, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01548 — Apply an 18 percent multiplier. Example 55.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 55, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 64.89999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 64.89999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01549 — Round the division result. Example 55.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 55, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 28}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 28}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01550 — Check whether a is even. Example 55.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 55, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01551 — Add two numbers. Example 56.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 56, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 59}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 59}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01552 — Subtract b from a. Example 56.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 56, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 53}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 53}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01553 — Multiply two numbers. Example 56.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 56, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 168}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 168}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01554 — Divide a by b. Example 56.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 56, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 18.666666666666668}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 18.666666666666668}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01555 — Calculate the remainder. Example 56.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 56, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01556 — Compare the numbers. Example 56.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 56, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01557 — Classify a as HIGH or LOW. Example 56.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 56, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01558 — Apply an 18 percent multiplier. Example 56.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 56, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 66.08}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 66.08}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01559 — Round the division result. Example 56.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 56, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 19}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 19}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01560 — Check whether a is even. Example 56.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 56, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01561 — Add two numbers. Example 57.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 57, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 61}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 61}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01562 — Subtract b from a. Example 57.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 57, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 53}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 53}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01563 — Multiply two numbers. Example 57.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 57, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 228}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 228}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01564 — Divide a by b. Example 57.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 57, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 14.25}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 14.25}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01565 — Calculate the remainder. Example 57.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 57, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01566 — Compare the numbers. Example 57.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 57, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01567 — Classify a as HIGH or LOW. Example 57.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 57, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01568 — Apply an 18 percent multiplier. Example 57.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 57, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 67.25999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 67.25999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01569 — Round the division result. Example 57.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 57, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 14}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 14}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01570 — Check whether a is even. Example 57.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 57, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01571 — Add two numbers. Example 58.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 58, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 63}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 63}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01572 — Subtract b from a. Example 58.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 58, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 53}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 53}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01573 — Multiply two numbers. Example 58.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 58, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 290}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 290}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01574 — Divide a by b. Example 58.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 58, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 11.6}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 11.6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01575 — Calculate the remainder. Example 58.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 58, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01576 — Compare the numbers. Example 58.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 58, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01577 — Classify a as HIGH or LOW. Example 58.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 58, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01578 — Apply an 18 percent multiplier. Example 58.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 58, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 68.44}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 68.44}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01579 — Round the division result. Example 58.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 58, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 12}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 12}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01580 — Check whether a is even. Example 58.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 58, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01581 — Add two numbers. Example 59.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 59, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 65}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 65}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01582 — Subtract b from a. Example 59.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 59, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 53}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 53}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01583 — Multiply two numbers. Example 59.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 59, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 354}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 354}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01584 — Divide a by b. Example 59.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 59, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 9.833333333333334}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9.833333333333334}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01585 — Calculate the remainder. Example 59.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 59, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01586 — Compare the numbers. Example 59.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 59, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01587 — Classify a as HIGH or LOW. Example 59.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 59, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01588 — Apply an 18 percent multiplier. Example 59.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 59, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 69.61999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 69.61999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01589 — Round the division result. Example 59.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 59, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 10}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 10}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01590 — Check whether a is even. Example 59.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 59, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01591 — Add two numbers. Example 60.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 60, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 67}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 67}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01592 — Subtract b from a. Example 60.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 60, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 53}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 53}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01593 — Multiply two numbers. Example 60.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 60, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 420}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 420}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01594 — Divide a by b. Example 60.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 60, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 8.571428571428571}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8.571428571428571}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01595 — Calculate the remainder. Example 60.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 60, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 4}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01596 — Compare the numbers. Example 60.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 60, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01597 — Classify a as HIGH or LOW. Example 60.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 60, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01598 — Apply an 18 percent multiplier. Example 60.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 60, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 70.8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 70.8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01599 — Round the division result. Example 60.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 60, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 9}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01600 — Check whether a is even. Example 60.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 60, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01601 — Add two numbers. Example 61.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 61, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 69}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 69}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01602 — Subtract b from a. Example 61.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 61, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 53}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 53}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01603 — Multiply two numbers. Example 61.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 61, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 488}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 488}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01604 — Divide a by b. Example 61.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 61, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 7.625}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7.625}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01605 — Calculate the remainder. Example 61.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 61, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 5}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01606 — Compare the numbers. Example 61.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 61, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01607 — Classify a as HIGH or LOW. Example 61.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 61, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01608 — Apply an 18 percent multiplier. Example 61.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 61, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 71.97999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 71.97999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01609 — Round the division result. Example 61.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 61, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01610 — Check whether a is even. Example 61.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 61, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01611 — Add two numbers. Example 62.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 62, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 71}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 71}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01612 — Subtract b from a. Example 62.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 62, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 53}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 53}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01613 — Multiply two numbers. Example 62.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 62, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 558}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 558}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01614 — Divide a by b. Example 62.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 62, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 6.888888888888889}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6.888888888888889}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01615 — Calculate the remainder. Example 62.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 62, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01616 — Compare the numbers. Example 62.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 62, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01617 — Classify a as HIGH or LOW. Example 62.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 62, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01618 — Apply an 18 percent multiplier. Example 62.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 62, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 73.16}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 73.16}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01619 — Round the division result. Example 62.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 62, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 7}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01620 — Check whether a is even. Example 62.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 62, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01621 — Add two numbers. Example 63.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 63, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 73}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 73}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01622 — Subtract b from a. Example 63.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 63, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 53}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 53}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01623 — Multiply two numbers. Example 63.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 63, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 630}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 630}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01624 — Divide a by b. Example 63.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 63, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 6.3}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6.3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01625 — Calculate the remainder. Example 63.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 63, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01626 — Compare the numbers. Example 63.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 63, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01627 — Classify a as HIGH or LOW. Example 63.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 63, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01628 — Apply an 18 percent multiplier. Example 63.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 63, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 74.33999999999999}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 74.33999999999999}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01629 — Round the division result. Example 63.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 63, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 6}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01630 — Check whether a is even. Example 63.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 63, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01631 — Add two numbers. Example 64.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 64, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 66}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 66}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01632 — Subtract b from a. Example 64.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 64, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 62}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 62}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01633 — Multiply two numbers. Example 64.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 64, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 128}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 128}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01634 — Divide a by b. Example 64.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 64, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 32.0}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 32.0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01635 — Calculate the remainder. Example 64.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 64, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 0}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 0}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01636 — Compare the numbers. Example 64.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 64, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01637 — Classify a as HIGH or LOW. Example 64.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 64, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01638 — Apply an 18 percent multiplier. Example 64.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 64, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 75.52}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 75.52}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01639 — Round the division result. Example 64.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 64, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 32}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 32}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01640 — Check whether a is even. Example 64.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 64, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01641 — Add two numbers. Example 65.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 65, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 68}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 68}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01642 — Subtract b from a. Example 65.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 65, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 62}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 62}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01643 — Multiply two numbers. Example 65.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 65, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 195}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 195}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01644 — Divide a by b. Example 65.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 65, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 21.666666666666668}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 21.666666666666668}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01645 — Calculate the remainder. Example 65.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 65, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01646 — Compare the numbers. Example 65.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 65, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01647 — Classify a as HIGH or LOW. Example 65.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 65, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01648 — Apply an 18 percent multiplier. Example 65.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 65, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 76.7}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 76.7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01649 — Round the division result. Example 65.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 65, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 22}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 22}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01650 — Check whether a is even. Example 65.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 65, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01651 — Add two numbers. Example 66.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 66, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 70}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 70}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01652 — Subtract b from a. Example 66.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 66, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 62}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 62}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01653 — Multiply two numbers. Example 66.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 66, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 264}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 264}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01654 — Divide a by b. Example 66.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 66, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 16.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 16.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01655 — Calculate the remainder. Example 66.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 66, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01656 — Compare the numbers. Example 66.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 66, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01657 — Classify a as HIGH or LOW. Example 66.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 66, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01658 — Apply an 18 percent multiplier. Example 66.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 66, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 77.88}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 77.88}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01659 — Round the division result. Example 66.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 66, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 16}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 16}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01660 — Check whether a is even. Example 66.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 66, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01661 — Add two numbers. Example 67.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 67, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 72}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 72}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01662 — Subtract b from a. Example 67.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 67, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 62}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 62}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01663 — Multiply two numbers. Example 67.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 67, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 335}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 335}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01664 — Divide a by b. Example 67.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 67, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 13.4}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 13.4}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01665 — Calculate the remainder. Example 67.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 67, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01666 — Compare the numbers. Example 67.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 67, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01667 — Classify a as HIGH or LOW. Example 67.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 67, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01668 — Apply an 18 percent multiplier. Example 67.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 67, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 79.06}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 79.06}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01669 — Round the division result. Example 67.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 67, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 13}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 13}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01670 — Check whether a is even. Example 67.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 67, "b": 5}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01671 — Add two numbers. Example 68.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 68, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 74}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 74}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01672 — Subtract b from a. Example 68.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 68, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 62}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 62}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01673 — Multiply two numbers. Example 68.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 68, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 408}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 408}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01674 — Divide a by b. Example 68.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 68, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 11.333333333333334}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 11.333333333333334}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01675 — Calculate the remainder. Example 68.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 68, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01676 — Compare the numbers. Example 68.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 68, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01677 — Classify a as HIGH or LOW. Example 68.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 68, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01678 — Apply an 18 percent multiplier. Example 68.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 68, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 80.24}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 80.24}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01679 — Round the division result. Example 68.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 68, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 11}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 11}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01680 — Check whether a is even. Example 68.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 68, "b": 6}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01681 — Add two numbers. Example 69.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 69, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 76}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 76}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01682 — Subtract b from a. Example 69.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 69, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 62}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 62}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01683 — Multiply two numbers. Example 69.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 69, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 483}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 483}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01684 — Divide a by b. Example 69.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 69, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 9.857142857142858}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9.857142857142858}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01685 — Calculate the remainder. Example 69.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 69, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 6}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01686 — Compare the numbers. Example 69.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 69, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01687 — Classify a as HIGH or LOW. Example 69.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 69, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01688 — Apply an 18 percent multiplier. Example 69.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 69, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 81.42}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 81.42}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01689 — Round the division result. Example 69.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 69, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 10}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 10}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01690 — Check whether a is even. Example 69.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 69, "b": 7}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01691 — Add two numbers. Example 70.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 70, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 78}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 78}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01692 — Subtract b from a. Example 70.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 70, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 62}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 62}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01693 — Multiply two numbers. Example 70.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 70, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 560}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 560}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01694 — Divide a by b. Example 70.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 70, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 8.75}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8.75}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01695 — Calculate the remainder. Example 70.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 70, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 6}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01696 — Compare the numbers. Example 70.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 70, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01697 — Classify a as HIGH or LOW. Example 70.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 70, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01698 — Apply an 18 percent multiplier. Example 70.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 70, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 82.6}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 82.6}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01699 — Round the division result. Example 70.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 70, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 9}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 9}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01700 — Check whether a is even. Example 70.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 70, "b": 8}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01701 — Add two numbers. Example 71.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 71, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 80}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 80}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01702 — Subtract b from a. Example 71.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 71, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 62}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 62}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01703 — Multiply two numbers. Example 71.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 71, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 639}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 639}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01704 — Divide a by b. Example 71.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 71, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 7.888888888888889}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7.888888888888889}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01705 — Calculate the remainder. Example 71.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 71, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01706 — Compare the numbers. Example 71.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 71, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01707 — Classify a as HIGH or LOW. Example 71.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 71, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01708 — Apply an 18 percent multiplier. Example 71.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 71, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 83.78}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 83.78}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01709 — Round the division result. Example 71.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 71, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 8}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 8}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01710 — Check whether a is even. Example 71.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 71, "b": 9}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01711 — Add two numbers. Example 72.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 72, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 82}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 82}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01712 — Subtract b from a. Example 72.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 72, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 62}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 62}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01713 — Multiply two numbers. Example 72.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 72, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 720}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 720}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01714 — Divide a by b. Example 72.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 72, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 7.2}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7.2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01715 — Calculate the remainder. Example 72.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 72, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01716 — Compare the numbers. Example 72.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 72, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01717 — Classify a as HIGH or LOW. Example 72.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 72, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01718 — Apply an 18 percent multiplier. Example 72.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 72, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 84.96}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 84.96}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01719 — Round the division result. Example 72.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 72, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 7}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 7}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01720 — Check whether a is even. Example 72.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 72, "b": 10}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01721 — Add two numbers. Example 73.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 73, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 75}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 75}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01722 — Subtract b from a. Example 73.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 73, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 71}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 71}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01723 — Multiply two numbers. Example 73.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 73, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 146}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 146}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01724 — Divide a by b. Example 73.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 73, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 36.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 36.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01725 — Calculate the remainder. Example 73.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 73, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 1}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 1}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01726 — Compare the numbers. Example 73.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 73, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01727 — Classify a as HIGH or LOW. Example 73.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 73, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01728 — Apply an 18 percent multiplier. Example 73.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 73, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 86.14}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 86.14}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01729 — Round the division result. Example 73.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 73, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 36}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 36}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01730 — Check whether a is even. Example 73.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 73, "b": 2}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01731 — Add two numbers. Example 74.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 74, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 77}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 77}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01732 — Subtract b from a. Example 74.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 74, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 71}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 71}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01733 — Multiply two numbers. Example 74.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 74, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 222}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 222}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01734 — Divide a by b. Example 74.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 74, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 24.666666666666668}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 24.666666666666668}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01735 — Calculate the remainder. Example 74.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 74, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 2}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 2}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01736 — Compare the numbers. Example 74.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 74, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01737 — Classify a as HIGH or LOW. Example 74.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 74, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01738 — Apply an 18 percent multiplier. Example 74.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 74, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 87.32}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 87.32}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01739 — Round the division result. Example 74.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 74, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 25}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 25}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01740 — Check whether a is even. Example 74.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 74, "b": 3}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01741 — Add two numbers. Example 75.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 75, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a + payload.b }
```

### Expected Output
```json
{"result": 79}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 79}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01742 — Subtract b from a. Example 75.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 75, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a - payload.b }
```

### Expected Output
```json
{"result": 71}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 71}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01743 — Multiply two numbers. Example 75.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 75, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * payload.b }
```

### Expected Output
```json
{"result": 300}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 300}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01744 — Divide a by b. Example 75.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 75, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a / payload.b }
```

### Expected Output
```json
{"result": 18.75}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 18.75}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01745 — Calculate the remainder. Example 75.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 75, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a mod payload.b }
```

### Expected Output
```json
{"result": 3}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": 3}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01746 — Compare the numbers. Example 75.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 75, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a > payload.b }
```

### Expected Output
```json
{"result": true}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": true}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01747 — Classify a as HIGH or LOW. Example 75.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 75, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: if (payload.a >= 50) "HIGH" else "LOW" }
```

### Expected Output
```json
{"result": "HIGH"}
```

### Explanation
What this teaches: if/else evaluates a condition and selects one result branch.

How it works: 1. Evaluate the condition. 2. Use the first branch when true. 3. Otherwise use else. 4. Return the selected value.

Why this output: the expression is evaluated against the supplied input and produces {"result": "HIGH"}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01748 — Apply an 18 percent multiplier. Example 75.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 75, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: payload.a * 1.18 }
```

### Expected Output
```json
{"result": 88.5}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 88.5}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01749 — Round the division result. Example 75.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 75, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: round(payload.a / payload.b) }
```

### Expected Output
```json
{"result": 19}
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces {"result": 19}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-01750 — Check whether a is even. Example 75.

**Difficulty:** Easy
**Topic:** Numbers & Conditional Logic

### Input
```json
{"a": 75, "b": 4}
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
{ result: (payload.a mod 2) == 0 }
```

### Expected Output
```json
{"result": false}
```

### Explanation
What this teaches: mod returns the remainder and is useful for parity and repeating-pattern checks.

How it works: 1. Evaluate both operands. 2. Perform the division. 3. Keep the remainder. 4. Return or compare it.

Why this output: the expression is evaluated against the supplied input and produces {"result": false}.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Validate numeric types and division-by-zero cases.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
