# Easy DataWeave Questions 02001-02250

Questions 2001–2250 of 4000 in the Easy level.

## DW-02001 — Double every item. Example 1.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02002 — Increment every item. Example 1.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02003 — Keep values greater than five. Example 1.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02004 — Keep even values. Example 1.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02005 — Count items. Example 1.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02006 — Sum values. Example 1.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02007 — Append 99. Example 1.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02008 — Prepend 0. Example 1.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02009 — Return the first item. Example 1.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02010 — Return the last item. Example 1.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02011 — Double every item. Example 2.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02012 — Increment every item. Example 2.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[6, 11, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 11, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02013 — Keep values greater than five. Example 2.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 15]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02014 — Keep even values. Example 2.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02015 — Count items. Example 2.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02016 — Sum values. Example 2.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02017 — Append 99. Example 2.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[5, 10, 15, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [5, 10, 15, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02018 — Prepend 0. Example 2.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 5, 10, 15]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 5, 10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02019 — Return the first item. Example 2.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
5
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 5.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02020 — Return the last item. Example 2.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
15
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 15.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02021 — Double every item. Example 3.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[0, 2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02022 — Increment every item. Example 3.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[1, 2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02023 — Keep values greater than five. Example 3.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02024 — Keep even values. Example 3.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[0, 2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02025 — Count items. Example 3.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02026 — Sum values. Example 3.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02027 — Append 99. Example 3.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[0, 1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02028 — Prepend 0. Example 3.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02029 — Return the first item. Example 3.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
0
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 0.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02030 — Return the last item. Example 3.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02031 — Double every item. Example 4.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[4, 8, 12, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 8, 12, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02032 — Increment every item. Example 4.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[3, 5, 7, 9]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [3, 5, 7, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02033 — Keep values greater than five. Example 4.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02034 — Keep even values. Example 4.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2, 4, 6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02035 — Count items. Example 4.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02036 — Sum values. Example 4.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02037 — Append 99. Example 4.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[2, 4, 6, 8, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02038 — Prepend 0. Example 4.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 2, 4, 6, 8]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02039 — Return the first item. Example 4.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02040 — Return the last item. Example 4.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
8
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 8.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02041 — Double every item. Example 5.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[20, 40, 60]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [20, 40, 60].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02042 — Increment every item. Example 5.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[11, 21, 31]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [11, 21, 31].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02043 — Keep values greater than five. Example 5.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02044 — Keep even values. Example 5.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02045 — Count items. Example 5.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02046 — Sum values. Example 5.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
60
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 60.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02047 — Append 99. Example 5.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[10, 20, 30, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02048 — Prepend 0. Example 5.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 10, 20, 30]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02049 — Return the first item. Example 5.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
10
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 10.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02050 — Return the last item. Example 5.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02051 — Double every item. Example 6.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[-4, -2, 0, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-4, -2, 0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02052 — Increment every item. Example 6.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[-1, 0, 1, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-1, 0, 1, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02053 — Keep values greater than five. Example 6.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02054 — Keep even values. Example 6.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[-2, 0]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [-2, 0].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02055 — Count items. Example 6.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02056 — Sum values. Example 6.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02057 — Append 99. Example 6.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[-2, -1, 0, 1, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [-2, -1, 0, 1, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02058 — Prepend 0. Example 6.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, -2, -1, 0, 1]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, -2, -1, 0, 1].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02059 — Return the first item. Example 6.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02060 — Return the last item. Example 6.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02061 — Double every item. Example 7.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[6, 12, 18]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 12, 18].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02062 — Increment every item. Example 7.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[4, 7, 10]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 7, 10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02063 — Keep values greater than five. Example 7.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 9]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02064 — Keep even values. Example 7.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[6]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02065 — Count items. Example 7.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02066 — Sum values. Example 7.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
18
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 18.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02067 — Append 99. Example 7.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[3, 6, 9, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [3, 6, 9, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02068 — Prepend 0. Example 7.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 3, 6, 9]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 3, 6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02069 — Return the first item. Example 7.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02070 — Return the last item. Example 7.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
9
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 9.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02071 — Double every item. Example 8.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[200, 400, 600]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [200, 400, 600].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02072 — Increment every item. Example 8.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[101, 201, 301]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [101, 201, 301].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02073 — Keep values greater than five. Example 8.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02074 — Keep even values. Example 8.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02075 — Count items. Example 8.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02076 — Sum values. Example 8.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
600
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 600.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02077 — Append 99. Example 8.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[100, 200, 300, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02078 — Prepend 0. Example 8.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 100, 200, 300]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02079 — Return the first item. Example 8.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
100
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 100.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02080 — Return the last item. Example 8.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
300
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 300.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02081 — Double every item. Example 9.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 10, 20, 40]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 10, 20, 40].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02082 — Increment every item. Example 9.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 6, 11, 21]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 6, 11, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02083 — Keep values greater than five. Example 9.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02084 — Keep even values. Example 9.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02085 — Count items. Example 9.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02086 — Sum values. Example 9.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
36
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 36.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02087 — Append 99. Example 9.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 5, 10, 20, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 5, 10, 20, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02088 — Prepend 0. Example 9.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 5, 10, 20]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 5, 10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02089 — Return the first item. Example 9.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02090 — Return the last item. Example 9.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02091 — Double every item. Example 10.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[14, 28, 42]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [14, 28, 42].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02092 — Increment every item. Example 10.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[8, 15, 22]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [8, 15, 22].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02093 — Keep values greater than five. Example 10.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[7, 14, 21]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02094 — Keep even values. Example 10.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[14]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [14].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02095 — Count items. Example 10.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02096 — Sum values. Example 10.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
42
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 42.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02097 — Append 99. Example 10.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[7, 14, 21, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02098 — Prepend 0. Example 10.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 7, 14, 21]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02099 — Return the first item. Example 10.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
7
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 7.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02100 — Return the last item. Example 10.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
21
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 21.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02101 — Double every item. Example 11.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02102 — Increment every item. Example 11.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02103 — Keep values greater than five. Example 11.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02104 — Keep even values. Example 11.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02105 — Count items. Example 11.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02106 — Sum values. Example 11.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02107 — Append 99. Example 11.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02108 — Prepend 0. Example 11.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02109 — Return the first item. Example 11.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02110 — Return the last item. Example 11.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02111 — Double every item. Example 12.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02112 — Increment every item. Example 12.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[6, 11, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 11, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02113 — Keep values greater than five. Example 12.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 15]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02114 — Keep even values. Example 12.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02115 — Count items. Example 12.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02116 — Sum values. Example 12.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02117 — Append 99. Example 12.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[5, 10, 15, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [5, 10, 15, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02118 — Prepend 0. Example 12.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 5, 10, 15]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 5, 10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02119 — Return the first item. Example 12.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
5
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 5.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02120 — Return the last item. Example 12.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
15
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 15.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02121 — Double every item. Example 13.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[0, 2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02122 — Increment every item. Example 13.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[1, 2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02123 — Keep values greater than five. Example 13.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02124 — Keep even values. Example 13.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[0, 2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02125 — Count items. Example 13.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02126 — Sum values. Example 13.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02127 — Append 99. Example 13.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[0, 1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02128 — Prepend 0. Example 13.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02129 — Return the first item. Example 13.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
0
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 0.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02130 — Return the last item. Example 13.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02131 — Double every item. Example 14.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[4, 8, 12, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 8, 12, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02132 — Increment every item. Example 14.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[3, 5, 7, 9]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [3, 5, 7, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02133 — Keep values greater than five. Example 14.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02134 — Keep even values. Example 14.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2, 4, 6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02135 — Count items. Example 14.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02136 — Sum values. Example 14.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02137 — Append 99. Example 14.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[2, 4, 6, 8, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02138 — Prepend 0. Example 14.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 2, 4, 6, 8]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02139 — Return the first item. Example 14.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02140 — Return the last item. Example 14.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
8
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 8.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02141 — Double every item. Example 15.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[20, 40, 60]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [20, 40, 60].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02142 — Increment every item. Example 15.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[11, 21, 31]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [11, 21, 31].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02143 — Keep values greater than five. Example 15.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02144 — Keep even values. Example 15.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02145 — Count items. Example 15.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02146 — Sum values. Example 15.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
60
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 60.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02147 — Append 99. Example 15.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[10, 20, 30, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02148 — Prepend 0. Example 15.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 10, 20, 30]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02149 — Return the first item. Example 15.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
10
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 10.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02150 — Return the last item. Example 15.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02151 — Double every item. Example 16.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[-4, -2, 0, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-4, -2, 0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02152 — Increment every item. Example 16.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[-1, 0, 1, 2]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [-1, 0, 1, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02153 — Keep values greater than five. Example 16.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02154 — Keep even values. Example 16.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[-2, 0]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [-2, 0].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02155 — Count items. Example 16.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02156 — Sum values. Example 16.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02157 — Append 99. Example 16.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[-2, -1, 0, 1, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [-2, -1, 0, 1, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02158 — Prepend 0. Example 16.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, -2, -1, 0, 1]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, -2, -1, 0, 1].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02159 — Return the first item. Example 16.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
-2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces -2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02160 — Return the last item. Example 16.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[-2, -1, 0, 1]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02161 — Double every item. Example 17.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[6, 12, 18]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 12, 18].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02162 — Increment every item. Example 17.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[4, 7, 10]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 7, 10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02163 — Keep values greater than five. Example 17.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 9]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02164 — Keep even values. Example 17.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[6]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02165 — Count items. Example 17.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02166 — Sum values. Example 17.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
18
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 18.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02167 — Append 99. Example 17.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[3, 6, 9, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [3, 6, 9, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02168 — Prepend 0. Example 17.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 3, 6, 9]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 3, 6, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02169 — Return the first item. Example 17.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02170 — Return the last item. Example 17.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[3, 6, 9]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
9
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 9.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02171 — Double every item. Example 18.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[200, 400, 600]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [200, 400, 600].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02172 — Increment every item. Example 18.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[101, 201, 301]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [101, 201, 301].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02173 — Keep values greater than five. Example 18.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02174 — Keep even values. Example 18.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[100, 200, 300]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02175 — Count items. Example 18.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02176 — Sum values. Example 18.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
600
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 600.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02177 — Append 99. Example 18.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[100, 200, 300, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [100, 200, 300, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02178 — Prepend 0. Example 18.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 100, 200, 300]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 100, 200, 300].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02179 — Return the first item. Example 18.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
100
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 100.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02180 — Return the last item. Example 18.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[100, 200, 300]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
300
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 300.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02181 — Double every item. Example 19.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 10, 20, 40]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 10, 20, 40].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02182 — Increment every item. Example 19.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 6, 11, 21]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 6, 11, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02183 — Keep values greater than five. Example 19.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02184 — Keep even values. Example 19.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02185 — Count items. Example 19.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02186 — Sum values. Example 19.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
36
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 36.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02187 — Append 99. Example 19.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 5, 10, 20, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 5, 10, 20, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02188 — Prepend 0. Example 19.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 5, 10, 20]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 5, 10, 20].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02189 — Return the first item. Example 19.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02190 — Return the last item. Example 19.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 5, 10, 20]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02191 — Double every item. Example 20.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[14, 28, 42]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [14, 28, 42].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02192 — Increment every item. Example 20.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[8, 15, 22]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [8, 15, 22].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02193 — Keep values greater than five. Example 20.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[7, 14, 21]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02194 — Keep even values. Example 20.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[14]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [14].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02195 — Count items. Example 20.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02196 — Sum values. Example 20.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
42
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 42.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02197 — Append 99. Example 20.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[7, 14, 21, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [7, 14, 21, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02198 — Prepend 0. Example 20.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 7, 14, 21]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 7, 14, 21].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02199 — Return the first item. Example 20.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
7
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 7.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02200 — Return the last item. Example 20.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[7, 14, 21]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
21
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 21.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02201 — Double every item. Example 21.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02202 — Increment every item. Example 21.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02203 — Keep values greater than five. Example 21.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02204 — Keep even values. Example 21.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02205 — Count items. Example 21.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02206 — Sum values. Example 21.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02207 — Append 99. Example 21.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02208 — Prepend 0. Example 21.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02209 — Return the first item. Example 21.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
1
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 1.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02210 — Return the last item. Example 21.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02211 — Double every item. Example 22.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02212 — Increment every item. Example 22.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[6, 11, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [6, 11, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02213 — Keep values greater than five. Example 22.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 15]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02214 — Keep even values. Example 22.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02215 — Count items. Example 22.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02216 — Sum values. Example 22.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02217 — Append 99. Example 22.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[5, 10, 15, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [5, 10, 15, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02218 — Prepend 0. Example 22.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 5, 10, 15]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 5, 10, 15].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02219 — Return the first item. Example 22.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
5
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 5.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02220 — Return the last item. Example 22.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[5, 10, 15]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
15
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 15.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02221 — Double every item. Example 23.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[0, 2, 4, 6]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02222 — Increment every item. Example 23.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[1, 2, 3, 4]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [1, 2, 3, 4].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02223 — Keep values greater than five. Example 23.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02224 — Keep even values. Example 23.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[0, 2]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [0, 2].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02225 — Count items. Example 23.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02226 — Sum values. Example 23.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
6
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 6.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02227 — Append 99. Example 23.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[0, 1, 2, 3, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 1, 2, 3, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02228 — Prepend 0. Example 23.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 0, 1, 2, 3]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 0, 1, 2, 3].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02229 — Return the first item. Example 23.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
0
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 0.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02230 — Return the last item. Example 23.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[0, 1, 2, 3]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
3
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02231 — Double every item. Example 24.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[4, 8, 12, 16]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [4, 8, 12, 16].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02232 — Increment every item. Example 24.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[3, 5, 7, 9]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [3, 5, 7, 9].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02233 — Keep values greater than five. Example 24.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02234 — Keep even values. Example 24.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[2, 4, 6, 8]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02235 — Count items. Example 24.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
4
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 4.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02236 — Sum values. Example 24.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
20
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 20.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02237 — Append 99. Example 24.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[2, 4, 6, 8, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [2, 4, 6, 8, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02238 — Prepend 0. Example 24.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 2, 4, 6, 8]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 2, 4, 6, 8].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02239 — Return the first item. Example 24.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
2
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 2.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02240 — Return the last item. Example 24.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[2, 4, 6, 8]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
8
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 8.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02241 — Double every item. Example 25.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ * 2)
```

### Expected Output
```json
[20, 40, 60]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [20, 40, 60].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02242 — Increment every item. Example 25.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload map ($ + 1)
```

### Expected Output
```json
[11, 21, 31]
```

### Explanation
What this teaches: map evaluates an expression for every array item and returns a new array.

How it works: 1. Read the input array. 2. Iterate with map. 3. Evaluate the expression for each item. 4. Collect the transformed values.

Why this output: the expression is evaluated against the supplied input and produces [11, 21, 31].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02243 — Keep values greater than five. Example 25.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter ($ > 5)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02244 — Keep even values. Example 25.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload filter (($ mod 2) == 0)
```

### Expected Output
```json
[10, 20, 30]
```

### Explanation
What this teaches: filter keeps only elements whose predicate evaluates to true.

How it works: 1. Read each item. 2. Evaluate the condition. 3. Keep true results. 4. Discard false results.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02245 — Count items. Example 25.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sizeOf(payload)
```

### Expected Output
```json
3
```

### Explanation
What this teaches: sizeOf returns the size of a string or collection.

How it works: 1. Select the value. 2. Pass it to sizeOf. 3. DataWeave calculates the size. 4. Return the number.

Why this output: the expression is evaluated against the supplied input and produces 3.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02246 — Sum values. Example 25.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
sum(payload)
```

### Expected Output
```json
60
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 60.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02247 — Append 99. Example 25.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload ++ [99]
```

### Expected Output
```json
[10, 20, 30, 99]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [10, 20, 30, 99].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02248 — Prepend 0. Example 25.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
[0] ++ payload
```

### Expected Output
```json
[0, 10, 20, 30]
```

### Explanation
What this teaches: The ++ operator combines compatible DataWeave values such as strings, arrays, and objects.

How it works: 1. Evaluate the left value. 2. Evaluate the right value. 3. Combine them according to their types. 4. Return the result.

Why this output: the expression is evaluated against the supplied input and produces [0, 10, 20, 30].

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02249 — Return the first item. Example 25.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[0]
```

### Expected Output
```json
10
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 10.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---

## DW-02250 — Return the last item. Example 25.

**Difficulty:** Easy
**Topic:** Arrays & Collections

### Input
```json
[10, 20, 30]
```

### DataWeave Answer
```dataweave
%dw 2.0
output application/json
---
payload[-1]
```

### Expected Output
```json
30
```

### Explanation
What this teaches: This transformation maps an input contract to a required output shape.

How it works: 1. Read payload. 2. Evaluate fields and functions. 3. Construct the declared output.

Why this output: the expression is evaluated against the supplied input and produces 30.

Practical note: verify null handling, data types, schema names, and edge cases before using the pattern in a MuleSoft flow.

### Common Mistakes
Check empty-array behavior and element types.

### Interview Tip
Be ready to explain the expression, input-to-output transformation, null/empty behavior, and one production use case.

---
